question,answer_llm,answer_orig,document
I just found this course — can I still sign up and start now?,"Yes — you can start now.

The course materials are available, and you can begin whenever you want. If you want a certificate, make sure to submit your project while submissions are still open.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
Is it too late to join the course if I missed the beginning?,"No, it’s not too late to join. You can start whenever you want, and the course materials and videos are available. If you want a certificate, you need to finish the course with the live cohort and submit your project before submissions close.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
Can I still take this course even though I discovered it late?,"Yes, you can still join the course even if you discovered it late. If you want to receive a certificate, make sure you submit your project while submissions are still being accepted.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
"If I join the course now, can I still get a certificate somehow?","Yes, but only if the course is still running as a **live cohort**.

To get a certificate, you need to:
- **submit your project while submissions are still open**, and
- **complete the required peer reviews**.

You **can’t get a certificate in self-paced mode**.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
What do I need to do to be eligible for the certificate if I’m joining late?,"You need to finish the course with the live cohort and pass the Capstone project. Homework is not mandatory for the certificate, but you must also complete the peer-review requirement: after submitting your project, you need to review 3 fellow students’ projects.","Yes, but if you want to receive a certificate, you need to submit your project while we’re still accepting submissions.",74eb249bbf
"I registered for the LLM Zoomcamp, but I still didn’t get any confirmation email — should I be worried?","You don’t need to worry. You’re already accepted — the confirmation email isn’t required.

You can start learning and submitting homework while the form is open, and registration is only used to gauge interest before the start date.",You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
Do I actually need a confirmation message to start the course and submit homework?,"No — you don’t need a confirmation message. You’re accepted, and you can start learning and submitting homework while the form is open, even without registering.",You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
Is there some accepted or registered list I’m supposed to be on before doing the assignments?,No—you don’t need to be on any accepted or registered list before doing the assignments. You can just start learning and submitting homework while the submission form is open. Registration is only used to gauge interest before the start date.,You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
What’s the point of registering if I can already begin learning and turning in homework without it?,"Registration is just to gauge interest before the start date. You’re already accepted, and it’s not checked against a registered list, so you can start learning and submit homework while the form is open even without registering.",You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
"If the registration form is only for interest, does that mean I can just join and start right away?","Yes — the registration form is only to gauge interest. You can still just join and start learning, and even submit homework while the form is open. You don’t need to wait for a confirmation email.",You don't need it. You're accepted. You can also just start learning and submitting homework (while the form is open) without registering. It is not checked against any registered list. Registration is just to gauge interest before the start date.,977bf7786c
"How do I join the Office Hours or live workshop stream if I’m a student, and where is the video link usually posted?","If you’re a student, you join the Office Hours or live workshop via **YouTube Live** and ask questions in **Slido** (the link is pinned in the chat when the session is live).

The **video link** is usually posted in the **announcements channel on Telegram and Slack** before the session starts. You can also watch on the **DataTalksClub YouTube Channel**.","The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
"Is the Zoom link for live sessions available to everyone, or only to instructors and TAs?",Only to instructors/presenters/TAs. Students watch via YouTube Live and can ask questions through Slido.,"The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
"If I can’t use Zoom for the workshop, what’s the usual way students watch and ask questions during the session?","Students usually watch the session on **YouTube Live** and ask questions via **Slido**. The Slido link is pinned in the chat when the session is live, and the video URL is usually posted in the **announcements channel on Telegram and Slack** before it starts.","The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
Where should I look for the live session URL before the Office Hours start?,"Look in the **announcements channel on Telegram and Slack** before it begins. The live session URL should be posted there, and you can also watch on the DataTalksClub **YouTube Channel**.","The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
"Should I ask questions in the chat during a live session, or is there a better place to send them?",Yes—please don’t post questions in the live chat. Use **Slido** instead; the Slido link is pinned in the chat when the session is live.,"The zoom link is only published to instructors/presenters/TAs.

Students participate via YouTube Live and submit questions to Slido (link is pinned in the chat when live). The video URL should be posted in the [announcements channel on Telegram and Slack](https://t.me/dezoomcamp) before it begins. You can also watch live on the DataTalksClub [YouTube Channel](https://www.youtube.com/c/DataTalksClub).

Don’t post questions in chat as they may be missed if the room is very active.",489dd1c9d9
"Where should I begin if I’m just joining the LLM Zoomcamp, and what pages or repo should I check first?","If you’re just joining, begin with:

1. The [LLM Zoomcamp docs](https://datatalks.club/docs/courses/llm-zoomcamp/)
2. The [general Zoomcamp logistics docs](https://datatalks.club/docs/courses/zoomcamp-logistics/)
3. The main [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp)

For the weekly workflow, use the GitHub repo first since it’s the main entry point and usually stays more up to date than the YouTube playlist. Open the lesson folders there; each lesson page has the relevant videos linked at the top.

If you’re specifically starting Module 1 in the 2026 cohort, check:
- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

If you want, I can also give you a “first 30 minutes” checklist for getting started.","Start with the [LLM Zoomcamp docs](https://datatalks.club/docs/courses/llm-zoomcamp/), the [general Zoomcamp logistics docs](https://datatalks.club/docs/courses/zoomcamp-logistics/), and the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp).

You can start whenever you want. The videos and GitHub materials are available, and the deadlines are listed in the [course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

A typical workflow is:

1. Watch the lesson videos.
2. Work through the lesson notebooks/code.
3. Read the homework instructions on GitHub.
4. Submit answers through the course platform before the deadline.

Homework is similar to the lesson flow, but uses a different dataset or slightly different task.",04919992b3
"What’s the usual weekly flow for this course — do I watch videos first, then do notebooks, then homework?","Yes — the usual workflow is:

1. Watch the lesson videos  
2. Work through the lesson notebooks/code  
3. Read the homework instructions on GitHub  
4. Submit your answers on the course platform before the deadline  

So yes: videos first, then notebooks, then homework.","Start with the [LLM Zoomcamp docs](https://datatalks.club/docs/courses/llm-zoomcamp/), the [general Zoomcamp logistics docs](https://datatalks.club/docs/courses/zoomcamp-logistics/), and the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp).

You can start whenever you want. The videos and GitHub materials are available, and the deadlines are listed in the [course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

A typical workflow is:

1. Watch the lesson videos.
2. Work through the lesson notebooks/code.
3. Read the homework instructions on GitHub.
4. Submit answers through the course platform before the deadline.

Homework is similar to the lesson flow, but uses a different dataset or slightly different task.",04919992b3
"Can I start LLM Zoomcamp anytime, or do I need to wait for a specific date?","You can start whenever you want. The course materials and videos are available, and you don’t need to wait for a specific date to begin learning or submitting homework while the submission form is open.","Start with the [LLM Zoomcamp docs](https://datatalks.club/docs/courses/llm-zoomcamp/), the [general Zoomcamp logistics docs](https://datatalks.club/docs/courses/zoomcamp-logistics/), and the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp).

You can start whenever you want. The videos and GitHub materials are available, and the deadlines are listed in the [course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

A typical workflow is:

1. Watch the lesson videos.
2. Work through the lesson notebooks/code.
3. Read the homework instructions on GitHub.
4. Submit answers through the course platform before the deadline.

Homework is similar to the lesson flow, but uses a different dataset or slightly different task.",04919992b3
Where do I find the deadlines and submit homework answers for the course?,"You can find the deadlines in the **LLM Zoomcamp course management platform**:

- https://courses.datatalks.club/llm-zoomcamp-2026/

That same platform is where you **submit your homework answers**. The GitHub repo has the homework instructions and extra context, but the **submission form and deadlines are on the course platform**.","Start with the [LLM Zoomcamp docs](https://datatalks.club/docs/courses/llm-zoomcamp/), the [general Zoomcamp logistics docs](https://datatalks.club/docs/courses/zoomcamp-logistics/), and the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp).

You can start whenever you want. The videos and GitHub materials are available, and the deadlines are listed in the [course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

A typical workflow is:

1. Watch the lesson videos.
2. Work through the lesson notebooks/code.
3. Read the homework instructions on GitHub.
4. Submit answers through the course platform before the deadline.

Homework is similar to the lesson flow, but uses a different dataset or slightly different task.",04919992b3
"Is the homework basically the same as the lesson exercises, or does it use different data/tasks?","The homework follows the lesson workflow, but usually uses a different dataset or asks you to apply the same idea in a slightly different way.","Start with the [LLM Zoomcamp docs](https://datatalks.club/docs/courses/llm-zoomcamp/), the [general Zoomcamp logistics docs](https://datatalks.club/docs/courses/zoomcamp-logistics/), and the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp).

You can start whenever you want. The videos and GitHub materials are available, and the deadlines are listed in the [course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

A typical workflow is:

1. Watch the lesson videos.
2. Work through the lesson notebooks/code.
3. Read the homework instructions on GitHub.
4. Submit answers through the course platform before the deadline.

Homework is similar to the lesson flow, but uses a different dataset or slightly different task.",04919992b3
How can I find my own entry on the leaderboard if I can’t spot my name right away?,"Click the **“Jump to your record on the leaderboard”** link. Your account is automatically assigned a random display name, so that link will take you straight to your entry.","When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
"Why do I have a random name on the leaderboard, and can I change it?","When you set up your account, you’re automatically assigned a random public display name on the leaderboard, like “Lucid Elbakyan.”

Yes, you can change it:
- Click **Edit Course Profile**
- Change the **first field** to whatever nickname you want shown publicly on the leaderboard

If you want, you can also set your **official name** in the **second field** for your certificate.","When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
Where do I check what my displayed name is in the course profile?,Click **“Edit Course Profile”**. Your **first field** is your public nickname/displayed name for the leaderboard.,"When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
"If I want a different nickname on the leaderboard, which profile field should I edit?",Edit the **first field** in **Edit Course Profile** — that’s your **nickname/display name** used on the leaderboard.,"When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
"What name will show on my certificate, and do I need to update it somewhere?","The name on your certificate will be the **second field in “Edit Course Profile”** — your official name as in your ID documents. If you leave it unchanged, it may show the default name like **“Lucid Elbakyan.”**

Yes, you need to **update it in “Edit Course Profile”** if you want a different name on the certificate.","When you set up your account, you are automatically assigned a random name, such as “Lucid Elbakyan.” Click on the ""Jump to your record on the leaderboard"" link to find your entry.

If you want to see what your Display name is, click on the ""Edit Course Profile"" button.

<{IMAGE:image_1}>

- **First field:** This is your nickname/displayed name. You can change it if you want to be known by your Slack username, GitHub username, or any other nickname of your choice. This is useful if you want to remain anonymous.
- **Second field:** Change this to your official name as in your identification documents—passport, national ID card, driver's license, etc. This is mandatory if you do not want ""Lucid Elbakyan"" on your certificate. This name will appear on your Certificate!",c2903069a0
Can I do this course on my own schedule and still receive a certificate at the end?,No. You can only get a certificate if you finish the course with a live cohort; self-paced mode does not include certificates.,"No, you can only get a certificate if you finish the course with a ""live"" cohort.

We don't award certificates for the self-paced mode. The reason is you need to peer-review 3 capstone(s) after submitting your project.

You can only peer-review projects at the time the course is running; after the form is closed and the peer-review list is compiled.",69d122f12e
"If I take the self-paced version, is there any way to get the course certificate later?","No. You can only get a certificate by finishing the course with a live cohort; self-paced mode does not award certificates, and there isn’t a later way to get one after that.","No, you can only get a certificate if you finish the course with a ""live"" cohort.

We don't award certificates for the self-paced mode. The reason is you need to peer-review 3 capstone(s) after submitting your project.

You can only peer-review projects at the time the course is running; after the form is closed and the peer-review list is compiled.",69d122f12e
Do certificates only count for people who finish with a live cohort?,Yes. Certificates are only awarded if you finish the course with a live cohort. Self-paced mode does not qualify.,"No, you can only get a certificate if you finish the course with a ""live"" cohort.

We don't award certificates for the self-paced mode. The reason is you need to peer-review 3 capstone(s) after submitting your project.

You can only peer-review projects at the time the course is running; after the form is closed and the peer-review list is compiled.",69d122f12e
Why is there no certificate for the self-paced track in this course?,"There is no certificate for the self-paced track because the course requires **peer-reviewing 3 capstones** after submitting your project, and that peer-review process is only available while a **live cohort** is running. Once the course form is closed and the peer-review list is compiled, self-paced participants can’t complete that requirement, so certificates aren’t awarded in self-paced mode.","No, you can only get a certificate if you finish the course with a ""live"" cohort.

We don't award certificates for the self-paced mode. The reason is you need to peer-review 3 capstone(s) after submitting your project.

You can only peer-review projects at the time the course is running; after the form is closed and the peer-review list is compiled.",69d122f12e
Is peer-review of the capstone projects required in order to earn the certificate?,"Yes. Peer-reviewing 3 capstone projects is required to earn the certificate. If you don’t complete the peer-review step, you may fail to get the certificate.","No, you can only get a certificate if you finish the course with a ""live"" cohort.

We don't award certificates for the self-paced mode. The reason is you need to peer-review 3 capstone(s) after submitting your project.

You can only peer-review projects at the time the course is running; after the form is closed and the peer-review list is compiled.",69d122f12e
Can I still get the certificate if I skipped the first homework?,"Yes. Skipping the first homework does **not** prevent you from getting the certificate. You only need to **pass the Capstone project**. Homework is optional, though it helps with learning and leaderboard points.","Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
"Is homework required for the course certificate, or is the Capstone enough?",Homework is **not required** for the course certificate. You only need to **pass the Capstone project** to get the certificate.,"Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
Do I need to finish every homework assignment to earn a certificate?,I don't know.,"Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
What actually matters for getting the certificate in this course?,"What actually matters for the certificate is **passing the Capstone project** and finishing the course with a **live cohort**.

A few key points:
- **Homework is not mandatory** for the certificate, though it helps and affects your leaderboard rank.
- You **cannot get a certificate in self-paced mode**.
- To get the certificate, you must also be able to **peer-review 3 capstones**, which is only possible while the course is running.

So the main requirement is: **complete the Capstone during a live cohort**.","Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
"If homework is optional, what does it affect besides the certificate?",It affects your **leaderboard rank** — the points from homework count toward it.,"Yes, you need to pass the Capstone project to get the certificate. Homework is not mandatory, though it is recommended for reinforcing concepts, and the points awarded count towards your rank on the leaderboard.",9f689c185f
Why does the homework content keep changing before it’s released?,"If the homework title contains `[DRAFT]`, it means the homework is not ready yet. Until the homework is fully ready, the content can still change.

The homework is considered ready only when:

- the homework form is open on the course management platform, and
- the homework title does not contain `[DRAFT]`.

So if you see changes before release, it’s because it’s still in draft status.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
What does [DRAFT] mean in a homework title?,"If a homework title contains `[DRAFT]`, it means the homework is not ready yet.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
How can I tell if a homework is actually ready to do?,"A homework is actually ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does **not** contain `[DRAFT]`.

If either of these is not true, it’s still not ready, and the content may keep changing.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
Is it safe to start a homework early if it still says [DRAFT]?,"No. If the homework title still contains `[DRAFT]`, it is not ready yet, and the content can still change. Working on it early is at your own risk because the final version may be different.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
When can I trust that the homework version won’t change anymore?,"You can trust the homework version won’t change anymore only when **both** are true:

- the homework form is **open** on the course management platform, and
- the homework title **does not contain** `[DRAFT]`.

Until then, the content can still change.","If the homework title contains `[DRAFT]`, it means the homework is not ready yet.

The homework is ready only when both are true:

- The homework form is open on the course management platform.
- The homework title does not contain `[DRAFT]`.

Until then, the content can still change. Working on the material or homework in advance is at your own risk, because the final version can be different.",96286b4be4
When is the next run of the course scheduled for?,Summer 2027.,Summer 2027.,bd31146b0e
Do you know when the course will be offered again?,Summer 2027.,Summer 2027.,bd31146b0e
What’s the next available session for this course?,Summer 2027.,Summer 2027.,bd31146b0e
Can you tell me the next time this course is starting?,The next time the course is offered is **Summer 2027**.,Summer 2027.,bd31146b0e
When should I expect the course to be available again?,The course will be offered next in **Summer 2027**.,Summer 2027.,bd31146b0e
Where do I find the lecture videos for this course?,"Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo—each lesson page has the relevant lecture videos linked at the top.","Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
Is there a main place I should open first to get to the lessons and videos?,"Yes — the main place to open first is the **LLM Zoomcamp GitHub repository**: https://github.com/DataTalksClub/llm-zoomcamp

Open the **lesson folders** there. Each lesson page has the relevant **videos linked at the top**.","Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
"Do the lesson pages include the videos somewhere, or do I need to search YouTube separately?","The lesson pages include the relevant videos at the top of each lesson folder in the GitHub repo, so you do **not** need to search YouTube separately. The repo is the main entry point and is kept more up to date than the YouTube playlist.","Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
What’s the best way to keep up with the latest class materials and recordings?,I don't know.,"Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
Should I use the GitHub repo or the YouTube playlist when I’m looking for the course lectures?,"Use the GitHub repo as the main entry point for lectures. Open the lesson folders there, and each lesson page has the relevant videos linked at the top.

If you’re unsure, follow the GitHub repo first, since it’s easier to keep updated than the YouTube playlist.","Use the [LLM Zoomcamp GitHub repository](https://github.com/DataTalksClub/llm-zoomcamp) as the main entry point.

Open the lesson folders in the repo. Each lesson page has the relevant videos linked at the top.

<{IMAGE:lessons}>

When in doubt, follow the GitHub repo first, because it is easier to keep updated than the YouTube playlist.",31456f4b5f
Where do I go to see the LLM Zoomcamp syllabus and keep up with deadlines and homework?,"Go to the **LLM Zoomcamp course management platform**:

https://courses.datatalks.club/llm-zoomcamp-2026/

That’s where you can see the syllabus, deadlines, homework, and track your progress.","Use the [LLM Zoomcamp course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

It contains the current cohort structure, homework, deadlines, and progress tracking. The process is the same as in other DataTalks.Club Zoomcamps.",20c5a1347e
Is there a central place for tracking my progress in the LLM Zoomcamp course?,"Yes — the central place to track your syllabus, deadlines, homework, and progress is the **LLM Zoomcamp course management platform**:

https://courses.datatalks.club/llm-zoomcamp-2026/

","Use the [LLM Zoomcamp course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

It contains the current cohort structure, homework, deadlines, and progress tracking. The process is the same as in other DataTalks.Club Zoomcamps.",20c5a1347e
What platform should I use to follow the current cohort structure and homework for LLM Zoomcamp?,"Use the **LLM Zoomcamp course management platform**:  
https://courses.datatalks.club/llm-zoomcamp-2026/

It has the current cohort structure, homework, deadlines, and progress tracking.","Use the [LLM Zoomcamp course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

It contains the current cohort structure, homework, deadlines, and progress tracking. The process is the same as in other DataTalks.Club Zoomcamps.",20c5a1347e
How do I find the official LLM Zoomcamp course page with deadlines and progress tracking?,"Use the official **LLM Zoomcamp course management platform** here:

[https://courses.datatalks.club/llm-zoomcamp-2026/](https://courses.datatalks.club/llm-zoomcamp-2026/)

It includes the syllabus, deadlines, homework, and progress tracking.","Use the [LLM Zoomcamp course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

It contains the current cohort structure, homework, deadlines, and progress tracking. The process is the same as in other DataTalks.Club Zoomcamps.",20c5a1347e
"Does LLM Zoomcamp use a course management platform for syllabus, assignments, and deadlines?","Yes. LLM Zoomcamp uses a course management platform for the syllabus, homework, deadlines, and progress tracking: https://courses.datatalks.club/llm-zoomcamp-2026/","Use the [LLM Zoomcamp course management platform](https://courses.datatalks.club/llm-zoomcamp-2026/).

It contains the current cohort structure, homework, deadlines, and progress tracking. The process is the same as in other DataTalks.Club Zoomcamps.",20c5a1347e
"Do you have live sessions or office hours for every module in the course, or is everything just pre-recorded?","There are **no separate live sessions for every module by default**. The module materials are **pre-recorded** and available in the course repo.

**Live sessions/office hours are announced separately when they happen.** If you get stuck, you can ask in **Slack** and follow the questions guidelines.

There is also **optional extra support** via **AI Shipping Labs**, which is a **paid** community with regular Zoom office hours and extra structure.","There are no separate live sessions for every module by default. Module materials are pre-recorded and available in the course repo.

Live sessions are announced separately when they happen. If you are stuck, ask your question in Slack and follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).

Optional extra support is available through [AI Shipping Labs](https://datatalks.club/docs/courses/llm-zoomcamp/ai-shipping-labs/), a paid community that includes regular Zoom office hours and additional structure. This is optional; the DataTalks.Club course content remains free.",d65e05bd7a
Where do I go if I get stuck on a lesson and need help with a question?,"If you get stuck, ask your question in the course’s support/community channels. The context doesn’t specify a single exact place.","There are no separate live sessions for every module by default. Module materials are pre-recorded and available in the course repo.

Live sessions are announced separately when they happen. If you are stuck, ask your question in Slack and follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).

Optional extra support is available through [AI Shipping Labs](https://datatalks.club/docs/courses/llm-zoomcamp/ai-shipping-labs/), a paid community that includes regular Zoom office hours and additional structure. This is optional; the DataTalks.Club course content remains free.",d65e05bd7a
"How are live sessions announced for this course, since I don't see them listed for each module?","Live sessions are announced separately when they happen, not for every module by default.

You can find the announcement in the course’s **Telegram and Slack announcement channels** before the session starts. The **video URL** is also posted there, and you can watch live on the **DataTalksClub YouTube channel**. Students join via **YouTube Live** and submit questions through **Slido** during the session.

If you’re stuck between sessions, ask in **Slack** and follow the asking-questions guidelines.","There are no separate live sessions for every module by default. Module materials are pre-recorded and available in the course repo.

Live sessions are announced separately when they happen. If you are stuck, ask your question in Slack and follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).

Optional extra support is available through [AI Shipping Labs](https://datatalks.club/docs/courses/llm-zoomcamp/ai-shipping-labs/), a paid community that includes regular Zoom office hours and additional structure. This is optional; the DataTalks.Club course content remains free.",d65e05bd7a
"Is there any extra support with regular Zoom office hours, or is that only part of a paid option?","The regular Zoom office hours are part of the optional paid support option, AI Shipping Labs. The main course content is free, but extra support with regular Zoom office hours is not included by default.","There are no separate live sessions for every module by default. Module materials are pre-recorded and available in the course repo.

Live sessions are announced separately when they happen. If you are stuck, ask your question in Slack and follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).

Optional extra support is available through [AI Shipping Labs](https://datatalks.club/docs/courses/llm-zoomcamp/ai-shipping-labs/), a paid community that includes regular Zoom office hours and additional structure. This is optional; the DataTalks.Club course content remains free.",d65e05bd7a
"Are the main course materials free, and what exactly is included in the optional paid support?","Yes — the main DataTalks.Club course content remains free.

The optional paid support is **AI Shipping Labs**, and it includes:
- **regular Zoom office hours**
- **additional structure** for support/community

It is optional and separate from the free course materials.","There are no separate live sessions for every module by default. Module materials are pre-recorded and available in the course repo.

Live sessions are announced separately when they happen. If you are stuck, ask your question in Slack and follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).

Optional extra support is available through [AI Shipping Labs](https://datatalks.club/docs/courses/llm-zoomcamp/ai-shipping-labs/), a paid community that includes regular Zoom office hours and additional structure. This is optional; the DataTalks.Club course content remains free.",d65e05bd7a
Can I count my Bluesky posts toward the learning in public credits?,Yes. Bluesky posts can be used toward the learning in public credits.,Yes. Bluesky posts can be used for learning in public credits.,dbf5369006
Does posting on Bluesky qualify for learning in public credits?,Yes. Bluesky posts can be used for learning in public credits.,Yes. Bluesky posts can be used for learning in public credits.,dbf5369006
Are Bluesky updates accepted as learning in public credits for the course?,Yes. Bluesky posts can be used for learning in public credits.,Yes. Bluesky posts can be used for learning in public credits.,dbf5369006
"If I share my learning on Bluesky, can I get public learning credit for it?",Yes. Bluesky posts can be used for learning in public credits.,Yes. Bluesky posts can be used for learning in public credits.,dbf5369006
Is Bluesky one of the platforms I can use for learning in public credits?,Yes. Bluesky posts can be used for learning in public credits.,Yes. Bluesky posts can be used for learning in public credits.,dbf5369006
What is the Telegram channel for LLM Zoomcamp announcements?,https://t.me/llm_zoomcamp,"The Telegram channel is [https://t.me/llm_zoomcamp](https://t.me/llm_zoomcamp).

Use it for announcements. For technical discussion and questions, use the course Slack channel.",054f3fd58f
Where can I find the LLM Zoomcamp Telegram link?,The LLM Zoomcamp Telegram link is: https://t.me/llm_zoomcamp,"The Telegram channel is [https://t.me/llm_zoomcamp](https://t.me/llm_zoomcamp).

Use it for announcements. For technical discussion and questions, use the course Slack channel.",054f3fd58f
Is there a Telegram group for the LLM Zoomcamp course?,"Yes — the Telegram channel for LLM Zoomcamp is **https://t.me/llm_zoomcamp**.

It’s mainly for announcements; for technical discussion and questions, use the course Slack channel.","The Telegram channel is [https://t.me/llm_zoomcamp](https://t.me/llm_zoomcamp).

Use it for announcements. For technical discussion and questions, use the course Slack channel.",054f3fd58f
What channel should I use for course announcements in LLM Zoomcamp?,"Use the **LLM Zoomcamp Telegram channel** for course announcements: https://t.me/llm_zoomcamp

For technical discussion and questions, use the course **Slack** channel.","The Telegram channel is [https://t.me/llm_zoomcamp](https://t.me/llm_zoomcamp).

Use it for announcements. For technical discussion and questions, use the course Slack channel.",054f3fd58f
Should I ask technical questions in the Telegram channel or somewhere else?,Use the **course Slack channel** for technical discussion and questions. The Telegram channel is for **announcements** only.,"The Telegram channel is [https://t.me/llm_zoomcamp](https://t.me/llm_zoomcamp).

Use it for announcements. For technical discussion and questions, use the course Slack channel.",054f3fd58f
Why does the FAQ dataset count not match what I see in the course videos?,"We load the data from the live FAQ dataset, which changes over time: new questions are added and old ones are deleted. The videos were recorded in May 2026, so by the time you run the code the count is different from what you see in the videos.","We load the data from the live FAQ dataset, which changes over time: new questions are added and old ones are deleted. The videos were recorded in May 2026, so by the time you run the code the count is different from what you see in the videos.",e2d595f23c
Is it normal that I get a different number of FAQ records when I run the code now?,"Yes, that’s normal.

The FAQ dataset is loaded from a live source, and it changes over time: new questions can be added and old ones removed. The videos were recorded in May 2026, so when you run the code now, the number of FAQ records may differ from what you see in the videos.","We load the data from the live FAQ dataset, which changes over time: new questions are added and old ones are deleted. The videos were recorded in May 2026, so by the time you run the code the count is different from what you see in the videos.",e2d595f23c
"Did the FAQ data change after the videos were recorded, and could that explain the mismatch?","Yes. The FAQ dataset is live and changes over time: new questions are added and old ones are deleted. The videos were recorded in May 2026, so the dataset count can differ later, which would explain the mismatch.","We load the data from the live FAQ dataset, which changes over time: new questions are added and old ones are deleted. The videos were recorded in May 2026, so by the time you run the code the count is different from what you see in the videos.",e2d595f23c
Why are there more or fewer FAQ entries in the live dataset than in the screenshots or demo?,The FAQ dataset is live and changes over time: new entries may be added and old ones may be deleted. So the number of FAQ records in the live dataset can differ from what you see in screenshots or the demo.,"We load the data from the live FAQ dataset, which changes over time: new questions are added and old ones are deleted. The videos were recorded in May 2026, so by the time you run the code the count is different from what you see in the videos.",e2d595f23c
How come the record count in the FAQ example is different from the one shown in the May 2026 videos?,"The FAQ dataset is live and changes over time: new questions are added and old ones are removed. The videos were recorded in May 2026, so the record count you see now can differ from the count shown in the videos.","We load the data from the live FAQ dataset, which changes over time: new questions are added and old ones are deleted. The videos were recorded in May 2026, so by the time you run the code the count is different from what you see in the videos.",e2d595f23c
Can I still turn in the homework if the deadline already passed but the form hasn’t been closed yet?,"Yes. As long as the submission form is still open, you can still turn in the homework even if the deadline has already passed.","Yes. As long as the submission form is still open, you can submit your answers, even if the listed deadline has already passed. You can no longer submit only after the form has been closed — so while it's still open, go ahead and submit.",a9353fadfe
The submission page is still open after the due date — does that mean I’m allowed to send my answers in?,"Yes. As long as the submission form is still open, you can still submit your answers even if the due date has passed.","Yes. As long as the submission form is still open, you can submit your answers, even if the listed deadline has already passed. You can no longer submit only after the form has been closed — so while it's still open, go ahead and submit.",a9353fadfe
"If the homework deadline is over, can I still submit as long as the form is open?","Yes. As long as the submission form is still open, you can submit even if the deadline has already passed. Once the form is closed, submissions are no longer accepted.","Yes. As long as the submission form is still open, you can submit your answers, even if the listed deadline has already passed. You can no longer submit only after the form has been closed — so while it's still open, go ahead and submit.",a9353fadfe
Do late homework submissions still work while the course form is open?,"Yes. As long as the homework submission form is still open, you can submit even if the deadline has already passed. Late submissions are not accepted only after the form has been closed.","Yes. As long as the submission form is still open, you can submit your answers, even if the listed deadline has already passed. You can no longer submit only after the form has been closed — so while it's still open, go ahead and submit.",a9353fadfe
"When does homework submission actually stop — at the deadline, or only after the form is closed?","Homework submission stops only **after the form is closed**, not exactly at the deadline.

If the form is still open, you can still submit even if the deadline has passed. Once it’s closed, no more submissions are allowed.","Yes. As long as the submission form is still open, you can submit your answers, even if the listed deadline has already passed. You can no longer submit only after the form has been closed — so while it's still open, go ahead and submit.",a9353fadfe
"Can I still turn in homework after the deadline, or do you close the form right away?","You can still submit **while the form is open**, even if the deadline has passed. We **do not** give individual deadline extensions, and once the form is **closed**, late submissions are no longer possible.","No. We don't give individual deadline extensions, and once the homework submission form is closed you can no longer submit it — there are no late submissions. While the form is still open you can submit, even if the listed deadline has already passed.

Missing a homework won't affect your certificate: homework isn't mandatory, only passing the Capstone project is. Homework points only count toward your leaderboard rank, so you'll still appear on the leaderboard with your other submissions.",cdc3b285e5
Do you ever give deadline extensions for homework if someone is late?,"No. We don’t give individual deadline extensions, and there are no late submissions once the homework submission form is closed. While the form is still open, you can still submit even if the deadline has passed.","No. We don't give individual deadline extensions, and once the homework submission form is closed you can no longer submit it — there are no late submissions. While the form is still open you can submit, even if the listed deadline has already passed.

Missing a homework won't affect your certificate: homework isn't mandatory, only passing the Capstone project is. Homework points only count toward your leaderboard rank, so you'll still appear on the leaderboard with your other submissions.",cdc3b285e5
"If I miss a homework, will it hurt my chance to get the certificate?","No. Missing a homework will not hurt your chance to get the certificate. You only need to pass the Capstone project to get the certificate, though homework is recommended and its points count toward your leaderboard rank.","No. We don't give individual deadline extensions, and once the homework submission form is closed you can no longer submit it — there are no late submissions. While the form is still open you can submit, even if the listed deadline has already passed.

Missing a homework won't affect your certificate: homework isn't mandatory, only passing the Capstone project is. Homework points only count toward your leaderboard rank, so you'll still appear on the leaderboard with your other submissions.",cdc3b285e5
"Are homework assignments required for finishing the course, or is something else enough?","Yes — something else is enough.

For the capstone project, you are **not required** to use homework-style orchestrations. A **plain Python script** that ingests and indexes your data is enough for full points on the ingestion-pipeline criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

If you meant the course overall, homework is part of the course structure, but for the project you have flexibility in the tooling.","No. We don't give individual deadline extensions, and once the homework submission form is closed you can no longer submit it — there are no late submissions. While the form is still open you can submit, even if the listed deadline has already passed.

Missing a homework won't affect your certificate: homework isn't mandatory, only passing the Capstone project is. Homework points only count toward your leaderboard rank, so you'll still appear on the leaderboard with your other submissions.",cdc3b285e5
What happens to leaderboard points if I skip a homework submission?,"If you skip a homework submission, you just miss out on those homework points. It won’t affect your certificate, but it can lower your leaderboard rank.","No. We don't give individual deadline extensions, and once the homework submission form is closed you can no longer submit it — there are no late submissions. While the form is still open you can submit, even if the listed deadline has already passed.

Missing a homework won't affect your certificate: homework isn't mandatory, only passing the Capstone project is. Homework points only count toward your leaderboard rank, so you'll still appear on the leaderboard with your other submissions.",cdc3b285e5
"Is the name I enter for the certificate ever shown publicly, or is it kept private?",No. The name you enter for the certificate is kept private and only appears on your certificate. It is not shown publicly or shared with third parties. The public leaderboard uses your separate display name instead.,"No. The certificate name only appears on your certificate — it isn't published online or shared with third parties. The public leaderboard uses your separate display name instead, which is a random nickname (like “Lucid Elbakyan”) and anonymous by default.

You can set both in “Edit Course Profile”: the first field is your public nickname for the leaderboard, and the second is the official name printed on your certificate. This lets you keep your real name off the public leaderboard while still having it on the certificate.",651ba06b34
"If I put my real name in the certificate field, will it be visible on the leaderboard or shared anywhere else?",No. The name in the certificate field only appears on your certificate and is not shown publicly on the leaderboard or shared with third parties. The leaderboard uses your separate display name instead.,"No. The certificate name only appears on your certificate — it isn't published online or shared with third parties. The public leaderboard uses your separate display name instead, which is a random nickname (like “Lucid Elbakyan”) and anonymous by default.

You can set both in “Edit Course Profile”: the first field is your public nickname for the leaderboard, and the second is the official name printed on your certificate. This lets you keep your real name off the public leaderboard while still having it on the certificate.",651ba06b34
What’s the difference between the display name on the leaderboard and the name printed on the certificate?,"The difference is:

- **Leaderboard display name:** a public nickname shown on the leaderboard, usually random by default, and you can change it.
- **Certificate name:** the official name printed only on your certificate, and it is not shown publicly.

You can set both in **Edit Course Profile**.","No. The certificate name only appears on your certificate — it isn't published online or shared with third parties. The public leaderboard uses your separate display name instead, which is a random nickname (like “Lucid Elbakyan”) and anonymous by default.

You can set both in “Edit Course Profile”: the first field is your public nickname for the leaderboard, and the second is the official name printed on your certificate. This lets you keep your real name off the public leaderboard while still having it on the certificate.",651ba06b34
Where do I change the nickname that shows publicly versus the certificate name in my course profile?,"In **Edit Course Profile**:

- **First field** = your public **nickname/display name** shown on the leaderboard
- **Second field** = your official **certificate name** printed on the certificate

","No. The certificate name only appears on your certificate — it isn't published online or shared with third parties. The public leaderboard uses your separate display name instead, which is a random nickname (like “Lucid Elbakyan”) and anonymous by default.

You can set both in “Edit Course Profile”: the first field is your public nickname for the leaderboard, and the second is the official name printed on your certificate. This lets you keep your real name off the public leaderboard while still having it on the certificate.",651ba06b34
Can I use a random public nickname and still have my real name on the certificate only?,"Yes. You can use a random public nickname for the leaderboard and keep your real name only on the certificate.

- **Public nickname/display name:** shown on the leaderboard
- **Official name:** printed on your certificate only

You can set both in **Edit Course Profile**.","No. The certificate name only appears on your certificate — it isn't published online or shared with third parties. The public leaderboard uses your separate display name instead, which is a random nickname (like “Lucid Elbakyan”) and anonymous by default.

You can set both in “Edit Course Profile”: the first field is your public nickname for the leaderboard, and the second is the official name printed on your certificate. This lets you keep your real name off the public leaderboard while still having it on the certificate.",651ba06b34
Why don’t we use LangChain in this course from the start?,"We don’t use LangChain from the start because the course wants you to first build the core pieces yourself: prompting, retrieval, indexing, and evaluation.

The idea is to learn the fundamentals before relying on a framework, like learning HTML, CSS, and JavaScript before using React or Angular. Once you understand what the framework automates, it’s much easier to use it well.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
What’s the reason for building RAG components by hand instead of using a framework?,"Because the course wants you to understand the core RAG pieces first: prompting, retrieval, indexing, and evaluation.

Building them by hand is like learning HTML/CSS/JavaScript before using React or Angular. Once you understand what the framework is doing, it’s much easier to use it well.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
Is LangChain skipped because the course wants us to understand retrieval and prompting first?,"Yes — that’s correct.

The course skips LangChain at first so you can understand the core RAG pieces yourself: prompting, retrieval, indexing, and evaluation. The idea is to learn the fundamentals before using a framework that automates them.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
Wouldn’t it be easier to just use LangChain for indexing and evaluation?,"It would be easier, but the course intentionally has you build the core pieces yourself first: prompting, retrieval, indexing, and evaluation. The idea is to understand what LangChain is automating before using a framework like React or Angular after learning HTML, CSS, and JavaScript.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
Why is it important to learn the basic pieces before switching to LangChain or another framework?,"Because the course wants you to understand the core mechanics first: prompting, retrieval, indexing, and evaluation. Once you know those basics, frameworks like LangChain become much easier to use well, since they mainly automate those pieces.

A good analogy is learning HTML, CSS, and JavaScript before React or Angular.","LangChain is a framework for building LLM-powered apps. In this course, we first build the core pieces ourselves: prompting, retrieval, indexing, and evaluation.

Think of it like learning HTML, CSS, and JavaScript before using React or Angular. Frameworks are easier to use well once you understand what they automate.",b7cdde6b25
Why do I get an OpenAI API error about insufficient credits when I run responses.create?,"You’re getting that error because your OpenAI API account doesn’t have enough quota/credits.

The course context says:

- `responses.create` can fail with **""OpenAI API Error: Insufficient credits""**
- This happens when your OpenAI account has **insufficient quota**
- There is **no free usage** of the OpenAI API, so you need to **add funds / enable pay-as-you-go**

To fix it:

1. Add credits to your OpenAI account in billing settings.
2. Make sure you’re using a model available to your account.
3. If needed, recreate your API key after adding credits.

If you want, I can also help you distinguish this from the OpenRouter 402 error.","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
What does it mean if responses.create fails with an Insufficient credits message?,"It means your OpenAI account doesn’t have enough credits to run the request. The API can return:

```text
OpenAI API Error: Insufficient credits
```

In practice, this usually means you need to add credits or use an account/key with available quota.","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
How can I fix the OpenAI API error I see on responses.create because of not enough credits?,"The error means your OpenAI API account has **insufficient credits/quota**.

To fix it:

1. **Add credits** to your OpenAI account  
   - Go to the billing overview and add at least **$5**:  
   https://platform.openai.com/settings/organization/billing/overview

2. **Use a model available to your account**  
   - In `responses.create(model='gpt-4o', ...)`, make sure the model is one you can access.

3. **Recreate your API key if needed**  
   - After adding credits, you may need to create a new API key and update it locally.

If you still get a 429/insufficient quota error, it’s still a billing/quota issue rather than a code issue.","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
Is the responses.create command failing due to low OpenAI account credits?,"Yes, **it can be**. The context says `responses.create` may fail with an **""Insufficient credits""** error when your OpenAI account does not have enough credits.

If you mean the OpenAI API directly, the related error is typically:

- **429 / insufficient_quota** for quota or billing issues

If you mean **OpenRouter** via `responses.create`, a **402** error can happen for a different reason, often because `max_output_tokens` is too high, not just because of low credits.

So the short answer is: **yes, low credits can cause `responses.create` to fail, but the exact error depends on whether you are using OpenAI directly or OpenRouter.**","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
"Why am I seeing ""OpenAI API Error: Insufficient credits"" when using responses.create?","You’re seeing that error because your OpenAI API account has run out of quota/credits. There is no free usage for the API.

To fix it:

1. Add credits to your OpenAI account, at least $5.
2. Make sure you’re using a model available to your account in `responses.create(...)`.
3. If needed, recreate your API key after adding credits and update it locally.

If you still get the error after that, double-check your billing and model selection.","You may receive the following error when running the OpenAI `responses.create` command due to insufficient credits in your OpenAI account:

```
OpenAI API Error: Insufficient credits
```",f5df151c59
Why am I getting OpenAI 429 RateLimitError even though my code looks fine?,"You’re likely seeing a `429 RateLimitError` because it’s actually a **billing/quota issue**, not a problem with your code.

If the error says something like:

- `You exceeded your current quota`
- `type: 'insufficient_quota'`

then your OpenAI account has **run out of credits** or doesn’t have billing set up. In the course context, there is **no free API usage**, so you need to:

1. Add credits to your account
2. Make sure billing is enabled
3. Possibly create a new API key after adding credits
4. Use a model available to your account, such as one shown in your dashboard

If you’re also hitting real rate limits during loops or batch processing, reduce the number of API calls, lower concurrency, add retries with backoff, and cache results. But if the error is `insufficient_quota`, the fix is billing, not code changes.","```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
"Does OpenAI API have any free quota, or do I need to add billing first?",There is no free usage of the OpenAI API. You need to add billing/funds first to use it.,"```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
What should I do to fix the insufficient_quota error in my RAG notebook?,"To fix the `insufficient_quota` error in your RAG notebook:

1. **Add credits to your OpenAI account**  
   Go to the billing overview and add at least **$5**:  
   https://platform.openai.com/settings/organization/billing/overview

2. **Use a model available to your account**  
   In `responses.create(model='gpt-4o', …)`, make sure you select a model you actually have access to.

3. **Recreate your API key if needed**  
   After adding credits, you may need to generate a new API key and update it in your notebook/local environment.

Then rerun the command.","```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
Do I need to change the model name in responses.create if my account has limited access?,"Yes—if you’re using `responses.create` and have limited access, you may need to point it to a model/endpoint your key can actually use.

From the context:
- For OpenRouter, a low-limit or free key can fail unless you set a reasonable `max_output_tokens` and use a pinned model.
- The example recommends using a specific model like `openai/gpt-oss-120b:free` instead of a generic `openrouter/free`.

So if your account has limited access, check:
1. your `OPENAI_MODEL`
2. your `OPENAI_BASE_URL`
3. your available credits/limits

If you’re still getting errors, switch to a model your account is allowed to access.

","```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
"After adding funds to my OpenAI account, do I have to create a new API key?",You **might** need to recreate your API key after adding credits. The context says: “You might need to recreate an API key after adding credits to your account and update it locally.”,"```json
RateLimitError: Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: [https://platform.openai.com/docs/guides/error-codes/api-errors.](https://platform.openai.com/docs/guides/error-codes/api-errors.)', 'type': 'insufficient_quota', 'param': None, 'code': 'insufficient_quota'}
```

The above errors are related to your OpenAI API account’s quota. There is no free usage of OpenAI’s API, so you will need to add funds using a credit card (see pay-as-you-go in the OpenAI settings at [platform.openai.com](http://platform.openai.com)). Once added, re-run your Python command and you should receive a successful return code.

Steps to resolve:

1. Add credits to your account [here](https://platform.openai.com/settings/organization/billing/overview) (min $5).
2. In `responses.create(model='gpt-4o', …)` specify one of the models available to you:

   <{IMAGE:image_1}>

3. You might need to recreate an API key after adding credits to your account and update it locally.",152af39a53
How much does it usually cost to use the OpenAI API for this course?,"Using the OpenAI API for this course should cost very little — you can recharge starting from $5, and initial usage is usually fractions of one cent.","Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
Do I need to spend a lot of money to use the OpenAI API in the RAG module?,"No. Using the OpenAI API for the RAG module should cost very little, and you don’t need to spend a lot. The course notes say you can recharge starting from $5, but initial usage is usually just fractions of a cent.","Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
What is the minimum amount I can add to my OpenAI account for the course?,The minimum amount is **$5**.,"Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
"Will I be charged a lot when I start using the OpenAI API, or is it usually very cheap?","Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.","Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
How much should I expect to pay for initial OpenAI API usage in this course?,"Using the OpenAI API for this course should cost very little. You can start by recharging with as little as $5, and initial usage is usually fractions of a cent.","Using the OpenAI API for the course should cost very little. You can recharge starting from $5, but initial usage is usually fractions of one cent.",554d0eb78b
"Do I need to pay for the OpenAI API to finish the course homework, or are there free options?","No, you don’t need to pay for the OpenAI API to finish the course homework. The course provides free or low-cost alternatives, and you can use other LLM providers or even local models instead of OpenAI.

If you specifically choose OpenAI’s API, there is no free usage there—you’d need to add credits.","No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
Can I complete the llm-zoomcamp assignments without subscribing to OpenAI?,Yes — you do **not** need to subscribe and pay for OpenAI to complete the course homeworks. You can use the free or low-cost alternatives listed in the course GitHub repo.,"No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
Where can I find cheaper or free alternatives to the OpenAI API for this course?,"You can find cheaper or free alternatives in the course GitHub repo, specifically the list of **OpenAI API alternatives**:

- https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives

The course notes say you **do not have to pay for OpenAI** to complete the homework, and you can also use other providers like **Groq, OpenRouter, DeepSeek, Gemini, Z.ai, Mistral**, or run models locally with **Ollama, vLLM, LM Studio**, etc.

","No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
"Is using OpenAI API required for the Module 1 RAG homework, or can I use another service?","No. OpenAI API is **not required** for the Module 1 RAG homework. You can use another provider, such as **Gemini, Groq, OpenRouter, Azure OpenAI, local models, or any other OpenAI-compatible service**.

The homework is designed so you **don’t need a paid service**. You may just need to adjust the code a bit for your provider, since response formats and tokenizers can differ.","No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
Does the course GitHub repo have a list of OpenAI API alternatives I can use instead?,"Yes. The course repo points to a curated list of **OpenAI API alternatives** in **Awesome LLMs**:

https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives

It also says you can use other providers like **Groq, OpenRouter, DeepSeek, Gemini, Z.ai, Mistral**, or even run models locally with **Ollama**, **vLLM**, or **LM Studio**.","No, you don't have to pay for this service in order to complete the course homeworks. You can use free or low-cost alternatives listed in the course GitHub repo.

See the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives).",85384a18e5
Why does the course recommend uv over pip and virtualenv for the setup?,"The course recommends `uv` because it’s fast and convenient. It replaces several traditional tools at once—like `pip`, `virtualenv`, `pip-tools`, `pipx`, `poetry`, `pyenv`, and `twine`—so you can manage Python versions, virtual environments, and dependencies with one tool and reproducible lockfiles.

You don’t have to use it; `pip` and `virtualenv` still work. The main reason is that `uv` makes the setup simpler and faster.","The course uses uv because it's fast and convenient. It's an all-in-one tool that replaces pip, virtualenv, pip-tools, pipx, poetry, pyenv, and twine — instead of juggling several tools, you manage Python versions, virtual environments, and dependencies through one fast command with reproducible lockfiles.

You're not required to use it — pip and virtualenv still work fine — but uv makes the setup simpler and faster, which is why the lessons use it.

Official docs: https://docs.astral.sh/uv/",f81dea8f7e
"What makes uv better or easier than using separate Python tools like pip, virtualenv, and pyenv?","uv is better/easier because it’s an all-in-one, fast tool that replaces several separate Python tools — including `pip`, `virtualenv`, and `pyenv`.

That means you can manage:
- Python versions
- virtual environments
- dependencies
- lockfiles for reproducible installs

all with one command, instead of juggling multiple tools. It also makes setup simpler and faster.

You’re not required to use it, though: `pip` and `virtualenv` still work fine.","The course uses uv because it's fast and convenient. It's an all-in-one tool that replaces pip, virtualenv, pip-tools, pipx, poetry, pyenv, and twine — instead of juggling several tools, you manage Python versions, virtual environments, and dependencies through one fast command with reproducible lockfiles.

You're not required to use it — pip and virtualenv still work fine — but uv makes the setup simpler and faster, which is why the lessons use it.

Official docs: https://docs.astral.sh/uv/",f81dea8f7e
"Do I have to use uv in the course, or can I still set things up with pip and virtualenv?","No — you’re **not required** to use `uv`.

You can still set things up with **`pip` and `virtualenv`**. The course uses `uv` because it’s faster and simpler, but `pip`/`virtualenv` work fine too.","The course uses uv because it's fast and convenient. It's an all-in-one tool that replaces pip, virtualenv, pip-tools, pipx, poetry, pyenv, and twine — instead of juggling several tools, you manage Python versions, virtual environments, and dependencies through one fast command with reproducible lockfiles.

You're not required to use it — pip and virtualenv still work fine — but uv makes the setup simpler and faster, which is why the lessons use it.

Official docs: https://docs.astral.sh/uv/",f81dea8f7e
"How does uv help with managing Python versions, environments, and dependencies in one place?","uv helps by being an all-in-one tool: it replaces separate tools like `pip`, `virtualenv`, `pip-tools`, `pipx`, `poetry`, `pyenv`, and `twine`. So instead of juggling multiple commands, you can manage Python versions, virtual environments, and dependencies in one fast place, with reproducible lockfiles.","The course uses uv because it's fast and convenient. It's an all-in-one tool that replaces pip, virtualenv, pip-tools, pipx, poetry, pyenv, and twine — instead of juggling several tools, you manage Python versions, virtual environments, and dependencies through one fast command with reproducible lockfiles.

You're not required to use it — pip and virtualenv still work fine — but uv makes the setup simpler and faster, which is why the lessons use it.

Official docs: https://docs.astral.sh/uv/",f81dea8f7e
"Is uv just a package installer, or does it replace other tools like poetry, pip-tools, and twine too?","Yes — `uv` is more than just a package installer. In the course context, it replaces tools like:

- `pip`
- `virtualenv`
- `pip-tools`
- `pipx`
- `poetry`
- `pyenv`
- `twine`

So the idea is that you can manage Python versions, virtual environments, dependencies, and lockfiles with one fast tool instead of juggling several.","The course uses uv because it's fast and convenient. It's an all-in-one tool that replaces pip, virtualenv, pip-tools, pipx, poetry, pyenv, and twine — instead of juggling several tools, you manage Python versions, virtual environments, and dependencies through one fast command with reproducible lockfiles.

You're not required to use it — pip and virtualenv still work fine — but uv makes the setup simpler and faster, which is why the lessons use it.

Official docs: https://docs.astral.sh/uv/",f81dea8f7e
Why does my notebook keep saying OPENAI_API_KEY is missing even though I added it to .env?,"Make sure you loaded the `.env` file in your notebook before using the key:

```python
from dotenv import load_dotenv
load_dotenv()
```

Also verify that:

- `python-dotenv` is installed
- the variable name in `.env` is exactly `OPENAI_API_KEY`

If needed:

```bash
pip install python-dotenv
```","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
Do I need python-dotenv for the API key to show up in Jupyter?,"Yes — in the course setup, `python-dotenv` is used to load your `.env` file in Jupyter, so the `OPENAI_API_KEY` shows up.

Typical steps:

```bash
pip install python-dotenv
```

Then in the notebook:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also make sure your `.env` file contains the variable exactly as:

```env
OPENAI_API_KEY=...
```

If you want, I can also show the minimal Jupyter example.","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
What's the right way to load my .env file before creating the OpenAI client?,"Load the `.env` file **before** creating the OpenAI client:

```python
from dotenv import load_dotenv
import os
from openai import OpenAI

load_dotenv()

client = OpenAI(api_key=os.getenv(""OPENAI_API_KEY""))
```

Make sure you’ve installed `python-dotenv` and that your `.env` file contains `OPENAI_API_KEY` exactly.","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
I set the key in .env but Jupyter still can't find it — what am I missing?,"Make sure you’re doing two things:

1. Install and load `python-dotenv` in the notebook:
```bash
pip install python-dotenv
```

```python
from dotenv import load_dotenv
load_dotenv()
```

2. Check that the variable name in your `.env` file is exactly:
```bash
OPENAI_API_KEY=...
```

If it still doesn’t work, I don’t know.","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
"Is the env variable name supposed to be OPENAI_API_KEY exactly, or can it be something else?","Yes — in the course context, it should be named exactly `OPENAI_API_KEY` in your `.env` file.","Make sure you installed and used `python-dotenv`.

```bash
pip install python-dotenv
```

Then load the `.env` file in the notebook before creating the OpenAI client:

```python
from dotenv import load_dotenv

load_dotenv()
```

Also check that the variable name in `.env` is exactly `OPENAI_API_KEY`.",86d99bbf21
How do I keep my OpenAI or Groq API key in a .env file and read it in Python?,"Store your API keys in a `.env` file and load them with `python-dotenv`.

### 1) Add `.env` to `.gitignore`
```gitignore
.env
```

### 2) Create a `.env` file
```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
```

### 3) Install `python-dotenv`
```bash
pip install python-dotenv
```

### 4) Read the keys in Python
```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```

If you’re in Jupyter, make sure you call `load_dotenv()` before creating the client and that the variable name in `.env` matches exactly.","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
What’s the easiest way to load env vars like OPENAI_API_KEY with python-dotenv?,"The easiest way is:

```python
from dotenv import load_dotenv
load_dotenv()
```

Then read the value with:

```python
import os
openai_api_key = os.getenv(""OPENAI_API_KEY"")
```

Make sure you have `python-dotenv` installed:

```bash
pip install python-dotenv
```

And put your key in a `.env` file like:

```bash
OPENAI_API_KEY=sk-...
```

","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
Should I add .env to .gitignore before storing API keys there?,"Yes — add `.env` to `.gitignore` before putting API keys there:

```gitignore
.env
```

That way the keys won’t be committed to GitHub.","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
"How do I set up a .env file for OPENAI_API_KEY, GROQ_API_KEY, and GEMINI_API_KEY?","Create a `.env` file in your project root with the keys:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Then:

1. Add `.env` to `.gitignore`:
```gitignore
.env
```

2. Install `python-dotenv` if needed:
```bash
pip install python-dotenv
```

3. Load it in Python:
```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
gemini_api_key = os.getenv(""GEMINI_API_KEY"")
```

I don't know.","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
Do I need to install anything special to use load_dotenv and get keys from .env?,"Yes — install `python-dotenv`:

```bash
pip install python-dotenv
```

Then load your `.env` file in Python:

```python
from dotenv import load_dotenv
load_dotenv()
```

After that, you can read keys with `os.getenv(""OPENAI_API_KEY"")` or similar.","Store API keys in a `.env` file and load them with `python-dotenv`, as recommended in the course.

Add `.env` to `.gitignore` so keys are never committed:

```gitignore
.env
```

Create a `.env` file:

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=...
```

Install `python-dotenv` if needed:

```bash
pip install python-dotenv
```

Load the keys in Python:

```python
import os
from dotenv import load_dotenv

load_dotenv()

openai_api_key = os.getenv(""OPENAI_API_KEY"")
groq_api_key = os.getenv(""GROQ_API_KEY"")
```",233dabe430
"Can I solve the homework with a different LLM provider instead of the one suggested, or is it required to use the recommended model?","No — it’s **not required** to use the recommended model. You can use a different LLM provider, including OpenAI-compatible ones like Groq, OpenRouter, Gemini, Azure OpenAI, or even a local model via Ollama/vLLM.

You may need to adjust the code a bit because response formats, tool schemas, and tokenizers can differ.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
"Is it okay to use Gemini, Groq, OpenRouter, Azure OpenAI, or even a local model for the assignment?","Yes. The assignment is not tied to one provider.

You can use Gemini, Groq, OpenRouter, Azure OpenAI, OpenAI-compatible providers, or even a local model like Ollama or LM Studio. You may need to adjust the code a bit because different providers can have different response formats, tool schemas, tokenizers, or endpoint requirements.

If you want, I can also help you adapt the homework code for a specific provider.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
"Does the homework assume I need a paid API, or can I do it without paying for a service?","You do **not** need a paid API. The homework is designed so you can do it without paying for a service.

You can use free or alternative providers, including local models, but you may need to adapt the code because different providers have different response formats, tool schemas, and tokenizers.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
"If I switch to another provider, what parts of the code usually need changes, like output format, tool schema, or tokenizer handling?","Usually you need to change:

- **Output handling**: e.g. `response.output` for OpenAI Responses API vs `response.choices[0].message.content` for chat-completions.
- **Tool/function schema**: providers often expect different tool schema shapes, so you may need to adapt the tools definition.
- **Tokenizer / token counting**: don’t use `tiktoken` or `cl100k_base` generically for other providers; use the provider’s usage fields, or its native tokenizer if usage isn’t exposed.
- **Provider config**: often just `base_url`, API key, and model name if the provider is OpenAI-compatible.

If the provider isn’t OpenAI-compatible, you usually need more code adaptation than just swapping the model name.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
Where can I find a list of OpenAI API alternatives that work for this course?,"You can find the list in the course GitHub repo here:

[Awesome LLMs — OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md#openai-api-alternatives)

It includes free or low-cost alternatives that work for the course.","Yes. The recommended model is not mandatory. You can use OpenAI, Gemini, Groq, OpenRouter, Azure OpenAI, local models, or another provider.

The homework is designed so you do not need a paid service. You may need to adapt the code for your provider, because response formats, tool schemas, and tokenizers differ.

For provider ideas, see the course list of [OpenAI API alternatives](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md).",ee43413718
How can I plug Google Gemini into the Module 1 notebook using the OpenAI-style API endpoint?,"Use the OpenAI-compatible Gemini endpoint in the Module 1 notebook like this:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

def llm(instructions, user_prompt, model=""gemini-3.1-flash-lite""):
    messages = [
        {""role"": ""developer"", ""content"": instructions},
        {""role"": ""user"", ""content"": user_prompt}
    ]

    response = client.chat.completions.create(
        model=model,
        messages=messages
    )

    return response.choices[0].message.content
```

What you need:
1. Put your Gemini API key in `.env` as `GEMINI_API_KEY`.
2. Point the OpenAI client to Google’s base URL.
3. Use a Gemini model name in `model=`.

One important note: this uses the older chat-completions style, so some notebook code may need changes, especially where it reads responses or handles tools/function calls.","To get started you need three things:

1. A Gemini API key saved in your `.env` file, for example as `GEMINI_API_KEY`.
2. An OpenAI client pointed at Google’s OpenAI-compatible base URL.
3. Your selected Google Gemini model name in your request.

Example code (loads the API key from `.env`, creates the Gemini client, and defines the `llm` helper):

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

def llm(instructions, user_prompt, model=""gemini-3.1-flash-lite""):
    message_history = [
        {""role"": ""developer"", ""content"": instructions},
        {""role"": ""user"", ""content"": user_prompt}
    ]

    response = client.chat.completions.create(
        model=model,
        messages=message_history
    )

    return response.choices[0].message.content
```

This uses the older chat completions style via the OpenAI-compatible endpoint, whereas many course examples use the newer Responses format. That means you will need to change the notebook code in a few places, especially where it reads the model response and where it handles tools or function calls.",0ae5c221b9
What do I need in my .env file and client setup before calling a Gemini model from the course notebook?,"You need:

1. A `.env` file with your Gemini key, for example:
   ```env
   GEMINI_API_KEY=your_key_here
   ```

2. In the notebook, load it with `python-dotenv`:
   ```python
   from dotenv import load_dotenv
   load_dotenv()
   ```

3. Create an OpenAI client using Google’s OpenAI-compatible base URL:
   ```python
   from openai import OpenAI
   import os

   client = OpenAI(
       api_key=os.getenv(""GEMINI_API_KEY""),
       base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
   )
   ```

Then pass your Gemini model name in the request, for example:
```python
model=""gemini-3.1-flash-lite""
```

If you want, I can also show the full notebook snippet.","To get started you need three things:

1. A Gemini API key saved in your `.env` file, for example as `GEMINI_API_KEY`.
2. An OpenAI client pointed at Google’s OpenAI-compatible base URL.
3. Your selected Google Gemini model name in your request.

Example code (loads the API key from `.env`, creates the Gemini client, and defines the `llm` helper):

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

def llm(instructions, user_prompt, model=""gemini-3.1-flash-lite""):
    message_history = [
        {""role"": ""developer"", ""content"": instructions},
        {""role"": ""user"", ""content"": user_prompt}
    ]

    response = client.chat.completions.create(
        model=model,
        messages=message_history
    )

    return response.choices[0].message.content
```

This uses the older chat completions style via the OpenAI-compatible endpoint, whereas many course examples use the newer Responses format. That means you will need to change the notebook code in a few places, especially where it reads the model response and where it handles tools or function calls.",0ae5c221b9
Is there a simple llm helper example for using Gemini through OpenAI-compatible chat completions?,"Yes — the context includes a simple helper example for Gemini via the OpenAI-compatible chat completions endpoint:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

def llm(instructions, user_prompt, model=""gemini-3.1-flash-lite""):
    message_history = [
        {""role"": ""developer"", ""content"": instructions},
        {""role"": ""user"", ""content"": user_prompt}
    ]

    response = client.chat.completions.create(
        model=model,
        messages=message_history
    )

    return response.choices[0].message.content
```

This is the simple `llm` helper for Gemini using OpenAI-compatible chat completions.","To get started you need three things:

1. A Gemini API key saved in your `.env` file, for example as `GEMINI_API_KEY`.
2. An OpenAI client pointed at Google’s OpenAI-compatible base URL.
3. Your selected Google Gemini model name in your request.

Example code (loads the API key from `.env`, creates the Gemini client, and defines the `llm` helper):

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

def llm(instructions, user_prompt, model=""gemini-3.1-flash-lite""):
    message_history = [
        {""role"": ""developer"", ""content"": instructions},
        {""role"": ""user"", ""content"": user_prompt}
    ]

    response = client.chat.completions.create(
        model=model,
        messages=message_history
    )

    return response.choices[0].message.content
```

This uses the older chat completions style via the OpenAI-compatible endpoint, whereas many course examples use the newer Responses format. That means you will need to change the notebook code in a few places, especially where it reads the model response and where it handles tools or function calls.",0ae5c221b9
Why does the notebook code need changes when switching from the Responses API to Gemini chat completions?,"Because the Responses API and Gemini chat completions use different response formats.

The notebook code from the course often expects OpenAI’s newer Responses API, where you access the result with something like `response.output`. Gemini via the OpenAI-compatible endpoint does not implement that same interface, so you need to switch to chat completions and read:

```python
response.choices[0].message.content
```

Also, if the notebook uses tools or function calls, the tools schema may need to be adapted to the shape Gemini expects.

So the code changes are needed because:
- `response.output` is OpenAI Responses API specific
- Gemini works better through `chat.completions`
- the response structure and tool handling are different

","To get started you need three things:

1. A Gemini API key saved in your `.env` file, for example as `GEMINI_API_KEY`.
2. An OpenAI client pointed at Google’s OpenAI-compatible base URL.
3. Your selected Google Gemini model name in your request.

Example code (loads the API key from `.env`, creates the Gemini client, and defines the `llm` helper):

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

def llm(instructions, user_prompt, model=""gemini-3.1-flash-lite""):
    message_history = [
        {""role"": ""developer"", ""content"": instructions},
        {""role"": ""user"", ""content"": user_prompt}
    ]

    response = client.chat.completions.create(
        model=model,
        messages=message_history
    )

    return response.choices[0].message.content
```

This uses the older chat completions style via the OpenAI-compatible endpoint, whereas many course examples use the newer Responses format. That means you will need to change the notebook code in a few places, especially where it reads the model response and where it handles tools or function calls.",0ae5c221b9
Which parts of the Module 1 notebook usually break or need updates when I use Gemini instead of the default model?,"Usually the places that need changes are:

- **Where the notebook reads the model output**
- **Where it handles tools / function calls**

The context also says that with Gemini via the OpenAI-compatible endpoint, the notebook is using the **older chat completions style**, while many course examples use the **newer Responses format**, so those parts may need updates.","To get started you need three things:

1. A Gemini API key saved in your `.env` file, for example as `GEMINI_API_KEY`.
2. An OpenAI client pointed at Google’s OpenAI-compatible base URL.
3. Your selected Google Gemini model name in your request.

Example code (loads the API key from `.env`, creates the Gemini client, and defines the `llm` helper):

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

def llm(instructions, user_prompt, model=""gemini-3.1-flash-lite""):
    message_history = [
        {""role"": ""developer"", ""content"": instructions},
        {""role"": ""user"", ""content"": user_prompt}
    ]

    response = client.chat.completions.create(
        model=model,
        messages=message_history
    )

    return response.choices[0].message.content
```

This uses the older chat completions style via the OpenAI-compatible endpoint, whereas many course examples use the newer Responses format. That means you will need to change the notebook code in a few places, especially where it reads the model response and where it handles tools or function calls.",0ae5c221b9
How do I get a Pydantic object back from Gemini if the Responses API parse method isn’t there?,"Use the OpenAI SDK’s **chat completions parsing flow** instead of `responses.parse`.

Define a Pydantic model, then call:

```python
response = client.chat.completions.parse(
    model=""gemini-3.1-flash-lite"",
    messages=messages,
    response_format=Questions
)
```

Then read the parsed object from:

```python
result = response.choices[0].message.parsed
```

So the key idea is: **with Gemini via the OpenAI-compatible endpoint, use `chat.completions.parse` and `message.parsed`, not the Responses API**.","To get parsed structured output, use the OpenAI SDK's chat-completions parsing flow instead of the newer Responses API. This is the right choice when you want to stay on the OpenAI SDK but call a chat-completions-compatible model like Gemini through the OpenAI-compatible endpoint.

First, define the structure you want Gemini to return as a Pydantic model:

```python
from pydantic import BaseModel

class Question(BaseModel):
    question: str
    answer: str

class Questions(BaseModel):
    questions: list[Question]
```

Then call `chat.completions.parse` with `response_format=Questions`. Pass the same kind of `messages` you would to any chat completion:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

messages = [
    {""role"": ""developer"", ""content"": ""Generate three FAQ questions about RAG.""},
    {""role"": ""user"", ""content"": ""Topic: structured output with Gemini""}
]

response = client.chat.completions.parse(
    model=""gemini-3.1-flash-lite"",
    messages=messages,
    response_format=Questions
)

result = response.choices[0].message.parsed
print(result.questions)
```

Notes:
- This approach keeps using the OpenAI SDK while leveraging Gemini through Google's endpoint.
- The parsed output (e.g., `result.questions`) is a Pydantic object available directly, with no manual JSON parsing.
- `chat.completions.parse` requires `pydantic` (`uv add pydantic`).",341f71f28c
What’s the easiest way to make Gemini return structured data through the OpenAI-compatible endpoint?,"The easiest way is to use `chat.completions.parse` with a Pydantic model as `response_format`.

Example:

```python
from pydantic import BaseModel

class Question(BaseModel):
    question: str
    answer: str

class Questions(BaseModel):
    questions: list[Question]
```

Then call:

```python
response = client.chat.completions.parse(
    model=""gemini-3.1-flash-lite"",
    messages=messages,
    response_format=Questions
)

result = response.choices[0].message.parsed
```

This works with Gemini through the OpenAI-compatible endpoint and gives you structured Pydantic output without manual JSON parsing.","To get parsed structured output, use the OpenAI SDK's chat-completions parsing flow instead of the newer Responses API. This is the right choice when you want to stay on the OpenAI SDK but call a chat-completions-compatible model like Gemini through the OpenAI-compatible endpoint.

First, define the structure you want Gemini to return as a Pydantic model:

```python
from pydantic import BaseModel

class Question(BaseModel):
    question: str
    answer: str

class Questions(BaseModel):
    questions: list[Question]
```

Then call `chat.completions.parse` with `response_format=Questions`. Pass the same kind of `messages` you would to any chat completion:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

messages = [
    {""role"": ""developer"", ""content"": ""Generate three FAQ questions about RAG.""},
    {""role"": ""user"", ""content"": ""Topic: structured output with Gemini""}
]

response = client.chat.completions.parse(
    model=""gemini-3.1-flash-lite"",
    messages=messages,
    response_format=Questions
)

result = response.choices[0].message.parsed
print(result.questions)
```

Notes:
- This approach keeps using the OpenAI SDK while leveraging Gemini through Google's endpoint.
- The parsed output (e.g., `result.questions`) is a Pydantic object available directly, with no manual JSON parsing.
- `chat.completions.parse` requires `pydantic` (`uv add pydantic`).",341f71f28c
Can I use `chat.completions.parse` with Gemini instead of the newer Responses API?,"Yes — for Gemini via the OpenAI-compatible endpoint, you should use `chat.completions.parse`, not the newer Responses API.

That is the recommended way to get structured Pydantic output from Gemini while staying on the OpenAI SDK.","To get parsed structured output, use the OpenAI SDK's chat-completions parsing flow instead of the newer Responses API. This is the right choice when you want to stay on the OpenAI SDK but call a chat-completions-compatible model like Gemini through the OpenAI-compatible endpoint.

First, define the structure you want Gemini to return as a Pydantic model:

```python
from pydantic import BaseModel

class Question(BaseModel):
    question: str
    answer: str

class Questions(BaseModel):
    questions: list[Question]
```

Then call `chat.completions.parse` with `response_format=Questions`. Pass the same kind of `messages` you would to any chat completion:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

messages = [
    {""role"": ""developer"", ""content"": ""Generate three FAQ questions about RAG.""},
    {""role"": ""user"", ""content"": ""Topic: structured output with Gemini""}
]

response = client.chat.completions.parse(
    model=""gemini-3.1-flash-lite"",
    messages=messages,
    response_format=Questions
)

result = response.choices[0].message.parsed
print(result.questions)
```

Notes:
- This approach keeps using the OpenAI SDK while leveraging Gemini through Google's endpoint.
- The parsed output (e.g., `result.questions`) is a Pydantic object available directly, with no manual JSON parsing.
- `chat.completions.parse` requires `pydantic` (`uv add pydantic`).",341f71f28c
How do I define the schema so Gemini gives me a list of FAQ questions and answers?,"Define a Pydantic schema with a nested list of FAQ items, then pass it as `response_format` to `chat.completions.parse`.

Example:

```python
from pydantic import BaseModel

class Question(BaseModel):
    question: str
    answer: str

class Questions(BaseModel):
    questions: list[Question]
```

Then:

```python
response = client.chat.completions.parse(
    model=""gemini-3.1-flash-lite"",
    messages=messages,
    response_format=Questions
)

result = response.choices[0].message.parsed
print(result.questions)
```

This will give you a structured list of FAQ questions and answers.","To get parsed structured output, use the OpenAI SDK's chat-completions parsing flow instead of the newer Responses API. This is the right choice when you want to stay on the OpenAI SDK but call a chat-completions-compatible model like Gemini through the OpenAI-compatible endpoint.

First, define the structure you want Gemini to return as a Pydantic model:

```python
from pydantic import BaseModel

class Question(BaseModel):
    question: str
    answer: str

class Questions(BaseModel):
    questions: list[Question]
```

Then call `chat.completions.parse` with `response_format=Questions`. Pass the same kind of `messages` you would to any chat completion:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

messages = [
    {""role"": ""developer"", ""content"": ""Generate three FAQ questions about RAG.""},
    {""role"": ""user"", ""content"": ""Topic: structured output with Gemini""}
]

response = client.chat.completions.parse(
    model=""gemini-3.1-flash-lite"",
    messages=messages,
    response_format=Questions
)

result = response.choices[0].message.parsed
print(result.questions)
```

Notes:
- This approach keeps using the OpenAI SDK while leveraging Gemini through Google's endpoint.
- The parsed output (e.g., `result.questions`) is a Pydantic object available directly, with no manual JSON parsing.
- `chat.completions.parse` requires `pydantic` (`uv add pydantic`).",341f71f28c
"Why am I supposed to use chat completions parsing, and not manual JSON parsing, with Gemini on the OpenAI SDK?","Because `chat.completions.parse` gives you **structured, typed output directly** as a Pydantic object, while manual JSON parsing is more fragile.

With Gemini through the OpenAI-compatible endpoint, the course recommends the chat-completions parsing flow because:

- **No manual `json.loads(...)` needed** — you get `response.choices[0].message.parsed` directly.
- **Less error-prone** — it handles formatting issues better than expecting the model to return perfect raw JSON.
- **Works with the OpenAI SDK** while using Gemini via the OpenAI-compatible endpoint.
- **Returns validated Pydantic objects**, so your output matches the schema you defined.

So the reason is not that manual JSON parsing is impossible, but that `chat.completions.parse` is the cleaner and safer way to get structured output in this setup.","To get parsed structured output, use the OpenAI SDK's chat-completions parsing flow instead of the newer Responses API. This is the right choice when you want to stay on the OpenAI SDK but call a chat-completions-compatible model like Gemini through the OpenAI-compatible endpoint.

First, define the structure you want Gemini to return as a Pydantic model:

```python
from pydantic import BaseModel

class Question(BaseModel):
    question: str
    answer: str

class Questions(BaseModel):
    questions: list[Question]
```

Then call `chat.completions.parse` with `response_format=Questions`. Pass the same kind of `messages` you would to any chat completion:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI(
    api_key=os.getenv(""GEMINI_API_KEY""),
    base_url=""https://generativelanguage.googleapis.com/v1beta/openai/""
)

messages = [
    {""role"": ""developer"", ""content"": ""Generate three FAQ questions about RAG.""},
    {""role"": ""user"", ""content"": ""Topic: structured output with Gemini""}
]

response = client.chat.completions.parse(
    model=""gemini-3.1-flash-lite"",
    messages=messages,
    response_format=Questions
)

result = response.choices[0].message.parsed
print(result.questions)
```

Notes:
- This approach keeps using the OpenAI SDK while leveraging Gemini through Google's endpoint.
- The parsed output (e.g., `result.questions`) is a Pydantic object available directly, with no manual JSON parsing.
- `chat.completions.parse` requires `pydantic` (`uv add pydantic`).",341f71f28c
"How do I install Ollama on macOS, Windows, or Linux?","To install Ollama, go to **https://ollama.com/download** and choose your operating system:

- **macOS**: download the `.pkg` file and install it.
- **Windows**: download the `.msi` file and install it.
- **Linux**: run:

```bash
curl -fsSL https://ollama.com/install.sh | sh
```

After installing, you can test it with:

```bash
ollama run llama3
```

","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
"After installing Ollama, what do I type to start the local model and try it out?","Type:

```bash
ollama run llama3
```

This starts the local model and lets you try it out.","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
How can I check if the Ollama server is actually running on my machine?,"You can check it by running:

```bash
curl http://localhost:11434
```

If Ollama is running, you should get a response like:

```json
{""models"": [...]}  
```

If you get a connection error, the server is not running.","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
What do I need to install in Python to use Ollama from a script?,"You need to install the Python client:

```bash
pip install ollama
```

","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
Can you show a super simple Python example for chatting with llama3 through Ollama?,"Sure — here’s the simplest Python example for chatting with `llama3` through Ollama:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": ""Hello!""}]
)

print(response['message']['content'])
```

Before running it, make sure:

1. Ollama is installed
2. `llama3` is running locally:
   ```bash
   ollama run llama3
   ```
3. The Python client is installed:
   ```bash
   pip install ollama
   ```

If you want, I can also show an even smaller version with a variable for the prompt.","First, install Ollama by visiting [https://ollama.com/download](https://ollama.com/download) and choosing your operating system:

- **macOS**: Download the `.pkg` and install it.
- **Windows**: Download the `.msi` and install it.
- **Linux**: Run the following command in the terminal:

  ```bash
  curl -fsSL https://ollama.com/install.sh | sh
  ```

Once installed, open a terminal and type:

```bash
ollama run llama3
```

This command will:

- Download the LLaMA 3 model (~4GB).
- Start the model locally.
- Open a chat-like interface where you can type questions.

To test the Ollama local server, run the following command:

```bash
curl http://localhost:11434
```

You should receive a response similar to:

```json
{""models"": [...]}  
```

Then, install the Python client with:

```bash
pip install ollama
```

Here is a minimal Python example:

```python
import ollama

response = ollama.chat(
    model='llama3',
    messages=[{""role"": ""user"", ""content"": your_prompt}]
)

print(response['message']['content'])
```",1d0b969028
Why do I get a connection refused error when I prompt my Ollama RAG in the homework?,"If you get a “connection refused” error when prompting your Ollama RAG in the homework, restart the Ollama server with:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Also make sure to rerun the notebook cell that starts `ollama serve` if you stopped and restarted the notebook.","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
How can I fix the connection refused issue with Ollama during the RAG assignment?,"Restart the Ollama server in your notebook:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

If you stop and restart the notebook cell, rerun the cell that contains `ollama serve`.","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
Do I need to restart the Ollama server if the notebook cell was stopped or rerun?,"Yes. If you stop or rerun the notebook cell that contains `ollama serve`, you should rerun it to restart the Ollama server:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
What command should I use to bring Ollama back up after the server dies in the notebook?,"Use:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

This restarts the Ollama server in the notebook after it dies.","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
Is there a way to avoid the Ollama connection refused problem when working on Module 1 RAG?,"Yes. If you get the Ollama “connection refused” error in Module 1 RAG, the suggested fix is to restart the Ollama server with:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Also, if you stop and restart the notebook cell, rerun the cell containing `ollama serve`.","If you encounter this error while doing the homework, you can resolve it by restarting the Ollama server using the following command:

```bash
!nohup ollama serve > nohup.out 2>&1 &
```

Make sure to rerun the cell containing `ollama serve` if you stop and restart the notebook cell.",c6fc2d4d11
Why can `tiktoken.encode()` give me a lower token count than the number OpenAI shows in the API response?,"` tiktoken.encode()` can give a lower count because it only counts the text you pass in, while OpenAI’s API response includes extra internal chat-formatting tokens.

In particular:

- each chat message adds about **4 structural tokens**
- the API adds **2 tokens** to mark the start of assistant generation
- whitespace, newlines, or unusual Unicode can also add a few more

So your visible prompt might be 320 tokens in `tiktoken`, but OpenAI could report 327 because of those extra internal tokens.","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
What extra tokens does OpenAI add in chat completions that I don’t see in my prompt text?,"OpenAI adds hidden chat-formatting tokens that aren’t in your visible prompt text:

- **4 structural tokens per message** in `chat.completions.create()`  
  These cover things like the role, content separators, and formatting.
- **2 extra tokens globally** to mark the start of assistant response generation.

So your prompt text may tokenize to one number, but OpenAI’s API count will be higher because of these internal tokens.","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
"If my prompt looks like 320 tokens in `tiktoken`, why might OpenAI report something like 327 instead?","OpenAI may report a slightly higher number because `tiktoken` only counts the visible text, while the chat API adds internal formatting tokens.

In the context:

- each chat message adds about **4 structural tokens**
- OpenAI also adds **2 tokens** to mark the start of assistant generation
- extra whitespace/newlines or unusual Unicode can add a few more

So a prompt that looks like **320 tokens** in `tiktoken` might show up as **327** in OpenAI’s usage count.

","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
"Do things like newlines, spaces, or weird Unicode characters affect the token count in OpenAI chat requests?","Yes. Newlines, extra spaces, and unusual Unicode characters can affect the token count in OpenAI chat requests, and there are also extra internal tokens added by chat formatting.

In the context:
- Each chat message adds structural tokens.
- OpenAI adds 2 global tokens for assistant generation.
- Extra whitespace/newlines or uncommon Unicode can slightly increase the count.

So the token count OpenAI reports can differ from a simple `tiktoken.encode()` count.","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
How does the chat message format in `chat.completions.create()` change the final token usage count?,"The chat message format adds **internal overhead tokens** to the final count.

Specifically, in `chat.completions.create()`:

- **Each message** adds about **4 structural tokens** for things like the role, separators, and content framing.
- The API also adds **2 global tokens** to mark the start of assistant generation.
- Extra whitespace/newlines or unusual Unicode can add a bit more.

So the final token usage reported by OpenAI can be **higher than the visible text token count**. For example, your prompt may look like **320 tokens** with `tiktoken`, but OpenAI may report **327** because of these chat-formatting tokens.","When using `tiktoken.encode()` to count tokens in your prompt, you might see a difference compared to OpenAI’s API response. For instance, you might get 320 tokens, while OpenAI reports 327. This is due to internal tokens added by OpenAI’s chat formatting.

Here’s what happens:

- Each message in a `chat.completions.create()` call (e.g., `{role: ""user"", content: ""...""}`) adds 4 structural tokens (role, content, separators).
- The API adds 2 tokens globally to mark the start of assistant response generation.
- Extra newlines, whitespace, or uncommon Unicode characters in your content may slightly increase the token count.

Thus, even if your visible text is 320 tokens, OpenAI may count 327 due to these internal additions.",bf0403d21b
"How can I make my API key load automatically when I enter a project folder, without typing export every time?","Use a project-local `.env` file and auto-load it with `dirdotenv`.

### Setup
```bash
uv tool install dirdotenv

# add this to ~/.bashrc or ~/.zshrc
eval ""$(dirdotenv hook bash)""   # or zsh
```

### In your project folder
```bash
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key loads automatically when you `cd` into the project directory.

If you prefer, `direnv` also works.","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
Is there a tool like direnv that works with both .env and .envrc and is okay across different shells?,"Yes — use **`dirdotenv`**. It’s like `direnv`, but works with both **`.env`** and **`.envrc`**, and is more portable across different shells and operating systems.

If you already use `direnv`, that’s also fine.","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
What should I put in my shell config so the environment file gets loaded when I open a terminal?,"Put the export in your shell startup file, e.g. `~/.bashrc` or `~/.zshrc`, so it loads automatically when you open a terminal.

If you’re using the `.env` approach from the context, you can also keep the variables in `./.env` and start Kestra with:

```bash
docker compose --env-file ./.env -f ./docker-compose.yml up -d
```

","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
"If I’m working in GitHub Codespaces, what’s the right way to store the key instead of putting it in the repo?","Use a secret or environment variable in Codespaces, not a file committed to the repo.

Keep the key out of version control, and store it in the Codespaces environment instead.","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
"For a Python script, how do I read variables from a .env file in the project root?","Use `python-dotenv` and call `load_dotenv()` at the start of your script. It will load the `.env` file from the project root.

```bash
pip install python-dotenv
```

```python
import os
from dotenv import load_dotenv

load_dotenv()  # loads .env from project root

my_var = os.getenv(""MY_VAR"")
```

Make sure your `.env` file is in the project root and contains lines like:

```env
MY_VAR=value
```

Also add `.env` to `.gitignore` so it is not committed.","Use [`dirdotenv`](https://github.com/alexeygrigorev/dirdotenv). It is like `direnv`, but works with both `.env` and `.envrc`, and is more portable across shells and operating systems.

```bash
uv tool install dirdotenv

# add this line to your ~/.bashrc or ~/.zshrc:
eval ""$(dirdotenv hook bash)""   # or zsh

# inside your project:
echo 'OPENAI_API_KEY=sk-...' > .env
echo '.env' >> .gitignore
```

After that, the key is loaded automatically when you `cd` into the project directory.

Important: always add `.env` and `.envrc` to `.gitignore` so keys never land on GitHub.

`direnv` is also fine if you already use it. In that case, create `.envrc`, add your exports there, and run `direnv allow`.

For GitHub Codespaces, use the built-in [Codespaces secrets](https://docs.github.com/en/codespaces/managing-your-codespaces/managing-your-account-specific-secrets-for-github-codespaces) instead of files in the repo.

For Python scripts, the equivalent is `python-dotenv`:

```python
from dotenv import load_dotenv
load_dotenv()  # loads .env from project root
```",8b2f5e9d04
How do I decide what weights to give different fields in minsearch or a similar search engine?,"The systematic way is to **evaluate different weight settings against a ground-truth dataset**.

A practical process is:

1. Create a small set of representative questions.
2. Mark which documents should be retrieved for each question.
3. Try different field weights.
4. Compare retrieval metrics like **hit rate, precision@k, recall@k, or MRR**.

For small projects, you can also tune weights by trial and error, but evaluation is the more reliable approach.","The systematic approach is to evaluate different weight settings against a ground-truth dataset.

For example:

1. Create a small set of representative questions.
2. Mark which documents should be retrieved for each question.
3. Try different field weights.
4. Compare retrieval metrics such as hit rate, precision@k, recall@k, or MRR.

You can tune weights by trial and error for small projects, but evaluation is the more reliable approach. The course covers this topic more directly in the evaluation module.",87d4ea4e08
"Is there a reliable way to tune field weights, or is it just trial and error?","Yes — the reliable way is to **evaluate different weight settings against a ground-truth dataset**, not just guess.

A good process is:

1. Create a small set of representative questions.
2. Mark which documents should be retrieved for each question.
3. Try different field weights.
4. Compare retrieval metrics like **hit rate, precision@k, recall@k, or MRR**.

You can do trial and error for a small project, but **evaluation is the more reliable approach**.","The systematic approach is to evaluate different weight settings against a ground-truth dataset.

For example:

1. Create a small set of representative questions.
2. Mark which documents should be retrieved for each question.
3. Try different field weights.
4. Compare retrieval metrics such as hit rate, precision@k, recall@k, or MRR.

You can tune weights by trial and error for small projects, but evaluation is the more reliable approach. The course covers this topic more directly in the evaluation module.",87d4ea4e08
What’s the best method to compare different weight settings for search fields?,"The best method is to **evaluate different weight settings against a ground-truth dataset**.

A good process is:

1. Create a small set of representative questions.
2. Mark which documents should be retrieved for each question.
3. Try different field weights.
4. Compare retrieval metrics like **hit rate, precision@k, recall@k, or MRR**.

For small projects, trial and error can help, but **systematic evaluation is the more reliable approach**.","The systematic approach is to evaluate different weight settings against a ground-truth dataset.

For example:

1. Create a small set of representative questions.
2. Mark which documents should be retrieved for each question.
3. Try different field weights.
4. Compare retrieval metrics such as hit rate, precision@k, recall@k, or MRR.

You can tune weights by trial and error for small projects, but evaluation is the more reliable approach. The course covers this topic more directly in the evaluation module.",87d4ea4e08
Should I build a test set before adjusting search field weights?,"Yes — it’s best to build a small ground-truth test set first, then adjust the search field weights against it.

The recommended process is:

1. Create a small set of representative questions.
2. Mark which documents should be retrieved for each question.
3. Try different field weights.
4. Compare retrieval metrics like hit rate, precision@k, recall@k, or MRR.

You can do trial and error for a small project, but a test set makes the tuning much more reliable.","The systematic approach is to evaluate different weight settings against a ground-truth dataset.

For example:

1. Create a small set of representative questions.
2. Mark which documents should be retrieved for each question.
3. Try different field weights.
4. Compare retrieval metrics such as hit rate, precision@k, recall@k, or MRR.

You can tune weights by trial and error for small projects, but evaluation is the more reliable approach. The course covers this topic more directly in the evaluation module.",87d4ea4e08
Which metrics should I look at when checking whether my field weights are working well?,"You should look at retrieval metrics such as:

- hit rate
- precision@k
- recall@k
- MRR

These are used to compare different field weight settings against a ground-truth dataset.","The systematic approach is to evaluate different weight settings against a ground-truth dataset.

For example:

1. Create a small set of representative questions.
2. Mark which documents should be retrieved for each question.
3. Try different field weights.
4. Compare retrieval metrics such as hit rate, precision@k, recall@k, or MRR.

You can tune weights by trial and error for small projects, but evaluation is the more reliable approach. The course covers this topic more directly in the evaluation module.",87d4ea4e08
What’s the best way to clean documents before using them in a RAG pipeline?,"The best way is to **make the documents clean, structured, and easy to chunk and retrieve**.

In practice, that means:

- Remove obvious noise like broken HTML, duplicate text, boilerplate, OCR errors, repeated headers, and repeated footers
- Preserve useful context like titles, section names, dates, page numbers, speaker names, and Q&A structure
- Store the text in a structured format that’s easy to process
- Chunk the documents so related context stays together
- Keep metadata that may help with filtering or ranking later

The goal is not just shorter text, but **better retrieval accuracy**.","Prepare the data so it is clean, structured, and easy to chunk and retrieve.

Common steps:

- Remove obvious noise such as broken HTML, duplicate text, boilerplate, OCR errors, repeated headers, and repeated footers.
- Preserve useful context such as titles, section names, dates, page numbers, speaker names, and Q&A structure.
- Store the result in a structured format that is easy to process. JSON is often convenient, but it is not mandatory.
- Chunk the documents in a way that keeps related context together.
- Keep metadata that may help filtering or ranking later.

The exact format depends on the source data. The goal is not just to make the text shorter, but to make retrieval more accurate.",649c280e6d
"When preparing data for retrieval, what kind of junk or noise should I strip out first?","Strip out obvious noise first, such as:

- broken HTML
- duplicate text
- boilerplate
- OCR errors
- repeated headers
- repeated footers

At the same time, keep useful context like titles, section names, dates, page numbers, speaker names, and Q&A structure.","Prepare the data so it is clean, structured, and easy to chunk and retrieve.

Common steps:

- Remove obvious noise such as broken HTML, duplicate text, boilerplate, OCR errors, repeated headers, and repeated footers.
- Preserve useful context such as titles, section names, dates, page numbers, speaker names, and Q&A structure.
- Store the result in a structured format that is easy to process. JSON is often convenient, but it is not mandatory.
- Chunk the documents in a way that keeps related context together.
- Keep metadata that may help filtering or ranking later.

The exact format depends on the source data. The goal is not just to make the text shorter, but to make retrieval more accurate.",649c280e6d
"Should I keep things like titles, section headings, page numbers, and speaker names in my RAG docs, or remove them?","Keep them if they add useful context.

The course says to:

- **Remove obvious noise** like broken HTML, duplicate text, boilerplate, OCR errors, repeated headers/footers
- **Preserve useful context** such as **titles, section names, dates, page numbers, speaker names, and Q&A structure**
- Keep metadata that may help later filtering or ranking

So the answer is: **don’t remove those by default** — keep them if they help retrieval, and only strip them if they’re just repeated noise.","Prepare the data so it is clean, structured, and easy to chunk and retrieve.

Common steps:

- Remove obvious noise such as broken HTML, duplicate text, boilerplate, OCR errors, repeated headers, and repeated footers.
- Preserve useful context such as titles, section names, dates, page numbers, speaker names, and Q&A structure.
- Store the result in a structured format that is easy to process. JSON is often convenient, but it is not mandatory.
- Chunk the documents in a way that keeps related context together.
- Keep metadata that may help filtering or ranking later.

The exact format depends on the source data. The goal is not just to make the text shorter, but to make retrieval more accurate.",649c280e6d
"Do RAG documents need to be stored as JSON, or can I use some other format?","JSON is convenient, but it’s not mandatory. You can use other formats as long as the documents are clean, structured, chunkable, and easy to retrieve. The exact format depends on your source data.","Prepare the data so it is clean, structured, and easy to chunk and retrieve.

Common steps:

- Remove obvious noise such as broken HTML, duplicate text, boilerplate, OCR errors, repeated headers, and repeated footers.
- Preserve useful context such as titles, section names, dates, page numbers, speaker names, and Q&A structure.
- Store the result in a structured format that is easy to process. JSON is often convenient, but it is not mandatory.
- Chunk the documents in a way that keeps related context together.
- Keep metadata that may help filtering or ranking later.

The exact format depends on the source data. The goal is not just to make the text shorter, but to make retrieval more accurate.",649c280e6d
How do I chunk documents for RAG so that retrieval stays accurate and context doesn’t get lost?,"Chunk documents so that **related context stays together** and retrieval works on the **chunked documents**, not the full originals.

From the course guidance:

- **Remove noise first**: broken HTML, duplicates, boilerplate, OCR errors, repeated headers/footers.
- **Preserve useful context**: titles, section names, dates, page numbers, speaker names, Q&A structure.
- **Keep metadata**: anything that may help filtering or ranking later.
- **Chunk in a context-aware way**: don’t just split by length; try to keep related text together.

The goal is **not only to make text shorter**, but to make retrieval **more accurate**.

For RAG homework, “point RAG at the chunk index” means:
- build the index on the **chunks**
- retrieve **chunks**
- pass only those retrieved chunks into the prompt

If you want, I can also give you a practical chunking recipe for PDFs, Markdown, or plain text.","Prepare the data so it is clean, structured, and easy to chunk and retrieve.

Common steps:

- Remove obvious noise such as broken HTML, duplicate text, boilerplate, OCR errors, repeated headers, and repeated footers.
- Preserve useful context such as titles, section names, dates, page numbers, speaker names, and Q&A structure.
- Store the result in a structured format that is easy to process. JSON is often convenient, but it is not mandatory.
- Chunk the documents in a way that keeps related context together.
- Keep metadata that may help filtering or ranking later.

The exact format depends on the source data. The goal is not just to make the text shorter, but to make retrieval more accurate.",649c280e6d
"Why does uv show a ""Failed to hardlink files"" warning in GitHub Codespaces, and should I worry about it?","It’s not an error. In GitHub Codespaces, `uv` may not be able to hardlink files from its cache into the environment, so it shows a warning and falls back to copying the files instead. The package still installs correctly, so you generally don’t need to worry.

If you want to suppress the warning for the current shell:

```bash
export UV_LINK_MODE=copy
```

To make it persistent:

```bash
echo 'export UV_LINK_MODE=copy' >> ~/.bashrc
source ~/.bashrc
```","No. This warning can happen in GitHub Codespaces when `uv` cannot hardlink files between the cache and the target environment.

The package still installs. `uv` falls back to copying files.

To suppress the warning for the current shell:

```bash
export UV_LINK_MODE=copy
```

To make it persistent:

```bash
echo 'export UV_LINK_MODE=copy' >> ~/.bashrc
source ~/.bashrc
```

See the `uv` documentation for more details: [https://docs.astral.sh/uv/](https://docs.astral.sh/uv/).",acf8fa5356
"If uv can’t hardlink in Codespaces, does the install still work or does it break the package setup?","It still works. If `uv` can’t hardlink in Codespaces, it falls back to copying files, so the package installs and the setup does not break. You may just see a warning.","No. This warning can happen in GitHub Codespaces when `uv` cannot hardlink files between the cache and the target environment.

The package still installs. `uv` falls back to copying files.

To suppress the warning for the current shell:

```bash
export UV_LINK_MODE=copy
```

To make it persistent:

```bash
echo 'export UV_LINK_MODE=copy' >> ~/.bashrc
source ~/.bashrc
```

See the `uv` documentation for more details: [https://docs.astral.sh/uv/](https://docs.astral.sh/uv/).",acf8fa5356
How can I stop the hardlink warning from appearing every time I use uv in my current terminal session?,"Set:

```bash
export UV_LINK_MODE=copy
```

This suppresses the hardlink warning for the current terminal session. If you want it to persist, add that line to your shell config like `~/.bashrc` or `~/.zshrc`.","No. This warning can happen in GitHub Codespaces when `uv` cannot hardlink files between the cache and the target environment.

The package still installs. `uv` falls back to copying files.

To suppress the warning for the current shell:

```bash
export UV_LINK_MODE=copy
```

To make it persistent:

```bash
echo 'export UV_LINK_MODE=copy' >> ~/.bashrc
source ~/.bashrc
```

See the `uv` documentation for more details: [https://docs.astral.sh/uv/](https://docs.astral.sh/uv/).",acf8fa5356
What’s the easiest way to make uv always use copy mode in Codespaces instead of hardlinks?,"Set this environment variable permanently in your shell:

```bash
echo 'export UV_LINK_MODE=copy' >> ~/.bashrc
source ~/.bashrc
```

This makes `uv` use copy mode instead of hardlinks in Codespaces.","No. This warning can happen in GitHub Codespaces when `uv` cannot hardlink files between the cache and the target environment.

The package still installs. `uv` falls back to copying files.

To suppress the warning for the current shell:

```bash
export UV_LINK_MODE=copy
```

To make it persistent:

```bash
echo 'export UV_LINK_MODE=copy' >> ~/.bashrc
source ~/.bashrc
```

See the `uv` documentation for more details: [https://docs.astral.sh/uv/](https://docs.astral.sh/uv/).",acf8fa5356
Is there an official place to read more about uv link mode and this warning?,"Yes — the official place to read more is the uv documentation:

https://docs.astral.sh/uv/

That’s the recommended source for details about `uv link mode` and the hardlink warning.","No. This warning can happen in GitHub Codespaces when `uv` cannot hardlink files between the cache and the target environment.

The package still installs. `uv` falls back to copying files.

To suppress the warning for the current shell:

```bash
export UV_LINK_MODE=copy
```

To make it persistent:

```bash
echo 'export UV_LINK_MODE=copy' >> ~/.bashrc
source ~/.bashrc
```

See the `uv` documentation for more details: [https://docs.astral.sh/uv/](https://docs.astral.sh/uv/).",acf8fa5356
What package do I need if my terminal says dotenv is not recognized?,"Install `python-dotenv`:

```bash
uv add python-dotenv
```

Then in Python:

```python
from dotenv import load_dotenv
load_dotenv()
```","Install `python-dotenv`:

```bash
uv add python-dotenv
```

Then import and use it in Python:

```python
from dotenv import load_dotenv

load_dotenv()
```

The package is documented here: [python-dotenv](https://pypi.org/project/python-dotenv/).",0bed1f48da
How do I install dotenv support in a Python project with uv?,"Install `python-dotenv` with uv:

```bash
uv add python-dotenv
```

Then load your `.env` file in Python:

```python
from dotenv import load_dotenv

load_dotenv()
```","Install `python-dotenv`:

```bash
uv add python-dotenv
```

Then import and use it in Python:

```python
from dotenv import load_dotenv

load_dotenv()
```

The package is documented here: [python-dotenv](https://pypi.org/project/python-dotenv/).",0bed1f48da
"After installing python-dotenv, how do I load environment variables in my script?","Import and call `load_dotenv()` in your script:

```python
from dotenv import load_dotenv

load_dotenv()
```

If needed, read the variables with `os.getenv()`:

```python
import os
from dotenv import load_dotenv

load_dotenv()

api_key = os.getenv(""OPENAI_API_KEY"")
```","Install `python-dotenv`:

```bash
uv add python-dotenv
```

Then import and use it in Python:

```python
from dotenv import load_dotenv

load_dotenv()
```

The package is documented here: [python-dotenv](https://pypi.org/project/python-dotenv/).",0bed1f48da
Is there a specific library for using load_dotenv in Python?,"Yes — the specific library is `python-dotenv`.

You can install it with:

```bash
pip install python-dotenv
```

and use:

```python
from dotenv import load_dotenv

load_dotenv()
```","Install `python-dotenv`:

```bash
uv add python-dotenv
```

Then import and use it in Python:

```python
from dotenv import load_dotenv

load_dotenv()
```

The package is documented here: [python-dotenv](https://pypi.org/project/python-dotenv/).",0bed1f48da
What is the recommended way to fix a 'dotenv is not recognized' error?,"Install `python-dotenv` and load your `.env` file:

```bash
uv add python-dotenv
```

Then in Python:

```python
from dotenv import load_dotenv
load_dotenv()
```

If needed, you can also use:

```bash
pip install python-dotenv
```","Install `python-dotenv`:

```bash
uv add python-dotenv
```

Then import and use it in Python:

```python
from dotenv import load_dotenv

load_dotenv()
```

The package is documented here: [python-dotenv](https://pypi.org/project/python-dotenv/).",0bed1f48da
Can I do the course on my own laptop instead of using Codespaces?,"Yes — you can run the course locally on your own laptop instead of using Codespaces.

Codespaces is just the easiest way to give everyone the same setup. If you go local, you should be comfortable setting up Python, `uv`, Jupyter, Docker, and any other tools needed for the module, and you should document your setup so it stays reproducible.","Yes. Codespaces is just the easiest way for everyone to start with the same environment.

You can run the course locally if you are comfortable setting up Python, `uv`, Jupyter, Docker, and any other tools needed for the module.

If you run locally, make sure you document your setup and keep your environment reproducible.",aa310de435
"Is Codespaces required, or is it just the easiest option to get started?","Codespaces is **not required**. It’s just the **easiest way to get started** because everyone can use the same environment.

You can also run the course locally if you’re comfortable setting up the needed tools like Python, `uv`, Jupyter, Docker, and anything else required.","Yes. Codespaces is just the easiest way for everyone to start with the same environment.

You can run the course locally if you are comfortable setting up Python, `uv`, Jupyter, Docker, and any other tools needed for the module.

If you run locally, make sure you document your setup and keep your environment reproducible.",aa310de435
What do I need installed if I want to follow the module locally?,"If you want to follow the module locally, you should have Python, `uv`, Jupyter, Docker, and any other tools needed for the module installed.","Yes. Codespaces is just the easiest way for everyone to start with the same environment.

You can run the course locally if you are comfortable setting up Python, `uv`, Jupyter, Docker, and any other tools needed for the module.

If you run locally, make sure you document your setup and keep your environment reproducible.",aa310de435
Can I skip Codespaces and use my own Python setup for this course?,"Yes. Codespaces is just the easiest way to start with the same environment.

You can run the course locally with your own Python setup if you’re comfortable installing the needed tools such as Python, `uv`, Jupyter, Docker, and anything else required by the module.

If you do run locally, make sure your setup is documented and reproducible.","Yes. Codespaces is just the easiest way for everyone to start with the same environment.

You can run the course locally if you are comfortable setting up Python, `uv`, Jupyter, Docker, and any other tools needed for the module.

If you run locally, make sure you document your setup and keep your environment reproducible.",aa310de435
"If I run everything on my machine, do I need to keep my setup documented and reproducible?","Yes — if you run the course locally, you should still document your setup and keep it reproducible.","Yes. Codespaces is just the easiest way for everyone to start with the same environment.

You can run the course locally if you are comfortable setting up Python, `uv`, Jupyter, Docker, and any other tools needed for the module.

If you run locally, make sure you document your setup and keep your environment reproducible.",aa310de435
"If I leave my code only in Codespaces and never commit it, is it still safe there?","Yes, but only to a point.

Code saved in Codespaces is stored in the Codespaces Linux VM, so it can remain there even if you don’t commit immediately. However, it is **not fully safe to rely on long-term** because Codespaces can stop, disconnect, be rebuilt, or be deleted later, and uncommitted work can be lost in those cases.

So you should **commit and push regularly** to make sure your work is stored in your GitHub repository.","The code is saved inside the Codespaces Linux VM.

However, you should still commit your work regularly. Codespaces can stop, disconnect, or be deleted later, and committing makes sure your work is stored in your GitHub repository.",610ccb23c0
Where exactly does my work go when I save it in GitHub Codespaces before pushing anything?,"Your work is saved inside the **Codespaces Linux VM** in the `/workspaces` environment.

A few important details:
- **Uncommitted changes stay in the codespace** if you stop/start it.
- If the codespace is **deleted, recreated, or rebuilt**, those uncommitted changes can be lost.
- So you should **commit and push regularly** to keep your work in GitHub.","The code is saved inside the Codespaces Linux VM.

However, you should still commit your work regularly. Codespaces can stop, disconnect, or be deleted later, and committing makes sure your work is stored in your GitHub repository.",610ccb23c0
Do I need to commit often in Codespaces even if the files are already saved in the VM?,"Yes — you should still commit often.

Even though your files are saved inside the Codespaces Linux VM, uncommitted changes can be lost if the codespace is stopped, deleted, or recreated. Committing regularly ensures your work is stored in your GitHub repository.","The code is saved inside the Codespaces Linux VM.

However, you should still commit your work regularly. Codespaces can stop, disconnect, or be deleted later, and committing makes sure your work is stored in your GitHub repository.",610ccb23c0
What happens to my changes if the Codespaces container stops or gets deleted later?,"Your uncommitted changes are saved in the Codespaces Linux VM, but they can be lost if the Codespace is deleted or recreated later. So you should commit and push your work regularly to keep it in your GitHub repository.","The code is saved inside the Codespaces Linux VM.

However, you should still commit your work regularly. Codespaces can stop, disconnect, or be deleted later, and committing makes sure your work is stored in your GitHub repository.",610ccb23c0
"Is code in Codespaces automatically backed up to my GitHub repo, or do I have to commit it myself?","No — code in Codespaces is saved in the Codespaces Linux VM, but it is **not automatically backed up to your GitHub repo**. You should **commit and push your work regularly** so it’s stored in GitHub.","The code is saved inside the Codespaces Linux VM.

However, you should still commit your work regularly. Codespaces can stop, disconnect, or be deleted later, and committing makes sure your work is stored in your GitHub repository.",610ccb23c0
Why does WSL2 say the model needs more memory than is available even though my PC has enough RAM?,"WSL2 only sees the memory allocated to the WSL virtual machine, not all the RAM in your PC. So even if your computer has enough RAM, WSL2 may still report that the model needs more memory than is available because WSL2 itself is limited to a smaller amount.

To give WSL2 more RAM:

1. Create a `.wslconfig` file in your Windows user profile folder, e.g.  
   `C:\Users\YourUsername\.wslconfig`

2. Add something like:

```ini
[wsl2]
memory=8GB
```

3. Restart WSL:

```bash
wsl --shutdown
```

4. Check the available memory inside WSL with:

```bash
free
```

If you want, I can also help you choose a good memory value for your setup.","Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
How do I give WSL2 more RAM so I can run this model?,"Your WSL2 is likely limited to less RAM than your host machine has.

To give WSL2 more memory:

1. Create a file named `.wslconfig` in your Windows user profile folder, for example:
   `C:\Users\YourUsername\.wslconfig`

2. Add this to the file:
   ```ini
   [wsl2]
   memory=8GB
   ```

   You can change `8GB` to whatever amount you want to allocate.

3. Restart WSL:
   ```bash
   wsl --shutdown
   ```

4. Open WSL again and check available memory with:
   ```bash
   free
   ```

If you want, I can also help you choose a safe RAM value for your machine.","Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
Where should I put the .wslconfig file to change WSL2 memory limits?,"Put the `.wslconfig` file in your Windows user profile directory:

`C:\Users\YourUsername\.wslconfig`","Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
What exact setting do I need in .wslconfig to allocate 8GB to WSL2?,"Use this exact setting in your `.wslconfig`:

```ini
[wsl2]
memory=8GB
```

Then run:

```bash
wsl --shutdown
```","Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
"After changing the WSL2 memory limit, how can I check that the new RAM setting actually took effect?","Run `free` inside WSL after restarting it. If the new memory limit took effect, `free` should show the updated available RAM.","Your WSL2 is set to use Y.Y GiB, not all your computer memory. To allocate more RAM, follow these steps:

1. Create a `.wslconfig` file under your Windows user profile directory: `C:\Users\YourUsername\.wslconfig`.

2. Include the desired RAM allocation in the file:

   ```ini
   [wsl2]
   memory=8GB
   ```

3. Restart WSL using the command:

   ```bash
   wsl --shutdown
   ```

4. Run the `free` command in WSL to verify the changes.

For more details, read [this article](https://www.aleksandrhovhannisyan.com/blog/limiting-memory-usage-in-wsl-2/).",f3dd94f323
"My GitHub Codespace is stuck on ""Finishing up"" and never opens. What usually helps fix that?","Usually the first thing to try is:

1. Go to **github.com/codespaces**
2. **Stop** the stuck codespace
3. **Start it again**

If it still hangs on **“Finishing up”**:

- Open it in the **browser** instead of desktop VS Code
- Or try a different browser like **Edge, Chrome, or Brave**
- As a last resort, **delete the codespace and create a new one**

If you want, I can also summarize what to do when your Codespace setup seems to have disappeared.","These are usually GitHub Codespaces reliability issues rather than a problem with the course, so there's no single guaranteed fix — but the following workarounds resolve most cases.

**Codespace won't connect / stuck on ""Finishing up"":**

- Go to [github.com/codespaces](https://github.com/codespaces), stop the codespace, and start it again.
- If it still won't connect, open it in the **browser** instead of desktop VS Code, or try a different browser (Edge/Chrome/Brave).
- As a last resort, delete the codespace and create a new one.

**""My setup is all gone"":**

- The repo in `/workspaces` persists across stop/start, but a **rebuild or a brand-new codespace** starts from a clean image, and system/global installs outside your project don't always survive. Reinstalling is quick with `uv` (`uv sync` / `uv add ...`).
- **Commit and push your work often** — uncommitted changes survive a stop/start but are lost if you delete or recreate the codespace.

If Codespaces keeps being flaky for you, consider running the course locally instead — see ""[Can I run the course locally instead of Codespaces?](026_aa310de435_can-i-run-the-course-locally-instead-of-codespaces.md)"".",830f3d2018
"If my Codespace won’t reconnect, should I try stopping it, reopening it in the browser, or making a new one?","Try stopping it first, then reopen it in the browser if it still won’t reconnect. If neither works, make a new codespace.","These are usually GitHub Codespaces reliability issues rather than a problem with the course, so there's no single guaranteed fix — but the following workarounds resolve most cases.

**Codespace won't connect / stuck on ""Finishing up"":**

- Go to [github.com/codespaces](https://github.com/codespaces), stop the codespace, and start it again.
- If it still won't connect, open it in the **browser** instead of desktop VS Code, or try a different browser (Edge/Chrome/Brave).
- As a last resort, delete the codespace and create a new one.

**""My setup is all gone"":**

- The repo in `/workspaces` persists across stop/start, but a **rebuild or a brand-new codespace** starts from a clean image, and system/global installs outside your project don't always survive. Reinstalling is quick with `uv` (`uv sync` / `uv add ...`).
- **Commit and push your work often** — uncommitted changes survive a stop/start but are lost if you delete or recreate the codespace.

If Codespaces keeps being flaky for you, consider running the course locally instead — see ""[Can I run the course locally instead of Codespaces?](026_aa310de435_can-i-run-the-course-locally-instead-of-codespaces.md)"".",830f3d2018
Why does it look like all my setup disappeared after a rebuild or after creating a fresh Codespace?,"Because a **rebuild** or a **brand-new Codespace** starts from a **clean image**, so anything installed **outside your project** may not survive. The repo itself in `/workspaces` persists across stop/start, but system/global installs can disappear.

If you need to set it up again, it’s usually quick with:

```bash
uv sync
```

or:

```bash
uv add ...
```

Also, if your work wasn’t committed, it can be lost when you delete or recreate the Codespace.","These are usually GitHub Codespaces reliability issues rather than a problem with the course, so there's no single guaranteed fix — but the following workarounds resolve most cases.

**Codespace won't connect / stuck on ""Finishing up"":**

- Go to [github.com/codespaces](https://github.com/codespaces), stop the codespace, and start it again.
- If it still won't connect, open it in the **browser** instead of desktop VS Code, or try a different browser (Edge/Chrome/Brave).
- As a last resort, delete the codespace and create a new one.

**""My setup is all gone"":**

- The repo in `/workspaces` persists across stop/start, but a **rebuild or a brand-new codespace** starts from a clean image, and system/global installs outside your project don't always survive. Reinstalling is quick with `uv` (`uv sync` / `uv add ...`).
- **Commit and push your work often** — uncommitted changes survive a stop/start but are lost if you delete or recreate the codespace.

If Codespaces keeps being flaky for you, consider running the course locally instead — see ""[Can I run the course locally instead of Codespaces?](026_aa310de435_can-i-run-the-course-locally-instead-of-codespaces.md)"".",830f3d2018
"Do files in `/workspaces` stay there when I stop and restart a Codespace, and what gets lost if I delete it?","Yes. Files in `/workspaces` persist across stopping and starting a Codespace.

If you delete the Codespace, what gets lost is:
- uncommitted code changes
- the Codespace VM/environment itself
- system/global installs and setup outside your project

Your GitHub repo remains, so anything you committed and pushed is safe.","These are usually GitHub Codespaces reliability issues rather than a problem with the course, so there's no single guaranteed fix — but the following workarounds resolve most cases.

**Codespace won't connect / stuck on ""Finishing up"":**

- Go to [github.com/codespaces](https://github.com/codespaces), stop the codespace, and start it again.
- If it still won't connect, open it in the **browser** instead of desktop VS Code, or try a different browser (Edge/Chrome/Brave).
- As a last resort, delete the codespace and create a new one.

**""My setup is all gone"":**

- The repo in `/workspaces` persists across stop/start, but a **rebuild or a brand-new codespace** starts from a clean image, and system/global installs outside your project don't always survive. Reinstalling is quick with `uv` (`uv sync` / `uv add ...`).
- **Commit and push your work often** — uncommitted changes survive a stop/start but are lost if you delete or recreate the codespace.

If Codespaces keeps being flaky for you, consider running the course locally instead — see ""[Can I run the course locally instead of Codespaces?](026_aa310de435_can-i-run-the-course-locally-instead-of-codespaces.md)"".",830f3d2018
Codespaces keep acting flaky for me — is there an easy workaround or should I just run the course locally?,"Yes — if Codespaces keeps being flaky, you can run the course locally instead.

Codespaces is just the easiest way to start with the same environment, but local setup is fine if you’re comfortable installing Python, `uv`, Jupyter, Docker, and any other required tools. If you go local, make sure to document your setup and keep it reproducible.

If you want, I can also summarize the usual Codespaces workarounds first.","These are usually GitHub Codespaces reliability issues rather than a problem with the course, so there's no single guaranteed fix — but the following workarounds resolve most cases.

**Codespace won't connect / stuck on ""Finishing up"":**

- Go to [github.com/codespaces](https://github.com/codespaces), stop the codespace, and start it again.
- If it still won't connect, open it in the **browser** instead of desktop VS Code, or try a different browser (Edge/Chrome/Brave).
- As a last resort, delete the codespace and create a new one.

**""My setup is all gone"":**

- The repo in `/workspaces` persists across stop/start, but a **rebuild or a brand-new codespace** starts from a clean image, and system/global installs outside your project don't always survive. Reinstalling is quick with `uv` (`uv sync` / `uv add ...`).
- **Commit and push your work often** — uncommitted changes survive a stop/start but are lost if you delete or recreate the codespace.

If Codespaces keeps being flaky for you, consider running the course locally instead — see ""[Can I run the course locally instead of Codespaces?](026_aa310de435_can-i-run-the-course-locally-instead-of-codespaces.md)"".",830f3d2018
"Can I skip OpenAI and use another LLM provider for the RAG homework, or is the course locked to OpenAI only?","Yes — you can use another LLM provider. The course is **not locked to OpenAI**.

You can use:
- OpenAI-compatible hosted providers like **Groq, OpenRouter, DeepSeek, Gemini, Z.ai, Mistral**
- Hosted open models via **Hugging Face**
- Local models with **Ollama, vLLM, LM Studio**, etc.
- A GPU machine serving your own model endpoint

Usually you only need to change:
- `base_url`
- API key
- model name

So for the RAG homework, feel free to skip OpenAI if you want or need to.","If the provider used in the course isn't available or is blocked in your region (or you simply prefer another one), you can use any other LLM provider — the course isn't tied to OpenAI. Just switch to something else:

- Hosted, OpenAI-compatible providers — e.g. Groq, OpenRouter, DeepSeek, Gemini, Z.ai, Mistral. The course code uses the OpenAI client, so you usually only need to change the `base_url`, the API key, and the model name.
- Open models via Hugging Face (e.g. Qwen, Llama) if you prefer hosted open-source models.
- Serve a model locally with [Ollama](https://ollama.com/), [vLLM](https://github.com/vllm-project/vllm), LM Studio, or anything else — no external API call at all, so regional blocks don't apply and you don't need a paid key. Most of these also expose an OpenAI-compatible endpoint, so the course code works with only a `base_url` change.
- Rent a GPU machine and serve the model there (e.g. with vLLM) if your own machine can't run the model you want. This gives you a private OpenAI-compatible endpoint to point the course code at — just remember to stop/delete the instance when you're done so you don't keep paying for it.
- A VPN also works if you just need to reach a provider that's blocked at the network level.

Anything with an OpenAI-compatible endpoint (or a locally served model) will work — pick whatever is available and convenient for you.

For a curated list of options, see [Awesome LLMs](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo, which collects OpenAI API alternatives and tools for running models locally.",c6ff9083c0
"If the provider from the course is blocked in my country, what are the easiest alternatives I can plug into the same code?","Yes — the easiest alternatives are any provider with an OpenAI-compatible API, because you can usually keep the same code and just change:

- `base_url`
- API key
- model name

Good options mentioned in the course are:

- Groq
- OpenRouter
- Gemini
- DeepSeek
- Z.ai
- Mistral
- Azure OpenAI

If you want to avoid regional blocks entirely, you can also run a model locally with:

- Ollama
- vLLM
- LM Studio

Then your code usually only needs a `base_url` change.

If you need, I can also suggest the simplest option based on your country and whether you want free or local-only.","If the provider used in the course isn't available or is blocked in your region (or you simply prefer another one), you can use any other LLM provider — the course isn't tied to OpenAI. Just switch to something else:

- Hosted, OpenAI-compatible providers — e.g. Groq, OpenRouter, DeepSeek, Gemini, Z.ai, Mistral. The course code uses the OpenAI client, so you usually only need to change the `base_url`, the API key, and the model name.
- Open models via Hugging Face (e.g. Qwen, Llama) if you prefer hosted open-source models.
- Serve a model locally with [Ollama](https://ollama.com/), [vLLM](https://github.com/vllm-project/vllm), LM Studio, or anything else — no external API call at all, so regional blocks don't apply and you don't need a paid key. Most of these also expose an OpenAI-compatible endpoint, so the course code works with only a `base_url` change.
- Rent a GPU machine and serve the model there (e.g. with vLLM) if your own machine can't run the model you want. This gives you a private OpenAI-compatible endpoint to point the course code at — just remember to stop/delete the instance when you're done so you don't keep paying for it.
- A VPN also works if you just need to reach a provider that's blocked at the network level.

Anything with an OpenAI-compatible endpoint (or a locally served model) will work — pick whatever is available and convenient for you.

For a curated list of options, see [Awesome LLMs](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo, which collects OpenAI API alternatives and tools for running models locally.",c6ff9083c0
"Do I need to rewrite the course code if I want to use Groq, OpenRouter, DeepSeek, Gemini, Mistral, or another OpenAI-style API?","Usually **no major rewrite** is needed.

If you use an **OpenAI-compatible API** like **Groq, OpenRouter, DeepSeek, Gemini, Mistral**, etc., the course code often works with just a few changes:

- `base_url`
- API key
- model name

However, you **may need to adapt some code** because different providers can have different:

- response formats
- tool/function calling schemas
- token usage fields
- tokenizer behavior

So the short answer is: **you usually only need small changes, not a full rewrite**.","If the provider used in the course isn't available or is blocked in your region (or you simply prefer another one), you can use any other LLM provider — the course isn't tied to OpenAI. Just switch to something else:

- Hosted, OpenAI-compatible providers — e.g. Groq, OpenRouter, DeepSeek, Gemini, Z.ai, Mistral. The course code uses the OpenAI client, so you usually only need to change the `base_url`, the API key, and the model name.
- Open models via Hugging Face (e.g. Qwen, Llama) if you prefer hosted open-source models.
- Serve a model locally with [Ollama](https://ollama.com/), [vLLM](https://github.com/vllm-project/vllm), LM Studio, or anything else — no external API call at all, so regional blocks don't apply and you don't need a paid key. Most of these also expose an OpenAI-compatible endpoint, so the course code works with only a `base_url` change.
- Rent a GPU machine and serve the model there (e.g. with vLLM) if your own machine can't run the model you want. This gives you a private OpenAI-compatible endpoint to point the course code at — just remember to stop/delete the instance when you're done so you don't keep paying for it.
- A VPN also works if you just need to reach a provider that's blocked at the network level.

Anything with an OpenAI-compatible endpoint (or a locally served model) will work — pick whatever is available and convenient for you.

For a curated list of options, see [Awesome LLMs](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo, which collects OpenAI API alternatives and tools for running models locally.",c6ff9083c0
"Is it possible to run a model locally with Ollama, vLLM, or LM Studio and still follow the course exercises without an external API?","Yes. You can run a model locally with Ollama, vLLM, or LM Studio and still follow the course exercises without using an external API.

The course notes say that any locally served model works, and most local servers expose an OpenAI-compatible endpoint, so you usually only need to change the `base_url` in the course code.","If the provider used in the course isn't available or is blocked in your region (or you simply prefer another one), you can use any other LLM provider — the course isn't tied to OpenAI. Just switch to something else:

- Hosted, OpenAI-compatible providers — e.g. Groq, OpenRouter, DeepSeek, Gemini, Z.ai, Mistral. The course code uses the OpenAI client, so you usually only need to change the `base_url`, the API key, and the model name.
- Open models via Hugging Face (e.g. Qwen, Llama) if you prefer hosted open-source models.
- Serve a model locally with [Ollama](https://ollama.com/), [vLLM](https://github.com/vllm-project/vllm), LM Studio, or anything else — no external API call at all, so regional blocks don't apply and you don't need a paid key. Most of these also expose an OpenAI-compatible endpoint, so the course code works with only a `base_url` change.
- Rent a GPU machine and serve the model there (e.g. with vLLM) if your own machine can't run the model you want. This gives you a private OpenAI-compatible endpoint to point the course code at — just remember to stop/delete the instance when you're done so you don't keep paying for it.
- A VPN also works if you just need to reach a provider that's blocked at the network level.

Anything with an OpenAI-compatible endpoint (or a locally served model) will work — pick whatever is available and convenient for you.

For a curated list of options, see [Awesome LLMs](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo, which collects OpenAI API alternatives and tools for running models locally.",c6ff9083c0
Where can I find a list of good LLM providers and local model tools that work with this course setup?,"You can find it in the course repo’s **awesome-llms** list:

- https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md

It tracks free and free-tier LLM providers and local model tools that work with the course setup. The course context also notes that Kestra supports OpenAI-compatible providers, including local models served through **Ollama** or **LM Studio**, and that **Groq** is a popular option because it’s OpenAI-compatible.","If the provider used in the course isn't available or is blocked in your region (or you simply prefer another one), you can use any other LLM provider — the course isn't tied to OpenAI. Just switch to something else:

- Hosted, OpenAI-compatible providers — e.g. Groq, OpenRouter, DeepSeek, Gemini, Z.ai, Mistral. The course code uses the OpenAI client, so you usually only need to change the `base_url`, the API key, and the model name.
- Open models via Hugging Face (e.g. Qwen, Llama) if you prefer hosted open-source models.
- Serve a model locally with [Ollama](https://ollama.com/), [vLLM](https://github.com/vllm-project/vllm), LM Studio, or anything else — no external API call at all, so regional blocks don't apply and you don't need a paid key. Most of these also expose an OpenAI-compatible endpoint, so the course code works with only a `base_url` change.
- Rent a GPU machine and serve the model there (e.g. with vLLM) if your own machine can't run the model you want. This gives you a private OpenAI-compatible endpoint to point the course code at — just remember to stop/delete the instance when you're done so you don't keep paying for it.
- A VPN also works if you just need to reach a provider that's blocked at the network level.

Anything with an OpenAI-compatible endpoint (or a locally served model) will work — pick whatever is available and convenient for you.

For a curated list of options, see [Awesome LLMs](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo, which collects OpenAI API alternatives and tools for running models locally.",c6ff9083c0
"Why am I getting 429 rate limit errors in the RAG notebooks, and what should I do first to avoid them?","You’re getting 429 errors because the RAG notebooks can make lots of OpenAI API calls very quickly, and free-tier or low-spend accounts have per-minute and per-day limits that are easy to exceed.

What you should do first: **reduce the number of API calls** before trying to handle errors. In practice, that means:

- **Cache results** so re-running the notebook doesn’t call the API again for the same inputs
- **Batch requests** where possible instead of looping one-by-one
- **Lower concurrency** if you’re using multiple workers
- Use retries/backoff as a backup, not the first fix

If the error is actually `insufficient_quota`, that’s not a rate limit problem — it means you need to add billing/credits.","Free-tier and low-spend OpenAI accounts have per-minute and per-day request limits that are easy to blow through when a notebook loops over many documents. Reduce the number of API calls before you worry about handling the error after the fact.

- **Retry with backoff.** The `openai` Python client retries automatically with exponential backoff; raise the limit so transient 429s resolve themselves instead of crashing the run:

  ```python
  from openai import OpenAI

  client = OpenAI(max_retries=5)
  ```

- **Lower concurrency.** In a thread/process pool, keep the pool small (2-3 workers) so you stay under the per-minute cap. A larger pool finishes one batch fast and then fails on the next.
- **Cache results.** Write embeddings and LLM responses to disk (JSONL, pickle, or a vector DB) and reload on re-run. Re-executing a notebook should not re-call the API for inputs you already processed.
- **Batch where possible.** Group independent inputs into a single request (e.g. embed a list of texts in one call) instead of looping one-by-one.
- **Use a cheaper/free provider.** `gpt-4o-mini` is cheap enough for the whole course; Groq's free tier (`llama-3.3-70b-versatile`) has generous per-minute limits via the OpenAI-compatible endpoint.

If you already see `insufficient_quota`, that is a billing issue, not a rate limit — see [OpenAI: Error: RateLimitError: Error code: 429](openai-error-ratelimiterror-error-code-429).",8e21752415
How can I set up the OpenAI client so temporary rate-limit errors retry instead of stopping my run?,"Set the OpenAI client with a higher retry count so temporary 429s are retried automatically:

```python
from openai import OpenAI

client = OpenAI(max_retries=5)
```

This uses exponential backoff and helps transient rate-limit errors resolve without stopping your run.","Free-tier and low-spend OpenAI accounts have per-minute and per-day request limits that are easy to blow through when a notebook loops over many documents. Reduce the number of API calls before you worry about handling the error after the fact.

- **Retry with backoff.** The `openai` Python client retries automatically with exponential backoff; raise the limit so transient 429s resolve themselves instead of crashing the run:

  ```python
  from openai import OpenAI

  client = OpenAI(max_retries=5)
  ```

- **Lower concurrency.** In a thread/process pool, keep the pool small (2-3 workers) so you stay under the per-minute cap. A larger pool finishes one batch fast and then fails on the next.
- **Cache results.** Write embeddings and LLM responses to disk (JSONL, pickle, or a vector DB) and reload on re-run. Re-executing a notebook should not re-call the API for inputs you already processed.
- **Batch where possible.** Group independent inputs into a single request (e.g. embed a list of texts in one call) instead of looping one-by-one.
- **Use a cheaper/free provider.** `gpt-4o-mini` is cheap enough for the whole course; Groq's free tier (`llama-3.3-70b-versatile`) has generous per-minute limits via the OpenAI-compatible endpoint.

If you already see `insufficient_quota`, that is a billing issue, not a rate limit — see [OpenAI: Error: RateLimitError: Error code: 429](openai-error-ratelimiterror-error-code-429).",8e21752415
What’s the best way to keep my notebook from making too many API calls when it loops over lots of docs?,"The best way is to **cache your results** so rerunning the notebook doesn’t call the API again for docs you’ve already processed.

A few practical tips from the course:

- **Write embeddings and LLM outputs to disk** and reload them on rerun.
- **Batch inputs** when possible, instead of calling the API one document at a time.
- **Keep concurrency low** if you’re using threads/processes.
- Use a **cheaper model** like `gpt-4o-mini` to reduce cost if you do need many calls.

If you want, I can also suggest a simple caching pattern for a notebook.","Free-tier and low-spend OpenAI accounts have per-minute and per-day request limits that are easy to blow through when a notebook loops over many documents. Reduce the number of API calls before you worry about handling the error after the fact.

- **Retry with backoff.** The `openai` Python client retries automatically with exponential backoff; raise the limit so transient 429s resolve themselves instead of crashing the run:

  ```python
  from openai import OpenAI

  client = OpenAI(max_retries=5)
  ```

- **Lower concurrency.** In a thread/process pool, keep the pool small (2-3 workers) so you stay under the per-minute cap. A larger pool finishes one batch fast and then fails on the next.
- **Cache results.** Write embeddings and LLM responses to disk (JSONL, pickle, or a vector DB) and reload on re-run. Re-executing a notebook should not re-call the API for inputs you already processed.
- **Batch where possible.** Group independent inputs into a single request (e.g. embed a list of texts in one call) instead of looping one-by-one.
- **Use a cheaper/free provider.** `gpt-4o-mini` is cheap enough for the whole course; Groq's free tier (`llama-3.3-70b-versatile`) has generous per-minute limits via the OpenAI-compatible endpoint.

If you already see `insufficient_quota`, that is a billing issue, not a rate limit — see [OpenAI: Error: RateLimitError: Error code: 429](openai-error-ratelimiterror-error-code-429).",8e21752415
"Should I use fewer workers in my thread pool for embeddings, and what number is safe?","Yes — for embeddings, use fewer workers. A safe pool size is **2–3 workers**.

That helps avoid hitting rate limits, especially on free-tier or low-spend API accounts.","Free-tier and low-spend OpenAI accounts have per-minute and per-day request limits that are easy to blow through when a notebook loops over many documents. Reduce the number of API calls before you worry about handling the error after the fact.

- **Retry with backoff.** The `openai` Python client retries automatically with exponential backoff; raise the limit so transient 429s resolve themselves instead of crashing the run:

  ```python
  from openai import OpenAI

  client = OpenAI(max_retries=5)
  ```

- **Lower concurrency.** In a thread/process pool, keep the pool small (2-3 workers) so you stay under the per-minute cap. A larger pool finishes one batch fast and then fails on the next.
- **Cache results.** Write embeddings and LLM responses to disk (JSONL, pickle, or a vector DB) and reload on re-run. Re-executing a notebook should not re-call the API for inputs you already processed.
- **Batch where possible.** Group independent inputs into a single request (e.g. embed a list of texts in one call) instead of looping one-by-one.
- **Use a cheaper/free provider.** `gpt-4o-mini` is cheap enough for the whole course; Groq's free tier (`llama-3.3-70b-versatile`) has generous per-minute limits via the OpenAI-compatible endpoint.

If you already see `insufficient_quota`, that is a billing issue, not a rate limit — see [OpenAI: Error: RateLimitError: Error code: 429](openai-error-ratelimiterror-error-code-429).",8e21752415
Is there a cheap/free option I can use for the course so I don’t hit OpenAI limits so fast?,"Yes — you don’t have to use paid OpenAI for the course.

Cheap/free options mentioned in the course are:

- **`gpt-4o-mini`** — very cheap and should be enough for the whole course
- **Groq free tier** — `llama-3.3-70b-versatile` via an OpenAI-compatible endpoint
- Other **OpenAI-compatible providers** like OpenRouter, Gemini, Mistral, DeepSeek, Z.ai, etc.
- **Local models** with Ollama, LM Studio, or vLLM, so you don’t call an external API at all

If you want the simplest low-cost route, the context specifically recommends **`gpt-4o-mini`** or **Groq’s free tier**.","Free-tier and low-spend OpenAI accounts have per-minute and per-day request limits that are easy to blow through when a notebook loops over many documents. Reduce the number of API calls before you worry about handling the error after the fact.

- **Retry with backoff.** The `openai` Python client retries automatically with exponential backoff; raise the limit so transient 429s resolve themselves instead of crashing the run:

  ```python
  from openai import OpenAI

  client = OpenAI(max_retries=5)
  ```

- **Lower concurrency.** In a thread/process pool, keep the pool small (2-3 workers) so you stay under the per-minute cap. A larger pool finishes one batch fast and then fails on the next.
- **Cache results.** Write embeddings and LLM responses to disk (JSONL, pickle, or a vector DB) and reload on re-run. Re-executing a notebook should not re-call the API for inputs you already processed.
- **Batch where possible.** Group independent inputs into a single request (e.g. embed a list of texts in one call) instead of looping one-by-one.
- **Use a cheaper/free provider.** `gpt-4o-mini` is cheap enough for the whole course; Groq's free tier (`llama-3.3-70b-versatile`) has generous per-minute limits via the OpenAI-compatible endpoint.

If you already see `insufficient_quota`, that is a billing issue, not a rate limit — see [OpenAI: Error: RateLimitError: Error code: 429](openai-error-ratelimiterror-error-code-429).",8e21752415
"What exactly are tools or functions in agentic RAG, and why do we need them?","In agentic RAG, tools/functions are things the model can **call for external help** when it needs it. Examples include:

- searching documents
- querying a database
- calling an API
- running a calculation

In this course, “tools” and “functions” are basically the same idea, so don’t worry about the naming difference.

We need them because in **agentic RAG** the model can **decide when to use them**. That’s different from basic RAG, where retrieval is usually a fixed step that happens before the model answers.

So the main reason is: tools/functions let the model act more flexibly and get help only when needed.","In the context of this course, tools and functions are closely related terms. Do not worry too much about the naming difference.

A tool/function is something the model can call when it needs external help, such as searching documents, querying a database, calling an API, or running a calculation.

The important idea is that in agentic RAG, the model can decide when to call a tool/function. In basic RAG, retrieval is usually a fixed step that happens before the model answers.",c296ea5a21
How is a tool call different from the normal RAG retrieval step?,"A tool call is an action the model decides to trigger when it needs outside help, like searching docs, querying a database, calling an API, or doing a calculation.

In normal RAG, retrieval is usually a fixed preprocessing step that happens before the model answers. In agentic RAG, the model can decide **when** to call the tool itself.

","In the context of this course, tools and functions are closely related terms. Do not worry too much about the naming difference.

A tool/function is something the model can call when it needs external help, such as searching documents, querying a database, calling an API, or running a calculation.

The important idea is that in agentic RAG, the model can decide when to call a tool/function. In basic RAG, retrieval is usually a fixed step that happens before the model answers.",c296ea5a21
Can a model decide on its own when to use a function in agentic RAG?,"Yes. In agentic RAG, the model can decide when to call a tool/function if it needs external help, such as searching documents, querying a database, calling an API, or running a calculation.","In the context of this course, tools and functions are closely related terms. Do not worry too much about the naming difference.

A tool/function is something the model can call when it needs external help, such as searching documents, querying a database, calling an API, or running a calculation.

The important idea is that in agentic RAG, the model can decide when to call a tool/function. In basic RAG, retrieval is usually a fixed step that happens before the model answers.",c296ea5a21
What are some real examples of tools a RAG system might call?,"Some real examples mentioned in the course are:

- searching documents
- querying a database
- calling an API
- running a calculation

For example, the course also shows a weather API call like: “What’s the weather in Berlin?”","In the context of this course, tools and functions are closely related terms. Do not worry too much about the naming difference.

A tool/function is something the model can call when it needs external help, such as searching documents, querying a database, calling an API, or running a calculation.

The important idea is that in agentic RAG, the model can decide when to call a tool/function. In basic RAG, retrieval is usually a fixed step that happens before the model answers.",c296ea5a21
Should I care a lot about the difference between the words tool and function in this course?,"Not really. In this course, **tool** and **function** are used as closely related terms, so you **shouldn’t worry too much about the naming difference**.

The main idea is that it’s something the model can call for external help, like:
- searching documents
- querying a database
- calling an API
- running a calculation

What matters more is **that the model can decide when to call it** in agentic RAG.","In the context of this course, tools and functions are closely related terms. Do not worry too much about the naming difference.

A tool/function is something the model can call when it needs external help, such as searching documents, querying a database, calling an API, or running a calculation.

The important idea is that in agentic RAG, the model can decide when to call a tool/function. In basic RAG, retrieval is usually a fixed step that happens before the model answers.",c296ea5a21
Which free models can I use if I need tool calling support?,"Several free models support tool calling. From the context, examples include:

- **Groq models** such as **Deepseek R1** and **Llama 4**  
- **Mistral**
- **Gemini**
- **Some local Ollama models**

If you use a provider other than OpenAI, you may need to adjust the tool schema and response handling because formats can differ.","Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
Does Groq have any free models that work with tools or function calling?,"Yes. Groq has several free models that support tool use / function calling, such as **DeepSeek R1** and **Llama 4**, and they can be used for free for development.","Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
"What are some examples of free models with tool use, like Deepseek R1 or Llama 4?","Some examples mentioned in the context are:

- **Deepseek R1**
- **Llama 4**

Also, other providers/models with tool or function calling support include:

- **Mistral**
- **Gemini**
- **some local Ollama models**

These can be used for free for development in some cases, especially via providers like **Groq**.","Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
"Besides Groq, which providers support tool or function calling too?","Besides Groq, the providers mentioned as supporting tool or function calling are:

- **Mistral**
- **Gemini**
- **some local Ollama models**","Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
Do I need to change my code when I switch from OpenAI to another model provider for tools?,"Yes, usually a little.

If you switch from OpenAI to another model provider for tools, you may need to adapt your code because:

- response formats can differ
- tool/function schema formats can differ
- tokenizers differ

If the other provider is **OpenAI-compatible** (for example Groq, OpenRouter, or some local servers), you often only need to change:

- `base_url`
- API key
- model name

But for some APIs, especially non-OpenAI ones, you may need to change the way you call the model and how you parse the response.

So the short answer is: **maybe small changes, not always a full rewrite**.","Several Groq models offer tool use, such as Deepseek R1 or Llama 4, all of which can be used for free for development.

Other providers also support tool or function calling, including Mistral, Gemini, and some local Ollama models.

You'll typically need to adapt the code when not using OpenAI, because tool schemas and response shapes differ between providers.

For more details, see the [Groq Tool Use Documentation](https://console.groq.com/docs/tool-use).",0d74a3616f
Why do I get `AttributeError: 'str' object has no attribute 'output'` when I try `client.responses.create(...)` with a model that isn't from OpenAI?,"Because `client.responses.create(...)` and `response.output` are part of OpenAI’s **Responses API**, which is **OpenAI-specific**. Non-OpenAI providers like Mistral, Groq, and Gemini don’t implement that API, so the object you get back isn’t shaped the way the OpenAI Responses API expects, leading to:

`AttributeError: 'str' object has no attribute 'output'`

For non-OpenAI models, use the **chat completions** API instead and read:

```python
response.choices[0].message.content
```

Example:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)

return response.choices[0].message.content
```

So the fix is:
- don’t use `responses.create(...)` with non-OpenAI models
- use `chat.completions.create(...)` instead
- adapt the tools schema if needed for that provider","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
"If I’m using Mistral or Groq instead of OpenAI, what should I use to read the assistant reply instead of `response.output`?","Use `response.choices[0].message.content`.

For non-OpenAI providers using `chat.completions.create(...)`, the assistant reply is typically read from:

```python
response.choices[0].message.content
```

`response.output` is for OpenAI’s Responses API, which Mistral and Groq don’t implement.","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
Can I still use the old `chat.completions` style for the homework if the new Responses API doesn’t work with my provider?,"Yes — it’s okay to use the older `chat.completions` API for the homework if the new Responses API doesn’t work with your provider.

For non-OpenAI providers, use:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,
)
return response.choices[0].message.content
```

Just keep in mind you may need to adapt the tool schema and token/usage fields to whatever your provider returns.","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
What needs to change in the code when switching from OpenAI Responses API to a non-OpenAI provider?,"When switching from OpenAI’s Responses API to a non-OpenAI provider, you need to change:

- **The API call**: use `client.chat.completions.create(...)` instead of `client.responses.create(...)`
- **How you read the answer**: use `response.choices[0].message.content` instead of `response.output`
- **The tools schema**: adapt it to whatever format your provider expects

Example:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

If the provider supports an OpenAI-compatible endpoint, you may only need to change the `base_url`, API key, and model name.","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
Do the tools definitions also have to be updated when I move from `responses.create` to `chat.completions.create`?,"Yes — you may need to adapt the tools schema when moving from `responses.create` to `chat.completions.create`.

The context says:

- For non-OpenAI providers, use `chat.completions.create(...)`
- And **“You’ll also have to adapt the tools schema to whatever shape your provider expects.”**

So the answer is: **possibly yes, depending on the provider**. The chat-completions API itself may be compatible, but the exact `tools` format can differ from the Responses API or vary by model/provider.","The new OpenAI Responses API (`client.responses.create(...)`, accessed via `response.output`) is OpenAI-specific. Other providers (Mistral, Groq, Gemini, etc.) don't implement it.

For non-OpenAI providers, use the chat-completions API and read `response.choices[0].message.content`:

```python
response = client.chat.completions.create(
    model=""<provider-model>"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    tools=tools_schema,  # may need adapting per provider
)
return response.choices[0].message.content
```

You'll also have to adapt the tools schema to whatever shape your provider expects.

It is okay to use the older `chat.completions` API for homework or projects if your provider supports that interface better than the Responses API.",9e7b3f0c25
"Why am I getting Error code 400 with Azure OpenAI saying ""Missing required parameter: tools[0].function"" when I try to use a weather tool?","Modify the `get_weather_tool` JSON so the tool includes a `function` object, like this:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```

This error happens because Azure OpenAI expects the tool schema to include `tools[0].function`, and your current weather tool definition is missing it.","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
What should the JSON for my weather tool look like in Agentic RAG so Azure OpenAI accepts it?,"Use this JSON for your weather tool so Azure OpenAI accepts it:

```json
{
  ""type"": ""function"",
  ""function"": {
    ""name"": ""get_weather"",
    ""description"": ""Get the current weather for a specific city or generate fake data"",
    ""parameters"": {
      ""type"": ""object"",
      ""properties"": {
        ""city"": {
          ""type"": ""string"",
          ""description"": ""The name of the city to get the weather for.""
        }
      },
      ""required"": [""city""],
      ""additionalProperties"": false
    }
  }
}
```

","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
How do I fix the tool definition if Azure says the first tool is missing the function field?,"Modify the `get_weather_tool` JSON to this:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
Can you show the correct `get_weather_tool` structure for a function tool with a city parameter?,"Yes — the correct `get_weather_tool` structure is:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
I keep getting a bad request in Azure OpenAI for my weather tool setup — what exact change do I need to make?,"Modify your `get_weather_tool` JSON to include the nested `""function""` object exactly like this:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```

The bad request happens because Azure OpenAI expects `tools[0].function`, and your tool definition is missing that structure.","Modify the `get_weather_tool` JSON to be the following:

```json
get_weather_tool = {
    ""type"": ""function"",
    ""function"": {
        ""name"": ""get_weather"",
        ""description"": ""Get the current weather for a specific city or generate fake data"",
        ""parameters"": {
            ""type"": ""object"",
            ""properties"": {
                ""city"": {
                    ""type"": ""string"",
                    ""description"": ""The name of the city to get the weather for.""
                }
            },
            ""required"": [""city""],
            ""additionalProperties"": false
        }
    }
}
```",b9c7c6bb34
"Why do I get a 404 with toyaikit on Groq, and what class should I use instead of the Responses runner?","You get the `404` because `OpenAIResponsesRunner` / `OpenAIClient` use OpenAI’s **Responses** endpoint (`responses.create`), and that endpoint is only implemented by OpenAI.

For Groq or other OpenAI-compatible providers, use the **chat completions** runner instead:

- `OpenAIChatCompletionsRunner`

If you want, I can also show the exact Groq example from the course context.","If you get a `404` (or similar) when using toyaikit with Groq or another non-OpenAI provider, it's because `OpenAIResponsesRunner` / `OpenAIClient` call OpenAI's Responses endpoint (`responses.create`), which only OpenAI implements. Switch to the **chat completions** classes, which use the standard `chat.completions.create` endpoint that Groq and other OpenAI-compatible providers support.

### OpenAI or Groq (chat completions)

```python
import os
from openai import OpenAI
from toyaikit.tools import Tools
from toyaikit.llm import OpenAIChatCompletionsClient
from toyaikit.chat.runners import OpenAIChatCompletionsRunner

tools = Tools()
tools.add_tools(my_tools_object)   # functions need type hints + an Args: docstring

# OpenAI:
llm_client = OpenAIChatCompletionsClient(model=""gpt-4o-mini"", client=OpenAI())

# Groq (same runner, just point the OpenAI client at Groq's base URL):
groq_client = OpenAI(
    api_key=os.getenv(""GROQ_API_KEY""),
    base_url=""https://api.groq.com/openai/v1"",
)
llm_client = OpenAIChatCompletionsClient(model=""llama-3.3-70b-versatile"", client=groq_client)

runner = OpenAIChatCompletionsRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

### Anthropic

```python
from toyaikit.tools import Tools
from toyaikit.llm import AnthropicClient
from toyaikit.chat.runners import AnthropicMessagesRunner

tools = Tools()
tools.add_tools(my_tools_object)

# Reads ANTHROPIC_API_KEY from the environment automatically:
llm_client = AnthropicClient(model=""claude-haiku-4-5"")

runner = AnthropicMessagesRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

Notes:

- `runner.loop(prompt=...)` runs one turn programmatically and returns a result with `.last_message`, `.all_messages`, and `.tokens` — no chat interface needed. Use `runner.run()` only for the interactive Jupyter loop.
- Pass an explicit, current model id. For Anthropic, `claude-haiku-4-5` / `claude-sonnet-4-5` work; an outdated id can 404.
- For Groq you'll see a harmless `UnknownModelWarning: No pricing data...` — the call still succeeds, only cost calculation is skipped.",0224d980b3
How can I run toyaikit with an OpenAI-compatible provider like Groq without hitting the OpenAI Responses endpoint?,"Use **toyaikit’s chat-completions classes**, not the Responses-based ones.

`OpenAIResponsesRunner` / `OpenAIClient` call `responses.create`, which only OpenAI supports. For Groq or other OpenAI-compatible providers, switch to:

- `OpenAIChatCompletionsClient`
- `OpenAIChatCompletionsRunner`

Example with Groq:

```python
import os
from openai import OpenAI
from toyaikit.tools import Tools
from toyaikit.llm import OpenAIChatCompletionsClient
from toyaikit.chat.runners import OpenAIChatCompletionsRunner

tools = Tools()
tools.add_tools(my_tools_object)

groq_client = OpenAI(
    api_key=os.getenv(""GROQ_API_KEY""),
    base_url=""https://api.groq.com/openai/v1"",
)

llm_client = OpenAIChatCompletionsClient(
    model=""llama-3.3-70b-versatile"",
    client=groq_client,
)

runner = OpenAIChatCompletionsRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)

result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

This uses `chat.completions.create`, so it works with Groq and similar providers without hitting OpenAI’s Responses endpoint.","If you get a `404` (or similar) when using toyaikit with Groq or another non-OpenAI provider, it's because `OpenAIResponsesRunner` / `OpenAIClient` call OpenAI's Responses endpoint (`responses.create`), which only OpenAI implements. Switch to the **chat completions** classes, which use the standard `chat.completions.create` endpoint that Groq and other OpenAI-compatible providers support.

### OpenAI or Groq (chat completions)

```python
import os
from openai import OpenAI
from toyaikit.tools import Tools
from toyaikit.llm import OpenAIChatCompletionsClient
from toyaikit.chat.runners import OpenAIChatCompletionsRunner

tools = Tools()
tools.add_tools(my_tools_object)   # functions need type hints + an Args: docstring

# OpenAI:
llm_client = OpenAIChatCompletionsClient(model=""gpt-4o-mini"", client=OpenAI())

# Groq (same runner, just point the OpenAI client at Groq's base URL):
groq_client = OpenAI(
    api_key=os.getenv(""GROQ_API_KEY""),
    base_url=""https://api.groq.com/openai/v1"",
)
llm_client = OpenAIChatCompletionsClient(model=""llama-3.3-70b-versatile"", client=groq_client)

runner = OpenAIChatCompletionsRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

### Anthropic

```python
from toyaikit.tools import Tools
from toyaikit.llm import AnthropicClient
from toyaikit.chat.runners import AnthropicMessagesRunner

tools = Tools()
tools.add_tools(my_tools_object)

# Reads ANTHROPIC_API_KEY from the environment automatically:
llm_client = AnthropicClient(model=""claude-haiku-4-5"")

runner = AnthropicMessagesRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

Notes:

- `runner.loop(prompt=...)` runs one turn programmatically and returns a result with `.last_message`, `.all_messages`, and `.tokens` — no chat interface needed. Use `runner.run()` only for the interactive Jupyter loop.
- Pass an explicit, current model id. For Anthropic, `claude-haiku-4-5` / `claude-sonnet-4-5` work; an outdated id can 404.
- For Groq you'll see a harmless `UnknownModelWarning: No pricing data...` — the call still succeeds, only cost calculation is skipped.",0224d980b3
"Is there a different toyaikit runner/client combo for Anthropic, and how do I set it up?","Yes. For Anthropic, use the **Anthropic client + Anthropic messages runner**, not the OpenAI runner.

Setup:

```python
from toyaikit.tools import Tools
from toyaikit.llm import AnthropicClient
from toyaikit.chat.runners import AnthropicMessagesRunner

tools = Tools()
tools.add_tools(my_tools_object)

llm_client = AnthropicClient(model=""claude-haiku-4-5"")
# reads ANTHROPIC_API_KEY automatically from the environment

runner = AnthropicMessagesRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)

result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

Notes:
- Use a current Anthropic model id, like `claude-haiku-4-5` or `claude-sonnet-4-5`.
- `runner.loop(prompt=...)` runs one turn programmatically.
- `runner.run()` is for the interactive Jupyter loop.","If you get a `404` (or similar) when using toyaikit with Groq or another non-OpenAI provider, it's because `OpenAIResponsesRunner` / `OpenAIClient` call OpenAI's Responses endpoint (`responses.create`), which only OpenAI implements. Switch to the **chat completions** classes, which use the standard `chat.completions.create` endpoint that Groq and other OpenAI-compatible providers support.

### OpenAI or Groq (chat completions)

```python
import os
from openai import OpenAI
from toyaikit.tools import Tools
from toyaikit.llm import OpenAIChatCompletionsClient
from toyaikit.chat.runners import OpenAIChatCompletionsRunner

tools = Tools()
tools.add_tools(my_tools_object)   # functions need type hints + an Args: docstring

# OpenAI:
llm_client = OpenAIChatCompletionsClient(model=""gpt-4o-mini"", client=OpenAI())

# Groq (same runner, just point the OpenAI client at Groq's base URL):
groq_client = OpenAI(
    api_key=os.getenv(""GROQ_API_KEY""),
    base_url=""https://api.groq.com/openai/v1"",
)
llm_client = OpenAIChatCompletionsClient(model=""llama-3.3-70b-versatile"", client=groq_client)

runner = OpenAIChatCompletionsRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

### Anthropic

```python
from toyaikit.tools import Tools
from toyaikit.llm import AnthropicClient
from toyaikit.chat.runners import AnthropicMessagesRunner

tools = Tools()
tools.add_tools(my_tools_object)

# Reads ANTHROPIC_API_KEY from the environment automatically:
llm_client = AnthropicClient(model=""claude-haiku-4-5"")

runner = AnthropicMessagesRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

Notes:

- `runner.loop(prompt=...)` runs one turn programmatically and returns a result with `.last_message`, `.all_messages`, and `.tokens` — no chat interface needed. Use `runner.run()` only for the interactive Jupyter loop.
- Pass an explicit, current model id. For Anthropic, `claude-haiku-4-5` / `claude-sonnet-4-5` work; an outdated id can 404.
- For Groq you'll see a harmless `UnknownModelWarning: No pricing data...` — the call still succeeds, only cost calculation is skipped.",0224d980b3
"Does `runner.loop(prompt=...)` work for one-off calls in toyaikit, or do I need the interactive `run()` method?","`runner.loop(prompt=...)` works for one-off programmatic calls in toyaikit. You do **not** need the interactive `run()` method unless you want the Jupyter chat-style loop.

`loop()` returns a result with:

- `.last_message`
- `.all_messages`
- `.tokens`

So for a single call, use `runner.loop(prompt=""..."")`.","If you get a `404` (or similar) when using toyaikit with Groq or another non-OpenAI provider, it's because `OpenAIResponsesRunner` / `OpenAIClient` call OpenAI's Responses endpoint (`responses.create`), which only OpenAI implements. Switch to the **chat completions** classes, which use the standard `chat.completions.create` endpoint that Groq and other OpenAI-compatible providers support.

### OpenAI or Groq (chat completions)

```python
import os
from openai import OpenAI
from toyaikit.tools import Tools
from toyaikit.llm import OpenAIChatCompletionsClient
from toyaikit.chat.runners import OpenAIChatCompletionsRunner

tools = Tools()
tools.add_tools(my_tools_object)   # functions need type hints + an Args: docstring

# OpenAI:
llm_client = OpenAIChatCompletionsClient(model=""gpt-4o-mini"", client=OpenAI())

# Groq (same runner, just point the OpenAI client at Groq's base URL):
groq_client = OpenAI(
    api_key=os.getenv(""GROQ_API_KEY""),
    base_url=""https://api.groq.com/openai/v1"",
)
llm_client = OpenAIChatCompletionsClient(model=""llama-3.3-70b-versatile"", client=groq_client)

runner = OpenAIChatCompletionsRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

### Anthropic

```python
from toyaikit.tools import Tools
from toyaikit.llm import AnthropicClient
from toyaikit.chat.runners import AnthropicMessagesRunner

tools = Tools()
tools.add_tools(my_tools_object)

# Reads ANTHROPIC_API_KEY from the environment automatically:
llm_client = AnthropicClient(model=""claude-haiku-4-5"")

runner = AnthropicMessagesRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

Notes:

- `runner.loop(prompt=...)` runs one turn programmatically and returns a result with `.last_message`, `.all_messages`, and `.tokens` — no chat interface needed. Use `runner.run()` only for the interactive Jupyter loop.
- Pass an explicit, current model id. For Anthropic, `claude-haiku-4-5` / `claude-sonnet-4-5` work; an outdated id can 404.
- For Groq you'll see a harmless `UnknownModelWarning: No pricing data...` — the call still succeeds, only cost calculation is skipped.",0224d980b3
What model names should I use for Anthropic or Groq in toyaikit so I don’t get a 404 from an old model id?,"Use the **current model IDs**, not old ones.

From the context:

- **Anthropic:** use `claude-haiku-4-5` or `claude-sonnet-4-5`
- **Groq:** use `llama-3.3-70b-versatile`

So for toyaikit, those are the model names to use to avoid a 404 from an outdated model id.","If you get a `404` (or similar) when using toyaikit with Groq or another non-OpenAI provider, it's because `OpenAIResponsesRunner` / `OpenAIClient` call OpenAI's Responses endpoint (`responses.create`), which only OpenAI implements. Switch to the **chat completions** classes, which use the standard `chat.completions.create` endpoint that Groq and other OpenAI-compatible providers support.

### OpenAI or Groq (chat completions)

```python
import os
from openai import OpenAI
from toyaikit.tools import Tools
from toyaikit.llm import OpenAIChatCompletionsClient
from toyaikit.chat.runners import OpenAIChatCompletionsRunner

tools = Tools()
tools.add_tools(my_tools_object)   # functions need type hints + an Args: docstring

# OpenAI:
llm_client = OpenAIChatCompletionsClient(model=""gpt-4o-mini"", client=OpenAI())

# Groq (same runner, just point the OpenAI client at Groq's base URL):
groq_client = OpenAI(
    api_key=os.getenv(""GROQ_API_KEY""),
    base_url=""https://api.groq.com/openai/v1"",
)
llm_client = OpenAIChatCompletionsClient(model=""llama-3.3-70b-versatile"", client=groq_client)

runner = OpenAIChatCompletionsRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

### Anthropic

```python
from toyaikit.tools import Tools
from toyaikit.llm import AnthropicClient
from toyaikit.chat.runners import AnthropicMessagesRunner

tools = Tools()
tools.add_tools(my_tools_object)

# Reads ANTHROPIC_API_KEY from the environment automatically:
llm_client = AnthropicClient(model=""claude-haiku-4-5"")

runner = AnthropicMessagesRunner(
    tools=tools,
    developer_prompt=""You are a helpful assistant."",
    llm_client=llm_client,
)
result = runner.loop(prompt=""What's the weather in Berlin?"")
print(result.last_message)
```

Notes:

- `runner.loop(prompt=...)` runs one turn programmatically and returns a result with `.last_message`, `.all_messages`, and `.tokens` — no chat interface needed. Use `runner.run()` only for the interactive Jupyter loop.
- Pass an explicit, current model id. For Anthropic, `claude-haiku-4-5` / `claude-sonnet-4-5` work; an outdated id can 404.
- For Groq you'll see a harmless `UnknownModelWarning: No pricing data...` — the call still succeeds, only cost calculation is skipped.",0224d980b3
Where do I actually find the Module 1 homework questions for this course?,"You can find the Module 1 homework questions in the course GitHub repo and on the course management platform.

For the 2026 cohort, start here:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The GitHub homework instructions often include extra context, so it’s a good idea to read those first.","Homework links are available in the course GitHub repo and in the course management platform.

For the 2026 Module 1 homework, use:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The course platform is useful for submission and deadlines, but the GitHub homework instructions often contain important extra context.",53f15299b6
Is the homework in the GitHub repo or only on the course platform?,"The homework is available in **both** places:

- the **course GitHub repo**
- the **course management platform**

For homework instructions, the GitHub repo often has extra important context, while the course platform is mainly useful for **submission and deadlines**.","Homework links are available in the course GitHub repo and in the course management platform.

For the 2026 Module 1 homework, use:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The course platform is useful for submission and deadlines, but the GitHub homework instructions often contain important extra context.",53f15299b6
Can you point me to the 2026 Module 1 homework instructions link?,"Yes — the 2026 Module 1 homework instructions link is:

[Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)","Homework links are available in the course GitHub repo and in the course management platform.

For the 2026 Module 1 homework, use:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The course platform is useful for submission and deadlines, but the GitHub homework instructions often contain important extra context.",53f15299b6
What should I check first if I can't find the homework questions anywhere?,"Check the **GitHub homework instructions first**, not just the course platform page.

For Module 1, start here:
- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

If it’s still unclear, ask in Slack and include the code or command output as text, not a screenshot.","Homework links are available in the course GitHub repo and in the course management platform.

For the 2026 Module 1 homework, use:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The course platform is useful for submission and deadlines, but the GitHub homework instructions often contain important extra context.",53f15299b6
Do the GitHub homework pages include anything useful besides the questions themselves?,"Yes. The GitHub homework instructions often include important extra context, not just the questions.","Homework links are available in the course GitHub repo and in the course management platform.

For the 2026 Module 1 homework, use:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The course platform is useful for submission and deadlines, but the GitHub homework instructions often contain important extra context.",53f15299b6
Why am I getting an empty list back when I filter my query for Homework Q3?,This is likely an indexing issue. You need to add the index settings **before** adding the data to the indices; then your filters and query should work correctly.,"This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
Could this be caused by how I indexed the data before running the filter?,"Yes — this can be caused by how the data was indexed. The index settings should be added **before** you add the data to the indices; otherwise, filtering/querying may not work correctly.","This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
Do I need to set up the index settings first before adding the documents?,"Yes. According to the course context, you should add the index settings first before adding the documents/data to the index.","This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
"My query works but filtering returns nothing, what am I missing in the index setup?","This is likely an indexing setup issue: you need to add the **index settings before adding the data to the indices**. If the settings aren’t in place first, filtering can return an empty result even though the query itself works.

","This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
Is the problem in the indexing step if my homework query keeps returning an empty list?,"Yes — based on the context, an empty list after filtering is likely caused by an indexing issue. You should add the index settings **before** adding the data to the index, then apply your filters and query.","This is likely to be an error when indexing the data. First, you need to add the index settings before adding the data to the indices, then you will be good to go applying your filters and query.",83da208a64
"Why do I get a 402 error from OpenRouter when I call responses.create(), and how do I stop it?","You’re getting a **402 from OpenRouter** because `responses.create()` is being called **without a reasonable `max_output_tokens` limit**. OpenRouter checks billing/limits against the **maximum possible output**, and if that max is too large, a free or low-limit key can be rejected **before the model runs**.

### How to stop it
Set a lower `max_output_tokens` value in your request, for example:

```python
response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

### If you’re using `rag_helper.py`
Add the same parameter inside the `llm()` method:

```python
response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

### If you’re using ToyAIKit
Pass it as an extra kwarg:

```python
llm_client = OpenAIClient(
    model=os.environ[""OPENAI_MODEL""],
    extra_kwargs={""max_output_tokens"": 1024},
)
```

### Also check
- Your `.env` points to OpenRouter:
  ```env
  OPENAI_API_KEY=sk-or-v1-...
  OPENAI_BASE_URL=https://openrouter.ai/api/v1
  OPENAI_MODEL=openai/gpt-oss-120b:free
  ```
- Your OpenRouter key has enough remaining credits/limits
- Prefer a **pinned model** like `openai/gpt-oss-120b:free` instead of `openrouter/free`

If you want, I can help you patch your exact code snippet.","OpenRouter can return APIStatusError with code 402 when responses.create() is called without a reasonable max_output_tokens limit. This happens because OpenRouter bills/limits checks against the maximum possible output (which can be very large, around 65536 tokens), so a free or low-limit key can be rejected before the model runs. This is different from a direct OpenAI endpoint (which typically returns 429 for insufficient quota).

Fix

Pass a lower limit in your responses.create() call:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI()  # uses OPENAI_API_KEY and OPENAI_BASE_URL from .env

response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework with rag_helper.py, add the same parameter in the ``llm()`` method.

```python
response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework Q6 with ToyAIKit:

```python
from toyaikit.llm import OpenAIClient

llm_client = OpenAIClient(
    model=os.environ[""OPENAI_MODEL""],
    extra_kwargs={""max_output_tokens"": 1024},
)
```

1024 is enough for homework answers; you can raise it later if needed.

If it still fails

1) Confirm your `.env` points at OpenRouter:

```env
OPENAI_API_KEY=sk-or-v1-...
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=openai/gpt-oss-120b:free
```

2) Check your OpenRouter key limit and remaining credits at https://openrouter.ai/settings/keys

3) Prefer a pinned model (for example `openai/gpt-oss-120b:free`) instead of `openrouter/free`, which can route to models with different limits.

Note: This behavior is different from OpenAI’s typical 429 handling for insufficient quota. If you still encounter issues after these steps, double-check the model and endpoint configuration to ensure the key is valid and has sufficient credits.",cfb07a27d5
Is there a specific max_output_tokens value I should set in my OpenAI client for the Module 1 homework?,"Yes — for the Module 1 homework, set:

```python
max_output_tokens=1024
```

That value is specifically recommended in the homework context and should be enough for the homework answers.","OpenRouter can return APIStatusError with code 402 when responses.create() is called without a reasonable max_output_tokens limit. This happens because OpenRouter bills/limits checks against the maximum possible output (which can be very large, around 65536 tokens), so a free or low-limit key can be rejected before the model runs. This is different from a direct OpenAI endpoint (which typically returns 429 for insufficient quota).

Fix

Pass a lower limit in your responses.create() call:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI()  # uses OPENAI_API_KEY and OPENAI_BASE_URL from .env

response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework with rag_helper.py, add the same parameter in the ``llm()`` method.

```python
response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework Q6 with ToyAIKit:

```python
from toyaikit.llm import OpenAIClient

llm_client = OpenAIClient(
    model=os.environ[""OPENAI_MODEL""],
    extra_kwargs={""max_output_tokens"": 1024},
)
```

1024 is enough for homework answers; you can raise it later if needed.

If it still fails

1) Confirm your `.env` points at OpenRouter:

```env
OPENAI_API_KEY=sk-or-v1-...
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=openai/gpt-oss-120b:free
```

2) Check your OpenRouter key limit and remaining credits at https://openrouter.ai/settings/keys

3) Prefer a pinned model (for example `openai/gpt-oss-120b:free`) instead of `openrouter/free`, which can route to models with different limits.

Note: This behavior is different from OpenAI’s typical 429 handling for insufficient quota. If you still encounter issues after these steps, double-check the model and endpoint configuration to ensure the key is valid and has sufficient credits.",cfb07a27d5
How do I fix the 402 issue in rag_helper.py when the llm() method calls responses.create()?,"Add a lower `max_output_tokens` limit in the `llm()` method where `responses.create()` is called.

Example:

```python
response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

This usually fixes the OpenRouter `402` issue because the request is otherwise checked against a very large possible output limit.","OpenRouter can return APIStatusError with code 402 when responses.create() is called without a reasonable max_output_tokens limit. This happens because OpenRouter bills/limits checks against the maximum possible output (which can be very large, around 65536 tokens), so a free or low-limit key can be rejected before the model runs. This is different from a direct OpenAI endpoint (which typically returns 429 for insufficient quota).

Fix

Pass a lower limit in your responses.create() call:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI()  # uses OPENAI_API_KEY and OPENAI_BASE_URL from .env

response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework with rag_helper.py, add the same parameter in the ``llm()`` method.

```python
response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework Q6 with ToyAIKit:

```python
from toyaikit.llm import OpenAIClient

llm_client = OpenAIClient(
    model=os.environ[""OPENAI_MODEL""],
    extra_kwargs={""max_output_tokens"": 1024},
)
```

1024 is enough for homework answers; you can raise it later if needed.

If it still fails

1) Confirm your `.env` points at OpenRouter:

```env
OPENAI_API_KEY=sk-or-v1-...
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=openai/gpt-oss-120b:free
```

2) Check your OpenRouter key limit and remaining credits at https://openrouter.ai/settings/keys

3) Prefer a pinned model (for example `openai/gpt-oss-120b:free`) instead of `openrouter/free`, which can route to models with different limits.

Note: This behavior is different from OpenAI’s typical 429 handling for insufficient quota. If you still encounter issues after these steps, double-check the model and endpoint configuration to ensure the key is valid and has sufficient credits.",cfb07a27d5
"For ToyAIKit in Q6, where do I pass max_output_tokens so OpenRouter doesn’t reject the request?","Pass it in `OpenAIClient` via `extra_kwargs`:

```python
from toyaikit.llm import OpenAIClient

llm_client = OpenAIClient(
    model=os.environ[""OPENAI_MODEL""],
    extra_kwargs={""max_output_tokens"": 1024},
)
```

This should keep OpenRouter from rejecting the request.","OpenRouter can return APIStatusError with code 402 when responses.create() is called without a reasonable max_output_tokens limit. This happens because OpenRouter bills/limits checks against the maximum possible output (which can be very large, around 65536 tokens), so a free or low-limit key can be rejected before the model runs. This is different from a direct OpenAI endpoint (which typically returns 429 for insufficient quota).

Fix

Pass a lower limit in your responses.create() call:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI()  # uses OPENAI_API_KEY and OPENAI_BASE_URL from .env

response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework with rag_helper.py, add the same parameter in the ``llm()`` method.

```python
response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework Q6 with ToyAIKit:

```python
from toyaikit.llm import OpenAIClient

llm_client = OpenAIClient(
    model=os.environ[""OPENAI_MODEL""],
    extra_kwargs={""max_output_tokens"": 1024},
)
```

1024 is enough for homework answers; you can raise it later if needed.

If it still fails

1) Confirm your `.env` points at OpenRouter:

```env
OPENAI_API_KEY=sk-or-v1-...
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=openai/gpt-oss-120b:free
```

2) Check your OpenRouter key limit and remaining credits at https://openrouter.ai/settings/keys

3) Prefer a pinned model (for example `openai/gpt-oss-120b:free`) instead of `openrouter/free`, which can route to models with different limits.

Note: This behavior is different from OpenAI’s typical 429 handling for insufficient quota. If you still encounter issues after these steps, double-check the model and endpoint configuration to ensure the key is valid and has sufficient credits.",cfb07a27d5
"If I’m still seeing this error, how can I check whether my OpenRouter key, base URL, or model name is the problem?","Yes — to check whether the problem is your OpenRouter key, base URL, or model name, verify these settings in your `.env`:

```env
OPENAI_API_KEY=sk-or-v1-...
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=openai/gpt-oss-120b:free
```

Then also:

1. Check your OpenRouter key limit and remaining credits at  
   https://openrouter.ai/settings/keys

2. Prefer a pinned model like `openai/gpt-oss-120b:free` instead of `openrouter/free`, since `openrouter/free` can route to models with different limits.

If it still fails after that, double-check that the key is valid and has sufficient credits.","OpenRouter can return APIStatusError with code 402 when responses.create() is called without a reasonable max_output_tokens limit. This happens because OpenRouter bills/limits checks against the maximum possible output (which can be very large, around 65536 tokens), so a free or low-limit key can be rejected before the model runs. This is different from a direct OpenAI endpoint (which typically returns 429 for insufficient quota).

Fix

Pass a lower limit in your responses.create() call:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()

client = OpenAI()  # uses OPENAI_API_KEY and OPENAI_BASE_URL from .env

response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework with rag_helper.py, add the same parameter in the ``llm()`` method.

```python
response = client.responses.create(
    model=os.environ[""OPENAI_MODEL""],
    input=message_history,
    max_output_tokens=1024,
)
```

For Module 1 homework Q6 with ToyAIKit:

```python
from toyaikit.llm import OpenAIClient

llm_client = OpenAIClient(
    model=os.environ[""OPENAI_MODEL""],
    extra_kwargs={""max_output_tokens"": 1024},
)
```

1024 is enough for homework answers; you can raise it later if needed.

If it still fails

1) Confirm your `.env` points at OpenRouter:

```env
OPENAI_API_KEY=sk-or-v1-...
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=openai/gpt-oss-120b:free
```

2) Check your OpenRouter key limit and remaining credits at https://openrouter.ai/settings/keys

3) Prefer a pinned model (for example `openai/gpt-oss-120b:free`) instead of `openrouter/free`, which can route to models with different limits.

Note: This behavior is different from OpenAI’s typical 429 handling for insufficient quota. If you still encounter issues after these steps, double-check the model and endpoint configuration to ensure the key is valid and has sufficient credits.",cfb07a27d5
"In the Module 1 homework, what does it actually mean to point RAG at the chunk index instead of the original docs?","It means you should build and query the RAG index on the **chunked documents**, not on the full original documents.

So instead of indexing `documents`, you index `chunks`:

```python
index.fit(chunks)
```

not:

```python
index.fit(documents)
```

In practice, RAG will then retrieve the most relevant **chunks** and include only those chunks in the prompt, which reduces the amount of context sent to the model.","It means you should build and search the index using the chunked documents, not the original full documents.

For example, if your original records are in `documents` and your split records are in `chunks`, fit the search index on `chunks`:

```python
index.fit(chunks)
```

not:

```python
index.fit(documents)
```

Then the RAG pipeline retrieves relevant chunks and puts only those chunks into the prompt. This is what reduces the amount of context sent to the model.",b4300adeb1
Should I build the search index from the split chunks or from the full documents in the homework?,"Build the index from the split chunks, not the full documents.

In the homework, you should fit/search the index on `chunks`, for example:

```python
index.fit(chunks)
```

not on `documents`.","It means you should build and search the index using the chunked documents, not the original full documents.

For example, if your original records are in `documents` and your split records are in `chunks`, fit the search index on `chunks`:

```python
index.fit(chunks)
```

not:

```python
index.fit(documents)
```

Then the RAG pipeline retrieves relevant chunks and puts only those chunks into the prompt. This is what reduces the amount of context sent to the model.",b4300adeb1
"If I already have documents and chunks, which one should I pass to fit() for the RAG index?","If you already have `documents` and `chunks`, pass the **chunks** to `fit()` for the RAG index:

```python
index.fit(chunks)
```

Not `documents`. The index should be built on the chunked documents so retrieval returns relevant chunks, not whole documents.","It means you should build and search the index using the chunked documents, not the original full documents.

For example, if your original records are in `documents` and your split records are in `chunks`, fit the search index on `chunks`:

```python
index.fit(chunks)
```

not:

```python
index.fit(documents)
```

Then the RAG pipeline retrieves relevant chunks and puts only those chunks into the prompt. This is what reduces the amount of context sent to the model.",b4300adeb1
Why does the homework say to use the chunked records for retrieval rather than the original records?,"Because the homework wants the RAG pipeline to retrieve only the most relevant **chunks**, not whole documents. If you index the original records, the retriever may return large full documents and send too much context to the model. By indexing the chunked records, the search is done over smaller pieces, which keeps the prompt shorter and more focused.

In other words: **build and search the index on `chunks`, not `documents`.**","It means you should build and search the index using the chunked documents, not the original full documents.

For example, if your original records are in `documents` and your split records are in `chunks`, fit the search index on `chunks`:

```python
index.fit(chunks)
```

not:

```python
index.fit(documents)
```

Then the RAG pipeline retrieves relevant chunks and puts only those chunks into the prompt. This is what reduces the amount of context sent to the model.",b4300adeb1
"When the RAG pipeline runs, does it send whole documents to the prompt or only the retrieved chunks?","Only the retrieved chunks are sent into the prompt, not the whole documents.","It means you should build and search the index using the chunked documents, not the original full documents.

For example, if your original records are in `documents` and your split records are in `chunks`, fit the search index on `chunks`:

```python
index.fit(chunks)
```

not:

```python
index.fit(documents)
```

Then the RAG pipeline retrieves relevant chunks and puts only those chunks into the prompt. This is what reduces the amount of context sent to the model.",b4300adeb1
"For the homework questions, do I need to get the exact same number as the multiple-choice options, or is the nearest option okay?","Choose the closest option if your numeric answer is near one of them. Small differences can happen, so an exact match isn’t always required. If your answer is far from all options, re-check the question and instructions.","Not always. If your numeric answer is close to one of the options, choose the closest option.

Small differences can come from:

- Slightly different filtering.
- Different dataset versions.
- Floating-point or rounding differences.
- Different model/provider behavior.

If your answer is far from every option, re-check the question, the dataset version, and the GitHub homework instructions.

See the general homework guidance: [homework logistics](https://datatalks.club/docs/courses/zoomcamp-logistics/homework/).",5e4ca64cb5
"If my numeric result is a little different from the listed answers, should I still pick the closest one?","Yes — if your numeric result is slightly different, you should pick the closest option. Small differences can happen because of filtering, dataset versions, rounding, or model/provider differences.","Not always. If your numeric answer is close to one of the options, choose the closest option.

Small differences can come from:

- Slightly different filtering.
- Different dataset versions.
- Floating-point or rounding differences.
- Different model/provider behavior.

If your answer is far from every option, re-check the question, the dataset version, and the GitHub homework instructions.

See the general homework guidance: [homework logistics](https://datatalks.club/docs/courses/zoomcamp-logistics/homework/).",5e4ca64cb5
What usually causes small mismatches between my homework answer and the available options?,"Small mismatches usually come from:

- Slightly different filtering
- Different dataset versions
- Floating-point or rounding differences
- Different model/provider behavior

If your answer is close to one of the options, choose the closest option. If it’s far from all of them, re-check the question, dataset version, and GitHub homework instructions.","Not always. If your numeric answer is close to one of the options, choose the closest option.

Small differences can come from:

- Slightly different filtering.
- Different dataset versions.
- Floating-point or rounding differences.
- Different model/provider behavior.

If your answer is far from every option, re-check the question, the dataset version, and the GitHub homework instructions.

See the general homework guidance: [homework logistics](https://datatalks.club/docs/courses/zoomcamp-logistics/homework/).",5e4ca64cb5
When should I worry that my homework result is wrong instead of just a rounding or dataset difference?,"Worry if your result is **far from every option** after you’ve checked the likely causes.

Common harmless causes of small differences are:

- rounding or floating-point noise
- slightly different filtering
- different dataset versions
- different model/provider behavior

For homework, if your numeric answer is **close to one of the options**, choose the **closest option**.

But if it’s **still far off**, then re-check:

- the question carefully
- the dataset version
- the GitHub homework instructions
- whether you used the correct model/workflow

For example, in Module 2 cosine similarity, using the wrong embedding model is a common reason for a result that doesn’t match the options.","Not always. If your numeric answer is close to one of the options, choose the closest option.

Small differences can come from:

- Slightly different filtering.
- Different dataset versions.
- Floating-point or rounding differences.
- Different model/provider behavior.

If your answer is far from every option, re-check the question, the dataset version, and the GitHub homework instructions.

See the general homework guidance: [homework logistics](https://datatalks.club/docs/courses/zoomcamp-logistics/homework/).",5e4ca64cb5
Is there a guide for homework rules and how to choose an option when my answer is close but not exact?,"Yes. The homework guidance says that if your numeric answer is close to one of the options, you should choose the closest option. It also notes that small differences can come from filtering, dataset versions, floating-point/rounding, or provider behavior.

The general homework guidance is here:
https://datatalks.club/docs/courses/zoomcamp-logistics/homework/

If your answer is far from all options, re-check the question, dataset version, and GitHub homework instructions.","Not always. If your numeric answer is close to one of the options, choose the closest option.

Small differences can come from:

- Slightly different filtering.
- Different dataset versions.
- Floating-point or rounding differences.
- Different model/provider behavior.

If your answer is far from every option, re-check the question, the dataset version, and the GitHub homework instructions.

See the general homework guidance: [homework logistics](https://datatalks.club/docs/courses/zoomcamp-logistics/homework/).",5e4ca64cb5
Where do I check the Module 1 homework details if the platform instructions are confusing?,"Check the **GitHub homework instructions first** — they often include important extra context that the platform page may not show.

For Module 1, start here:
- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The course platform is mainly for **submission and deadlines**. If it’s still unclear after reading the GitHub instructions, ask in Slack and include the code or command output as text.","First read the GitHub homework instructions, not only the course platform page.

For Module 1 in the 2026 cohort, start here:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The homework follows the lesson workflow, but usually uses a different dataset or asks you to apply the same idea in a slightly different way.

If it is still unclear, ask in Slack and include the code or command output as text, not a screenshot. Follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).",7d3a2e3505
"Is the course page enough for the homework, or should I also open the GitHub repo?","You should also open the GitHub repo.

The course platform is useful for submission and deadlines, but the homework instructions on GitHub often contain important extra context.","First read the GitHub homework instructions, not only the course platform page.

For Module 1 in the 2026 cohort, start here:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The homework follows the lesson workflow, but usually uses a different dataset or asks you to apply the same idea in a slightly different way.

If it is still unclear, ask in Slack and include the code or command output as text, not a screenshot. Follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).",7d3a2e3505
"If the homework task looks different from the lesson, is that normal or did I miss something?","Yes, that’s normal. The homework usually follows the lesson workflow, but it often uses a different dataset or asks you to apply the same idea in a slightly different way.

If it still seems unclear, first check the GitHub homework instructions, since they may be different from the course platform page.","First read the GitHub homework instructions, not only the course platform page.

For Module 1 in the 2026 cohort, start here:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The homework follows the lesson workflow, but usually uses a different dataset or asks you to apply the same idea in a slightly different way.

If it is still unclear, ask in Slack and include the code or command output as text, not a screenshot. Follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).",7d3a2e3505
What should I do when I still don't understand a homework question after reading the instructions?,"If it’s still unclear after reading the instructions, ask in Slack and include the code or command output as text, not a screenshot.","First read the GitHub homework instructions, not only the course platform page.

For Module 1 in the 2026 cohort, start here:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The homework follows the lesson workflow, but usually uses a different dataset or asks you to apply the same idea in a slightly different way.

If it is still unclear, ask in Slack and include the code or command output as text, not a screenshot. Follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).",7d3a2e3505
"When I ask for help in Slack, is it better to paste the command output or just upload a screenshot?","It’s better to paste the command output as text, not upload a screenshot.","First read the GitHub homework instructions, not only the course platform page.

For Module 1 in the 2026 cohort, start here:

- [Module 1 cohort materials](https://github.com/DataTalksClub/llm-zoomcamp/tree/main/cohorts/2026/01-agentic-rag)
- [Module 1 homework instructions](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/cohorts/2026/01-agentic-rag/homework.md)

The homework follows the lesson workflow, but usually uses a different dataset or asks you to apply the same idea in a slightly different way.

If it is still unclear, ask in Slack and include the code or command output as text, not a screenshot. Follow the [asking questions guidelines](https://datatalks.club/docs/courses/zoomcamp-logistics/asking-questions/).",7d3a2e3505
How should I count tokens for the Module 1 homework if I'm using a different LLM provider instead of OpenAI?,"For Module 1 homework, use the **token count from the model’s response object**.

If you use a different provider, check its **usage fields** in the response and adapt the code accordingly. For example, OpenAI-compatible APIs may expose something like:

- `response.usage.input_tokens`
- `response.usage.prompt_tokens`

Do **not** use `tiktoken` or `cl100k_base` as a generic tokenizer for other providers like Gemini, Mistral, Hugging Face, or Groq, because tokenization differs.

If your provider doesn’t return usage info, use that provider’s **native tokenizer** as an approximation.","For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
Where do I get the token count from when the model response includes usage info?,"Get it from the model’s **response object**, specifically the provider’s usage fields.

For OpenAI-compatible clients, that may be something like:

- `response.usage.input_tokens`
- `response.usage.prompt_tokens`

Use whichever field your provider returns. If you’re using a non-OpenAI provider, check its response object and adapt to its usage field.","For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
"Can I just use tiktoken or cl100k_base for Gemini, Mistral, Groq, or Hugging Face models in this homework?","No — for this homework you should **not** use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Groq, Hugging Face, or other non-OpenAI providers, because tokenization differs by model.

Instead, use the **token count from the model’s response object** when available. If the provider doesn’t expose usage fields, use that provider’s **native tokenizer** as an approximation.","For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
What should I do if my provider doesn't give token usage in the response object?,"If your provider doesn’t expose token usage, use that provider’s native tokenizer as an approximation.","For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
"For the multiple-choice questions, do I need the exact token count or just the closest answer option?","For multiple-choice questions, you should choose the **closest answer option**, not an exact token count.

The homework guidance says that small differences are expected, and if your numeric answer is close to one of the options, pick the closest one.","For the current Module 1 homework, get the token count from the model response object.

For example, OpenAI-compatible clients usually return usage information on the response, such as `response.usage.input_tokens` or `response.usage.prompt_tokens`, depending on the API style.

If you use a non-OpenAI provider, check the provider's response object for its usage fields and adapt the code. Do not use `tiktoken` or `cl100k_base` as a generic tokenizer for Gemini, Mistral, Hugging Face, Groq, or other providers because tokenization differs by model.

If your provider does not expose token usage, use that provider's native tokenizer as an approximation. For multiple-choice homework questions, choose the closest option.",fe8fed31e6
"Why does the homework checker give a 500 or other non-200 error when I submit my repo link, and what should I change so it can access it?","The checker sends a GET request to your submitted URL and expects an HTTP `200` response. If it gets `500` or another non-200 error, the link is probably not publicly reachable.

What to change:
- Make the repo **public**
- Submit the **plain repo URL**, not the `.git` URL  
  - Good: `https://github.com/you/repo`
  - Bad: `https://github.com/you/repo.git`
- Double-check for a **typo** in the URL

Quick test: open the link in an **incognito/private window**. If it loads there, the checker should be able to access it too.","The submission checker fetches the URL you submit with a GET request and expects an HTTP `200` response. A non-200 status (404, 500, etc.) almost always means the link isn't publicly reachable as-is. Check the usual causes:

- **Private repo** — make the repository public so the checker can access it.
- **Trailing `.git`** — submit the plain repository URL (e.g. `https://github.com/you/repo`), not `https://github.com/you/repo.git`.
- **Typo in the URL** — paste the exact link.

A quick test: open the URL in a private/incognito browser window. If it loads for you there, it'll work for the checker too.",bcdb8740ce
"Do I need to make my GitHub repository public for the submission to work, or is a private repo okay?","Yes — for the submission checker to access it, your GitHub repository needs to be **public**. A private repo is not enough.

Also, make sure you submit the plain repo URL, not one ending in `.git`.","The submission checker fetches the URL you submit with a GET request and expects an HTTP `200` response. A non-200 status (404, 500, etc.) almost always means the link isn't publicly reachable as-is. Check the usual causes:

- **Private repo** — make the repository public so the checker can access it.
- **Trailing `.git`** — submit the plain repository URL (e.g. `https://github.com/you/repo`), not `https://github.com/you/repo.git`.
- **Typo in the URL** — paste the exact link.

A quick test: open the URL in a private/incognito browser window. If it loads for you there, it'll work for the checker too.",bcdb8740ce
I submitted a link that ends with .git — could that be the reason the checker rejects it?,"Yes — submit the plain repository URL, not the one ending in `.git`.

For example:
- `https://github.com/you/repo`
- not `https://github.com/you/repo.git`

The checker does a GET request and expects a `200` response, so the `.git` version can cause it to reject the link.","The submission checker fetches the URL you submit with a GET request and expects an HTTP `200` response. A non-200 status (404, 500, etc.) almost always means the link isn't publicly reachable as-is. Check the usual causes:

- **Private repo** — make the repository public so the checker can access it.
- **Trailing `.git`** — submit the plain repository URL (e.g. `https://github.com/you/repo`), not `https://github.com/you/repo.git`.
- **Typo in the URL** — paste the exact link.

A quick test: open the URL in a private/incognito browser window. If it loads for you there, it'll work for the checker too.",bcdb8740ce
What’s the best way to test whether my repo URL is actually reachable before resubmitting the homework?,"Open the repo URL in a private/incognito browser window. If it loads there, it should be reachable by the checker too.","The submission checker fetches the URL you submit with a GET request and expects an HTTP `200` response. A non-200 status (404, 500, etc.) almost always means the link isn't publicly reachable as-is. Check the usual causes:

- **Private repo** — make the repository public so the checker can access it.
- **Trailing `.git`** — submit the plain repository URL (e.g. `https://github.com/you/repo`), not `https://github.com/you/repo.git`.
- **Typo in the URL** — paste the exact link.

A quick test: open the URL in a private/incognito browser window. If it loads for you there, it'll work for the checker too.",bcdb8740ce
"If my URL looks correct but the homework still fails with a non-200 response, what common mistakes should I double-check?","Double-check these common mistakes:

- **Private repo** — make sure the repository is public.
- **Trailing `.git`** — use the plain repo URL, not `.../repo.git`.
- **URL typo** — paste the exact link and verify it carefully.

A quick test is to open the URL in a private/incognito window. If it loads there, the checker should be able to access it too.","The submission checker fetches the URL you submit with a GET request and expects an HTTP `200` response. A non-200 status (404, 500, etc.) almost always means the link isn't publicly reachable as-is. Check the usual causes:

- **Private repo** — make the repository public so the checker can access it.
- **Trailing `.git`** — submit the plain repository URL (e.g. `https://github.com/you/repo`), not `https://github.com/you/repo.git`.
- **Typo in the URL** — paste the exact link.

A quick test: open the URL in a private/incognito browser window. If it loads for you there, it'll work for the checker too.",bcdb8740ce
"What exactly are embeddings in vector search, and why do they matter?","Embeddings are numerical vector representations of text or other non-numerical data that preserve meaning and context.

Why they matter in vector search:
- Similar items get similar vectors.
- That makes it possible to measure semantic similarity mathematically.
- You can then search by meaning, not just exact keywords.

In practice, vector search compares these embeddings using things like dot product or cosine similarity to find the most relevant documents.","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
How do embeddings turn text or other non-numeric data into numbers without losing meaning?,"Embeddings convert text or other non-numeric data into numbers by mapping it into a numerical vector space while preserving meaning and context. Similar inputs should produce similar vectors, so their closeness can be used to measure semantic similarity.","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
Why do similar inputs end up with similar embedding values?,"Because embeddings are designed so that **similar inputs are mapped to nearby points in vector space** while preserving meaning and context. That way, mathematical similarity measures can be used to find semantically similar items.","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
How does the closeness of embedding vectors help with semantic similarity search?,"Embedding vectors that are close together represent texts with similar meaning. In semantic similarity search, this means a query vector will be near the vectors of documents with related content, so you can find relevant results by looking for the nearest vectors instead of matching exact words.

","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
"What’s the connection between embeddings, vector space models, and dimensionality reduction?","Embeddings are a way to convert text or other non-numerical data into numbers while preserving meaning and context. The idea is that similar inputs get similar vectors.

A **vector space model** is the framework that represents data as points in a vector space, where proximity between vectors reflects semantic similarity.

**Dimensionality reduction** is related because embeddings turn high-dimensional, complex data into lower-dimensional numeric vectors that are easier to compare mathematically.

So the connection is:

- **Embeddings** produce the vectors
- **Vector space models** are the representation/search approach
- **Dimensionality reduction** is part of making the data compact enough to work well in that vector space

","Embeddings refer to the process of converting non-numerical data into numerical data while preserving meaning and context. When similar non-numerical data is input into an embedding algorithm, it should yield similar numerical data. The proximity of these numerical values allows for the use of mathematical semantic similarity algorithms. Related concepts include the ""vector space model"" and ""dimensionality reduction.""",69430a79a8
"How can I get rid of the warning about ""multi-qa-mpnet-base-dot-v1"" and sentence-transformers version?","To get rid of that warning, upgrade `sentence-transformers` to version 3.0.0 or higher:

```bash
pip install sentence-transformers>=3.0.0
```

If you still see it after upgrading, make sure your environment is using the updated package.","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
What should I install to suppress the model warning from sentence-transformers?,"Install:

```bash
pip install sentence-transformers>=3.0.0
```

This upgrades `sentence-transformers` to v3.0.0 or higher and suppresses the warning.","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
"Is there a way to stop the ""was made on sentence transformers v3.0.0"" warning?","Yes — upgrade `sentence-transformers` to `3.0.0` or higher to suppress that warning:

```bash
pip install sentence-transformers>=3.0.0
```

","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
Do I need to upgrade sentence-transformers to fix this warning message?,"Yes. To suppress that warning, upgrade `sentence-transformers` to version 3.0.0 or higher:

```bash
pip install sentence-transformers>=3.0.0
```","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
What command should I run so the multi-qa-mpnet-base-dot-v1 warning disappears?,"Run:

```bash
pip install sentence-transformers>=3.0.0
```","To suppress the warning, upgrade `sentence-transformers` to version 3.0.0 or higher. You can do this by running the following command:

```bash
pip install sentence-transformers>=3.0.0
```",aa3f597424
"Why did the lesson use dot product directly for cosine similarity, but the homework says to normalize first?","In the lesson, `.dot(...)` was used directly because the embeddings were already normalized to unit length, so dot product = cosine similarity.

In the homework, the vectors from TF-IDF + SVD are not normalized by default, so you need to normalize them first if you want dot product to match cosine similarity.","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
"Are the embeddings from `model.encode(...)` already unit vectors, so `.dot(...)` works as cosine similarity?","Yes — in the lesson, `model.encode(...)` was assumed to return **normalized unit-length embeddings**, so `.dot(...)` is equivalent to **cosine similarity**.

That said, this depends on the model:
- For the lesson’s embedding model, the vectors were treated as already normalized.
- In the homework, the `Embedder` also returns **normalized vectors**, so dot product works directly there too.
- For other embedding methods like TF-IDF + SVD, you usually need to normalize manually first.

If you want, I can also explain how to check whether a vector is unit length.","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
"With TF-IDF plus SVD embeddings in the homework, why can't I just use dot product without normalization?","Because TF-IDF + SVD vectors are **not normalized by default**.

- For **unit-length embeddings**, `dot(a, b)` equals cosine similarity.
- For **TF-IDF + SVD**, the dot product also includes the vectors’ magnitudes, so it is **not** cosine similarity unless you normalize first.

So in the homework, you should normalize the vectors before using dot product if you want cosine similarity.","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
What is the difference between using dot product and cosine similarity when vectors are not normalized?,"If vectors are **not normalized**, the **dot product** is not the same as **cosine similarity**.

- **Dot product** depends on both:
  - the **direction** of the vectors
  - their **magnitude/length**
- **Cosine similarity** depends only on the **angle/direction** between them

So with unnormalized vectors, a vector with a larger magnitude can get a higher dot product even if it is not more semantically similar.

If you want cosine similarity with non-normalized vectors, you must **normalize them first**.","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
"In vector search, when is it okay to skip normalization and rely on `.dot(...)` alone?","It’s okay to skip normalization and use `.dot(...)` alone when the embeddings are already unit-normalized.

In that case, the dot product is equivalent to cosine similarity, so normalization is unnecessary.

If the embeddings are not normalized, then `.dot(...)` by itself is not cosine similarity, and you should normalize first.","In the lesson, `.dot(...)` was used under the assumption that the embeddings returned by the model (e.g., `model.encode(...)` from OpenAI) are already normalized to have unit length. In that case, the dot product is mathematically equivalent to cosine similarity.

In the homework, however, we use classic embeddings like TF-IDF + SVD, which are not normalized by default. This means that the dot product does not represent cosine similarity unless we manually normalize the vectors.",92cd22cada
"In vector search for this project, should I index just the answer text, just the question, or both together?","It’s an experiment: **try all three and measure which works best on your dataset**.

The course suggests these options:
- **answer text only**
- **question only**
- **question + answer text together** — often the best, but a bit longer and slightly more expensive

Choose the one with the best **hit rate / MRR** on your ground-truth set.","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
What is the difference between embedding only the answer versus embedding only the question in the vector database?,"The difference is in what text you turn into vectors for retrieval:

- **Embed only the answer**: the vector database stores the semantic meaning of the answer text. This works because the model can match a user’s question to a semantically similar answer.
- **Embed only the question**: the vector database stores the semantic meaning of the original questions, so retrieval works well when user queries are similar to those questions.
- **Embed both question + answer**: combines both signals and is often the best option, but it uses longer input and costs a bit more.

So the main difference is **which side of the Q&A pair the embedding is optimized for**, and the best choice depends on which gives the best hit rate / MRR on your dataset.","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
"Does combining the question and answer into one embedding usually work better, or is it just more expensive?","Usually **yes, combining `question + answer` into one embedding often works best** — but it’s **not guaranteed**, and the course says it should be treated as an **experiment on your dataset**.

What the context says:

- **Answer only** can work well.
- **Question only** can work well.
- **Question + text** is **often the best**.
- It does make the input longer, so it’s **slightly more expensive**.

So the short answer is: **it often performs better, not just more expensive, but you should verify it using hit rate / MRR on your ground-truth set.**","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
"How do I decide which embedding setup gives the best results for my dataset: question, text, or question + text?","There’s no single best choice. The course recommends treating it as an experiment on your dataset:

- **Embed `text` only**
- **Embed `question` only**
- **Embed `question + "" "" + text`**

Then evaluate them on a **ground-truth set** and compare metrics like **hit rate** or **MRR**.  
In practice, **`question + text` is often the best**, but it’s a bit longer and slightly more expensive.

So the best way to decide is: **try all three and pick the one with the best retrieval score on your data**.","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
Should I test all three embedding options and choose the one with the highest hit rate or MRR?,"Yes — for your ground-truth set, test all three embedding options and choose the one with the best hit rate or MRR. The course specifically recommends comparing options and picking the one that performs best on your evaluation set.","There's no single right answer — it's an experiment to run on your dataset. The course shows three options:

- Embed the answer (`text`) only — works because the model captures semantic similarity between questions and their answers.
- Embed the question only — works because user queries look like the indexed questions.
- Embed `question + "" "" + text` — often the best, but produces longer input and slightly more cost.

Pick whichever gives the best hit rate / MRR on your ground-truth set. The course materials include a side-by-side comparison.",5c4a8d2e60
What exactly does cosine similarity measure when comparing two vectors?,"Cosine similarity measures the **angle between two vectors**, not their length.

- If the vectors point in the same direction, similarity is **1**
- If they are orthogonal, similarity is **0**
- If they point in opposite directions, similarity is **-1**

So it tells you **how aligned two vectors are**, regardless of magnitude.","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
How is cosine similarity used to tell whether two documents are related in text analysis?,"Cosine similarity tells you how closely two document vectors point in the same direction.

In text analysis, each document is turned into a vector of word counts, TF-IDF values, or embeddings. Cosine similarity then measures the angle between those vectors:

- **1** means the documents are very similar
- **0** means they share no similarity
- **-1** means they are opposites

So, if two documents have a **high cosine similarity**, they are considered **related in content**.","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
"What does the cosine similarity score mean if it is 1, 0, or -1?","A cosine similarity score of:

- **1** means the vectors are **identical**
- **0** means the vectors are **orthogonal** — **no similarity**
- **-1** means the vectors are **completely opposite**","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
Why do people use cosine similarity with word counts or TF-IDF vectors?,"People use cosine similarity with word counts or TF-IDF vectors because it measures how similar two documents are based on the **angle** between their vectors, not their length.

That is useful in text analysis because:

- documents can be different lengths
- TF-IDF and word-count vectors represent content as directions in vector space
- cosine similarity tells you whether the documents use similar terms, even if one is much longer than the other

With normalized vectors, cosine similarity is equivalent to the dot product.","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
How does the angle between vectors connect to similarity in cosine similarity?,"Cosine similarity uses the **cosine of the angle** between two vectors as its similarity score:

- **Small angle** → vectors point in a similar direction → **high similarity**
- **Angle of 90°** → vectors are orthogonal → **no similarity** (`0`)
- **Angle of 180°** → vectors point in opposite directions → **-1**

So, the closer the angle is to **0°**, the more similar the vectors are.","Cosine similarity is a measure used to calculate the similarity between two non-zero vectors, often used in text analysis to determine how similar two documents are based on their content. This metric computes the cosine of the angle between two vectors, which are typically word counts or TF-IDF values of the documents. The cosine similarity value ranges from -1 to 1, where 1 indicates that the vectors are identical, 0 indicates that the vectors are orthogonal (no similarity), and -1 represents completely opposite vectors.",db78580409
Why does cosine similarity turn into a dot product after normalizing embeddings and the query vector?,"Cosine similarity becomes a dot product after normalization because for unit-length vectors, the magnitudes are all 1, so the cosine formula simplifies to just the dot product.

In other words:

- Cosine similarity = compare the angle between two vectors
- If both vectors are normalized, their lengths don’t affect the result
- So the similarity is just the dot product of the normalized vectors

That’s why, once embeddings and the query are normalized, you can compute cosine similarity efficiently with matrix multiplication.","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
How can we use matrix multiplication to compute cosine similarity between one query and many document embeddings?,"If the embeddings are normalized to unit length, cosine similarity between one query and many documents is just the dot product of the query vector with each document vector.

So you can stack all document embeddings into a matrix `D` and compute:

```python
similarities = D @ q
```

where:

- `D` is a matrix of shape `(num_documents, embedding_dim)`
- `q` is the query vector of shape `(embedding_dim,)`

This gives one similarity score per document, efficiently using matrix multiplication.

If the vectors are not normalized, you must normalize them first, because plain dot product is not cosine similarity.","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
"If vectors are unit length, why do their magnitudes stop affecting cosine similarity?","If vectors are unit length, their magnitudes are all 1, so they no longer contribute to the cosine similarity.

Cosine similarity measures the angle between vectors. Normally, magnitudes matter in the dot product, but when both vectors are normalized to unit length, the formula simplifies: the cosine similarity becomes just the dot product. So the magnitudes stop affecting it because they’ve all been fixed to the same value.","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
What makes cosine similarity efficient for vector search when embeddings are normalized?,"When embeddings are normalized to unit length, cosine similarity becomes equivalent to the dot product. That means you can compute it efficiently with a single matrix multiplication between the query vector and all embeddings, instead of doing more expensive per-vector calculations.","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
Why is comparing a query to a whole embedding matrix just a matrix multiply instead of separate calculations?,"Because if the query and all embeddings are already normalized, the cosine similarity between the query and each vector is just a dot product. Stacking all embeddings into a matrix lets you compute all those dot products at once as one matrix multiply, instead of doing separate pairwise calculations in a loop.","Cosine similarity measures how aligned two vectors are, regardless of their magnitude. When all vectors (including the query) are normalized to unit length, their magnitudes no longer matter. In this case, cosine similarity is equivalent to simply taking the dot product between the query and each document embedding. This allows us to compute similarities efficiently using matrix multiplication.",95feb4e75b
"For Module 2, do I have to make a brand new GitHub repo, or can I reuse the same one and just start another codespace?","You can reuse the same GitHub repo. For Module 2, you just need to create a **new codespace** from that existing repository — no new repo required.","Just a new codespace. A codespace is an environment (see *Can I run the course locally instead of Codespaces?*); you create it from your existing repository, so you don't need a new GitHub repo.

Use a separate codespace for Module 2 because the vector-search dependencies are fairly heavy. Keeping them isolated means you can simply stop or delete that codespace when you're done, rather than leaving the extra weight in your Module 1 environment. Setup is quick.

Since you may delete this codespace later, commit your work to your repository (see *What happens to code saved in Codespaces if I do not commit it?*).",1e829a8c6f
Why does the course recommend opening a separate codespace for the vector search module instead of keeping everything in the same environment?,"The course recommends a separate codespace because the vector-search dependencies are fairly heavy. Keeping them isolated lets you stop or delete that codespace when you’re done, instead of leaving the extra setup in your Module 1 environment. It’s also quick to create from your existing repository.","Just a new codespace. A codespace is an environment (see *Can I run the course locally instead of Codespaces?*); you create it from your existing repository, so you don't need a new GitHub repo.

Use a separate codespace for Module 2 because the vector-search dependencies are fairly heavy. Keeping them isolated means you can simply stop or delete that codespace when you're done, rather than leaving the extra weight in your Module 1 environment. Setup is quick.

Since you may delete this codespace later, commit your work to your repository (see *What happens to code saved in Codespaces if I do not commit it?*).",1e829a8c6f
"If I create a new codespace for Module 2, does it get created from my existing repository or from a fresh repository?","It gets created from your existing repository, not from a fresh repository.","Just a new codespace. A codespace is an environment (see *Can I run the course locally instead of Codespaces?*); you create it from your existing repository, so you don't need a new GitHub repo.

Use a separate codespace for Module 2 because the vector-search dependencies are fairly heavy. Keeping them isolated means you can simply stop or delete that codespace when you're done, rather than leaving the extra weight in your Module 1 environment. Setup is quick.

Since you may delete this codespace later, commit your work to your repository (see *What happens to code saved in Codespaces if I do not commit it?*).",1e829a8c6f
What should I do with my Module 2 work if I might delete the codespace later — is committing it enough?,"Yes — committing it is enough.

Since you may delete the Module 2 codespace later, you should **commit your work to your repository** so it’s محفوظ in GitHub. Uncommitted changes can be lost if the codespace is deleted or recreated.","Just a new codespace. A codespace is an environment (see *Can I run the course locally instead of Codespaces?*); you create it from your existing repository, so you don't need a new GitHub repo.

Use a separate codespace for Module 2 because the vector-search dependencies are fairly heavy. Keeping them isolated means you can simply stop or delete that codespace when you're done, rather than leaving the extra weight in your Module 1 environment. Setup is quick.

Since you may delete this codespace later, commit your work to your repository (see *What happens to code saved in Codespaces if I do not commit it?*).",1e829a8c6f
"Is there any reason to keep the Module 2 dependencies inside my Module 1 codespace, or is it better to isolate them?","It’s better to isolate them in a separate Module 2 codespace.

The context says Module 2’s vector-search dependencies are fairly heavy, so keeping them separate lets you stop or delete that codespace later without cluttering your Module 1 environment.","Just a new codespace. A codespace is an environment (see *Can I run the course locally instead of Codespaces?*); you create it from your existing repository, so you don't need a new GitHub repo.

Use a separate codespace for Module 2 because the vector-search dependencies are fairly heavy. Keeping them isolated means you can simply stop or delete that codespace when you're done, rather than leaving the extra weight in your Module 1 environment. Setup is quick.

Since you may delete this codespace later, commit your work to your repository (see *What happens to code saved in Codespaces if I do not commit it?*).",1e829a8c6f
Why does download.py get stuck at 0% when it tries to fetch model.onnx from HuggingFace?,"This happens because the connection to HuggingFace’s CDN is slow or blocked, so the download appears stuck at 0%.

The fix is to download the file manually in your browser:

https://huggingface.co/Xenova/all-MiniLM-L6-v2/resolve/main/onnx/model.onnx

Then save it to:

`models/Xenova/all-MiniLM-L6-v2/model.onnx`

`download.py` checks whether the file already exists locally, so once you place it there, it should skip the download and continue.","This can happen due to a slow or blocked connection to HuggingFace's CDN. Fix: download the file directly from your browser at https://huggingface.co/Xenova/all-MiniLM-L6-v2/resolve/main/onnx/model.onnx and save it to models/Xenova/all-MiniLM-L6-v2/model.onnx. The script checks if the file exists locally before re-downloading, so it'll skip straight past once it's there.",29b69fbe0b
Is there a way to avoid the download hanging and just add the ONNX file manually?,"Yes — you can avoid the hanging download by downloading the ONNX file manually in your browser and placing it in the expected local path.

Download it here:

https://huggingface.co/Xenova/all-MiniLM-L6-v2/resolve/main/onnx/model.onnx

Then save it as:

```bash
models/Xenova/all-MiniLM-L6-v2/model.onnx
```

The script will check whether the file already exists locally, so once it’s there, it should skip the download.","This can happen due to a slow or blocked connection to HuggingFace's CDN. Fix: download the file directly from your browser at https://huggingface.co/Xenova/all-MiniLM-L6-v2/resolve/main/onnx/model.onnx and save it to models/Xenova/all-MiniLM-L6-v2/model.onnx. The script checks if the file exists locally before re-downloading, so it'll skip straight past once it's there.",29b69fbe0b
What should I do if the HuggingFace download is super slow or never starts?,"If the HuggingFace download is super slow or never starts, download the file directly in your browser instead:

https://huggingface.co/Xenova/all-MiniLM-L6-v2/resolve/main/onnx/model.onnx

Then save it to:

`models/Xenova/all-MiniLM-L6-v2/model.onnx`

After that, rerun the script — it will detect the local file and skip the download.","This can happen due to a slow or blocked connection to HuggingFace's CDN. Fix: download the file directly from your browser at https://huggingface.co/Xenova/all-MiniLM-L6-v2/resolve/main/onnx/model.onnx and save it to models/Xenova/all-MiniLM-L6-v2/model.onnx. The script checks if the file exists locally before re-downloading, so it'll skip straight past once it's there.",29b69fbe0b
Can I open the model.onnx link in my browser and place it in the right folder myself?,"Yes. You can open the `model.onnx` link in your browser, download it manually, and save it to:

`models/Xenova/all-MiniLM-L6-v2/model.onnx`

The script will detect that the file already exists locally and skip downloading it again.","This can happen due to a slow or blocked connection to HuggingFace's CDN. Fix: download the file directly from your browser at https://huggingface.co/Xenova/all-MiniLM-L6-v2/resolve/main/onnx/model.onnx and save it to models/Xenova/all-MiniLM-L6-v2/model.onnx. The script checks if the file exists locally before re-downloading, so it'll skip straight past once it's there.",29b69fbe0b
"If I already saved model.onnx locally, will download.py still try to download it again?","No. The script checks whether the file already exists locally first, so if `model.onnx` is already there, `download.py` should skip downloading it again.","This can happen due to a slow or blocked connection to HuggingFace's CDN. Fix: download the file directly from your browser at https://huggingface.co/Xenova/all-MiniLM-L6-v2/resolve/main/onnx/model.onnx and save it to models/Xenova/all-MiniLM-L6-v2/model.onnx. The script checks if the file exists locally before re-downloading, so it'll skip straight past once it's there.",29b69fbe0b
"Why does the matrix-dot version and the Python loop version of vector search not match exactly, even though they should do the same thing?","Because of floating-point precision and the order of operations.

`X.dot(v_query)` uses optimized low-level BLAS code, while the Python loop adds the same products sequentially. Since floating-point addition is not perfectly associative, the numbers can differ in the last few decimal places even though the math is the same.

So the results are effectively the same, just not exactly identical. Compare them with a tolerance, for example:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```","In [Lesson 4 (Vector Search)](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/02-vector-search/lessons/04-vector-search.md) we compute the scores two ways — `scores = X.dot(v_query)` and the equivalent for-loop — and they can come out slightly different. This is normal, it's just floating-point precision. If you compare them with `np.allclose(scores, scores_loop)` you might get `False`, even though both compute the same dot products. They just add the numbers up in a slightly different order, and floating-point addition isn't perfectly associative, so the results can differ in the last few decimal places.

Under the hood, `X.dot(v)` runs optimized BLAS code that sums in a different order than a sequential Python loop. The math is the same; only the rounding differs, so the values aren't really ""wrong.""

Just compare them with a small tolerance instead of expecting an exact match:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```",e889793af9
"Is it normal for `np.allclose(scores, scores_loop)` to return False when I compare vector search scores from `X.dot(v_query)` and a loop?","Yes — that’s normal.

`X.dot(v_query)` and a Python loop can produce tiny differences because floating-point addition is not perfectly associative, and the operations are summed in a different order. So `np.allclose(scores, scores_loop)` may return `False` if the default tolerance is too strict, even though both methods are computing the same dot products.

Try:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```

If you want, I can also explain why the matrix version and the loop differ internally.","In [Lesson 4 (Vector Search)](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/02-vector-search/lessons/04-vector-search.md) we compute the scores two ways — `scores = X.dot(v_query)` and the equivalent for-loop — and they can come out slightly different. This is normal, it's just floating-point precision. If you compare them with `np.allclose(scores, scores_loop)` you might get `False`, even though both compute the same dot products. They just add the numbers up in a slightly different order, and floating-point addition isn't perfectly associative, so the results can differ in the last few decimal places.

Under the hood, `X.dot(v)` runs optimized BLAS code that sums in a different order than a sequential Python loop. The math is the same; only the rounding differs, so the values aren't really ""wrong.""

Just compare them with a small tolerance instead of expecting an exact match:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```",e889793af9
Do I need to worry if the two vector search implementations give slightly different numbers in the last decimals?,"No — small differences in the last decimals are normal and usually nothing to worry about. They come from floating-point precision and from the two implementations summing numbers in a different order.

Use a tolerance check like:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```

The values are effectively the same.","In [Lesson 4 (Vector Search)](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/02-vector-search/lessons/04-vector-search.md) we compute the scores two ways — `scores = X.dot(v_query)` and the equivalent for-loop — and they can come out slightly different. This is normal, it's just floating-point precision. If you compare them with `np.allclose(scores, scores_loop)` you might get `False`, even though both compute the same dot products. They just add the numbers up in a slightly different order, and floating-point addition isn't perfectly associative, so the results can differ in the last few decimal places.

Under the hood, `X.dot(v)` runs optimized BLAS code that sums in a different order than a sequential Python loop. The math is the same; only the rounding differs, so the values aren't really ""wrong.""

Just compare them with a small tolerance instead of expecting an exact match:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```",e889793af9
Why do optimized BLAS dot products and a manual for-loop end up with tiny differences in vector search results?,"They differ because of **floating-point precision** and the fact that the two methods sum numbers in a **different order**.

- `X.dot(v_query)` uses optimized **BLAS** routines under the hood.
- A manual Python for-loop adds the values sequentially.

Floating-point addition is **not perfectly associative**, so tiny rounding differences can appear in the last few decimal places. The results are effectively the same, just not bit-for-bit identical.

Use a tolerance check like:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```

to compare them.","In [Lesson 4 (Vector Search)](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/02-vector-search/lessons/04-vector-search.md) we compute the scores two ways — `scores = X.dot(v_query)` and the equivalent for-loop — and they can come out slightly different. This is normal, it's just floating-point precision. If you compare them with `np.allclose(scores, scores_loop)` you might get `False`, even though both compute the same dot products. They just add the numbers up in a slightly different order, and floating-point addition isn't perfectly associative, so the results can differ in the last few decimal places.

Under the hood, `X.dot(v)` runs optimized BLAS code that sums in a different order than a sequential Python loop. The math is the same; only the rounding differs, so the values aren't really ""wrong.""

Just compare them with a small tolerance instead of expecting an exact match:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```",e889793af9
What’s the right way to compare the score arrays from the matrix version and the loop version without expecting exact equality?,"Use `np.allclose` with a small tolerance, not exact equality:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```

Because the matrix and loop versions sum floating-point numbers in a different order, tiny differences in the last decimals are normal.","In [Lesson 4 (Vector Search)](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/02-vector-search/lessons/04-vector-search.md) we compute the scores two ways — `scores = X.dot(v_query)` and the equivalent for-loop — and they can come out slightly different. This is normal, it's just floating-point precision. If you compare them with `np.allclose(scores, scores_loop)` you might get `False`, even though both compute the same dot products. They just add the numbers up in a slightly different order, and floating-point addition isn't perfectly associative, so the results can differ in the last few decimal places.

Under the hood, `X.dot(v)` runs optimized BLAS code that sums in a different order than a sequential Python loop. The math is the same; only the rounding differs, so the values aren't really ""wrong.""

Just compare them with a small tolerance instead of expecting an exact match:

```python
np.allclose(scores, scores_loop, atol=1e-5)
```",e889793af9
Why am I getting IndexError: list index out of range when I try to access the best chunk in my vector search code?,"The error usually means the number of embeddings you created does not match the number of chunks in your list.

Make sure you generate embeddings directly from the chunk contents, for example:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```

Then `X` should have the same number of rows as `len(chunks)`. If they differ, accessing the “best chunk” by index can produce `IndexError: list index out of range`.","The error typically happens when the number of embeddings you generate does not match the number of document chunks. Make sure you create embeddings directly from the chunk list:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```

The number of rows in `X` should be equal to `len(chunks)`.",1a7b27c4df
What usually causes list index out of range when selecting the top matching chunk from embeddings?,"It usually happens when the number of embeddings does not match the number of document chunks. Make sure you generate embeddings directly from the chunk list, so the embedding matrix has the same number of rows as `len(chunks)`:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```","The error typically happens when the number of embeddings you generate does not match the number of document chunks. Make sure you create embeddings directly from the chunk list:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```

The number of rows in `X` should be equal to `len(chunks)`.",1a7b27c4df
How do I make sure my chunk list and embedding list stay aligned so I don't get this error?,"Make sure you generate the embeddings **directly from the chunk list** and don’t modify one list without the other.

A safe pattern is:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```

This way, the number of embeddings in `X` will match `len(chunks)`, so your chunk list and embedding list stay aligned.","The error typically happens when the number of embeddings you generate does not match the number of document chunks. Make sure you create embeddings directly from the chunk list:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```

The number of rows in `X` should be equal to `len(chunks)`.",1a7b27c4df
Should I create embeddings from the whole chunks array or from the chunk contents only?,"Create embeddings from the chunk contents only, not the whole `chunks` array.

Use something like:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```

The embeddings should line up 1:1 with the chunks.","The error typically happens when the number of embeddings you generate does not match the number of document chunks. Make sure you create embeddings directly from the chunk list:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```

The number of rows in `X` should be equal to `len(chunks)`.",1a7b27c4df
What check can I do to avoid an IndexError when the number of embeddings doesn't match the number of chunks?,"Check that the number of embedding rows matches the number of chunks:

```python
len(X) == len(chunks)
```

A safe way is to generate embeddings directly from the chunk contents:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```

If `len(X)` is not equal to `len(chunks)`, that mismatch is likely what leads to the `IndexError`.","The error typically happens when the number of embeddings you generate does not match the number of document chunks. Make sure you create embeddings directly from the chunk list:

```python
contents = [chunk[""content""] for chunk in chunks]
X = embedder.encode_batch(contents)
```

The number of rows in `X` should be equal to `len(chunks)`.",1a7b27c4df
"In Module 2 homework Q2, my cosine similarity is way off the multiple-choice answers — could I be using the wrong embedding model?","Yes — the most common reason is using the wrong embedding model.

For Module 2 Homework Q2, you should use the homework’s `Embedder` from `embedder.py`, not `sentence-transformers`:

```python
from embedder import Embedder
model = Embedder()
```

If you used a different model like `multi-qa-mpnet-base-dot-v1` or `all-mpnet-base-v2`, your cosine similarity can be very different and not match the multiple-choice options.

Also double-check that you are:

- embedding the page’s `content` field,
- comparing it to the query vector from Q1,
- using the normalized vectors from `Embedder` so the dot product is the cosine similarity.

If it’s still slightly off after that, choose the closest option.","The most common cause is using a different embedding model than the homework specifies. Homework 2 tells you **not** to use `sentence-transformers` and to use the lightweight ONNX `Embedder` from `embedder.py` instead:

```python
from embedder import Embedder
model = Embedder()
```

Both approaches produce the same vectors for the same model, but if you embed with a different model (for example the `multi-qa-mpnet-base-dot-v1` model from the lessons, or `all-mpnet-base-v2`) you'll get a cosine value that isn't among the options.

Also check that:

- You're embedding the page's `content` field (not the filename or the whole dict).
- You're comparing against the query vector from Q1.
- The vectors are normalized — the `Embedder` returns normalized vectors, so the dot product is the cosine similarity directly.

If your value is still slightly off after using the right model, pick the closest option — small numerical differences are expected.",eaa17a9dc6
"For the homework embeddings, do I need to use the ONNX `Embedder` from `embedder.py` instead of `sentence-transformers`?","Yes — for Homework 2, you should use the lightweight ONNX `Embedder` from `embedder.py`, not `sentence-transformers`.

Use:

```python
from embedder import Embedder
model = Embedder()
```

The homework explicitly says not to use `sentence-transformers`.","The most common cause is using a different embedding model than the homework specifies. Homework 2 tells you **not** to use `sentence-transformers` and to use the lightweight ONNX `Embedder` from `embedder.py` instead:

```python
from embedder import Embedder
model = Embedder()
```

Both approaches produce the same vectors for the same model, but if you embed with a different model (for example the `multi-qa-mpnet-base-dot-v1` model from the lessons, or `all-mpnet-base-v2`) you'll get a cosine value that isn't among the options.

Also check that:

- You're embedding the page's `content` field (not the filename or the whole dict).
- You're comparing against the query vector from Q1.
- The vectors are normalized — the `Embedder` returns normalized vectors, so the dot product is the cosine similarity directly.

If your value is still slightly off after using the right model, pick the closest option — small numerical differences are expected.",eaa17a9dc6
Why would I get a cosine value that doesn't match any option if I used `multi-qa-mpnet-base-dot-v1` or `all-mpnet-base-v2`?,"Because those models are **not the ones the homework expects**. The homework uses the lightweight ONNX `Embedder` from `embedder.py`, and if you embed with `multi-qa-mpnet-base-dot-v1` or `all-mpnet-base-v2`, you’ll get a cosine value that won’t match the answer choices.

Use:

```python
from embedder import Embedder
model = Embedder()
```

Also make sure you’re:

- embedding the page’s **`content`** field,
- comparing it to the **Q1 query vector**,
- using the fact that the returned vectors are already **normalized**.

If it’s still slightly off after that, choose the **closest option**.","The most common cause is using a different embedding model than the homework specifies. Homework 2 tells you **not** to use `sentence-transformers` and to use the lightweight ONNX `Embedder` from `embedder.py` instead:

```python
from embedder import Embedder
model = Embedder()
```

Both approaches produce the same vectors for the same model, but if you embed with a different model (for example the `multi-qa-mpnet-base-dot-v1` model from the lessons, or `all-mpnet-base-v2`) you'll get a cosine value that isn't among the options.

Also check that:

- You're embedding the page's `content` field (not the filename or the whole dict).
- You're comparing against the query vector from Q1.
- The vectors are normalized — the `Embedder` returns normalized vectors, so the dot product is the cosine similarity directly.

If your value is still slightly off after using the right model, pick the closest option — small numerical differences are expected.",eaa17a9dc6
"Am I supposed to embed the page `content` field for Q2, or something else like the filename or full record?","Yes — for Q2 you should embed the page’s `content` field, not the filename or the full record.","The most common cause is using a different embedding model than the homework specifies. Homework 2 tells you **not** to use `sentence-transformers` and to use the lightweight ONNX `Embedder` from `embedder.py` instead:

```python
from embedder import Embedder
model = Embedder()
```

Both approaches produce the same vectors for the same model, but if you embed with a different model (for example the `multi-qa-mpnet-base-dot-v1` model from the lessons, or `all-mpnet-base-v2`) you'll get a cosine value that isn't among the options.

Also check that:

- You're embedding the page's `content` field (not the filename or the whole dict).
- You're comparing against the query vector from Q1.
- The vectors are normalized — the `Embedder` returns normalized vectors, so the dot product is the cosine similarity directly.

If your value is still slightly off after using the right model, pick the closest option — small numerical differences are expected.",eaa17a9dc6
"If my similarity is still a little different after using the right setup, should I just choose the closest answer?","Yes — if you’ve used the correct setup and the value is still slightly off, choose the closest option. Small numerical differences are expected.","The most common cause is using a different embedding model than the homework specifies. Homework 2 tells you **not** to use `sentence-transformers` and to use the lightweight ONNX `Embedder` from `embedder.py` instead:

```python
from embedder import Embedder
model = Embedder()
```

Both approaches produce the same vectors for the same model, but if you embed with a different model (for example the `multi-qa-mpnet-base-dot-v1` model from the lessons, or `all-mpnet-base-v2`) you'll get a cosine value that isn't among the options.

Also check that:

- You're embedding the page's `content` field (not the filename or the whole dict).
- You're comparing against the query vector from Q1.
- The vectors are normalized — the `Embedder` returns normalized vectors, so the dot product is the cosine similarity directly.

If your value is still slightly off after using the right model, pick the closest option — small numerical differences are expected.",eaa17a9dc6
"How do I set up Gemini, OpenAI, and Tavily keys so Kestra can actually use them in the orchestration module?","Set them as environment variables **before starting Kestra**.

For the orchestration module, Kestra expects:

- a plain `GEMINI_API_KEY` for the AI Copilot
- base64-encoded `SECRET_...` variables for the flows

Example:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
export SECRET_GEMINI_API_KEY=$(echo -n $GEMINI_API_KEY | base64)
export SECRET_OPENAI_API_KEY=$(echo -n ""your-openai-api-key-here"" | base64)
export SECRET_TAVILY_API_KEY=$(echo -n ""your-tavily-api-key-here"" | base64)
```

Then restart Kestra so it picks them up:

```bash
docker compose up -d
```

Inside a flow, reference the secret without the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```

So, in short: **export plain Gemini for Copilot, base64 `SECRET_` vars for flows, then restart Kestra.**","Kestra reads secrets from environment variables that are prefixed with `SECRET_` and whose value is **base64-encoded**. You export them in the terminal *before* starting Kestra. For Gemini you need two variables — the plain one (used by the AI Copilot) and the base64-encoded `SECRET_` one (used by the flows):

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""                              # used by AI Copilot
export SECRET_GEMINI_API_KEY=$(echo -n $GEMINI_API_KEY | base64)             # used by the flows
export SECRET_OPENAI_API_KEY=$(echo -n ""your-openai-api-key-here"" | base64)  # required for flow 3
export SECRET_TAVILY_API_KEY=$(echo -n ""your-tavily-api-key-here"" | base64)  # required for web search (flows 3, 5, 6)
```

Then start (or restart) Kestra so it picks up the variables:

```bash
docker compose up -d
```

Inside a flow, reference a secret **without** the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```

See the [setup lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/03-setup.md) for the full walkthrough. Never commit your keys to Git.",3860e5fe8b
"Do I need both a normal Gemini key and a SECRET_GEMINI_API_KEY for Kestra, or is one enough?","One is enough for the flows, but Kestra also uses a plain `GEMINI_API_KEY` for the AI Copilot.

So in practice:

- **For Kestra flows:** use `SECRET_GEMINI_API_KEY`
- **For the AI Copilot:** use `GEMINI_API_KEY`

If you want both to work, export **both**.","Kestra reads secrets from environment variables that are prefixed with `SECRET_` and whose value is **base64-encoded**. You export them in the terminal *before* starting Kestra. For Gemini you need two variables — the plain one (used by the AI Copilot) and the base64-encoded `SECRET_` one (used by the flows):

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""                              # used by AI Copilot
export SECRET_GEMINI_API_KEY=$(echo -n $GEMINI_API_KEY | base64)             # used by the flows
export SECRET_OPENAI_API_KEY=$(echo -n ""your-openai-api-key-here"" | base64)  # required for flow 3
export SECRET_TAVILY_API_KEY=$(echo -n ""your-tavily-api-key-here"" | base64)  # required for web search (flows 3, 5, 6)
```

Then start (or restart) Kestra so it picks up the variables:

```bash
docker compose up -d
```

Inside a flow, reference a secret **without** the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```

See the [setup lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/03-setup.md) for the full walkthrough. Never commit your keys to Git.",3860e5fe8b
What’s the right way to export API secrets for Kestra before starting docker compose?,"Export them in your terminal **before** starting `docker compose`, and use `echo -n` so you don’t add a newline:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
export SECRET_OPENAI_API_KEY=$(echo -n ""your-openai-api-key-here"" | base64)
export SECRET_TAVILY_API_KEY=$(echo -n ""your-tavily-api-key-here"" | base64)
```

Then start Kestra:

```bash
docker compose up -d
```

Use the secret in flows without the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```

","Kestra reads secrets from environment variables that are prefixed with `SECRET_` and whose value is **base64-encoded**. You export them in the terminal *before* starting Kestra. For Gemini you need two variables — the plain one (used by the AI Copilot) and the base64-encoded `SECRET_` one (used by the flows):

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""                              # used by AI Copilot
export SECRET_GEMINI_API_KEY=$(echo -n $GEMINI_API_KEY | base64)             # used by the flows
export SECRET_OPENAI_API_KEY=$(echo -n ""your-openai-api-key-here"" | base64)  # required for flow 3
export SECRET_TAVILY_API_KEY=$(echo -n ""your-tavily-api-key-here"" | base64)  # required for web search (flows 3, 5, 6)
```

Then start (or restart) Kestra so it picks up the variables:

```bash
docker compose up -d
```

Inside a flow, reference a secret **without** the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```

See the [setup lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/03-setup.md) for the full walkthrough. Never commit your keys to Git.",3860e5fe8b
"Inside a Kestra flow, should I reference SECRET_GEMINI_API_KEY directly or use the secret name without the SECRET_ prefix?","Use the secret **without** the `SECRET_` prefix inside the flow:

```yaml
{{ secret('GEMINI_API_KEY') }}
```

`SECRET_GEMINI_API_KEY` is the environment variable you export for Kestra to read the secret.","Kestra reads secrets from environment variables that are prefixed with `SECRET_` and whose value is **base64-encoded**. You export them in the terminal *before* starting Kestra. For Gemini you need two variables — the plain one (used by the AI Copilot) and the base64-encoded `SECRET_` one (used by the flows):

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""                              # used by AI Copilot
export SECRET_GEMINI_API_KEY=$(echo -n $GEMINI_API_KEY | base64)             # used by the flows
export SECRET_OPENAI_API_KEY=$(echo -n ""your-openai-api-key-here"" | base64)  # required for flow 3
export SECRET_TAVILY_API_KEY=$(echo -n ""your-tavily-api-key-here"" | base64)  # required for web search (flows 3, 5, 6)
```

Then start (or restart) Kestra so it picks up the variables:

```bash
docker compose up -d
```

Inside a flow, reference a secret **without** the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```

See the [setup lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/03-setup.md) for the full walkthrough. Never commit your keys to Git.",3860e5fe8b
"Why are my API keys not being picked up by Kestra after I added them, and do I need to restart the container?","Kestra won’t pick up new API key environment variables until it is restarted. You need to export the variables first, then restart the container so Kestra reads them.

Also make sure:

- `SECRET_...` variables are **base64-encoded**
- you use `echo -n` when encoding, to avoid adding a newline
- in the flow, reference the secret **without** the `SECRET_` prefix, e.g. `{{ secret('GEMINI_API_KEY') }}`

Example:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
docker compose up -d
```

If it still fails, the base64 value is often the issue.","Kestra reads secrets from environment variables that are prefixed with `SECRET_` and whose value is **base64-encoded**. You export them in the terminal *before* starting Kestra. For Gemini you need two variables — the plain one (used by the AI Copilot) and the base64-encoded `SECRET_` one (used by the flows):

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""                              # used by AI Copilot
export SECRET_GEMINI_API_KEY=$(echo -n $GEMINI_API_KEY | base64)             # used by the flows
export SECRET_OPENAI_API_KEY=$(echo -n ""your-openai-api-key-here"" | base64)  # required for flow 3
export SECRET_TAVILY_API_KEY=$(echo -n ""your-tavily-api-key-here"" | base64)  # required for web search (flows 3, 5, 6)
```

Then start (or restart) Kestra so it picks up the variables:

```bash
docker compose up -d
```

Inside a flow, reference a secret **without** the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```

See the [setup lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/03-setup.md) for the full walkthrough. Never commit your keys to Git.",3860e5fe8b
"Why does Kestra AI Copilot keep saying it can only help with creating flows, and what setting am I missing?","It’s usually saying that because the Copilot did **not get a valid Gemini API key**, so it falls back to a canned message like “I can only assist with creating Kestra flows.”

What you’re missing is the **plain** environment variable:

- `GEMINI_API_KEY` for the Copilot
- not just `SECRET_GEMINI_API_KEY` for flows

Fix:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
docker compose up -d
```

Then restart Kestra. If it still doesn’t work, check that:

- `echo $GEMINI_API_KEY` shows the key in the same shell
- the key is valid and not mistyped
- you may have hit Gemini free-tier quota/rate limits

So the main missing setting is the plain `GEMINI_API_KEY`.","This message means the AI Copilot didn't get a valid Gemini API key, so it falls back to a canned refusal. In Kestra's Open Source edition the Copilot only supports Gemini, and it reads the **plain** `GEMINI_API_KEY` variable (not the base64-encoded `SECRET_GEMINI_API_KEY` that the flows use).

Make sure you exported the plain key before starting Kestra, then restart it:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
docker compose up -d
```

If it still fails, the key is usually missing, mistyped, or rate-limited:

- Confirm the variable is actually set in the shell you ran `docker compose up` from (`echo $GEMINI_API_KEY`).
- Generate a fresh key in [Google AI Studio](https://aistudio.google.com/app/apikey).
- If you've been running the agent/multi-agent flows a lot, you may have hit the free-tier quota (`429 Resource Exhausted`) — wait a minute and retry.

Note that the Copilot needs the **plain** `GEMINI_API_KEY` while the flows need `SECRET_GEMINI_API_KEY` — export both.",04440cab11
"In Kestra OSS, do I need to use a regular Gemini key or the base64 secret env var for the Copilot?","Use the **plain `GEMINI_API_KEY`** for the **Copilot**.

The **base64 `SECRET_GEMINI_API_KEY`** is for **flows**. For Kestra OSS, export both before starting Kestra:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
```

So for the Copilot specifically: **regular Gemini key, not the base64 secret env var**.","This message means the AI Copilot didn't get a valid Gemini API key, so it falls back to a canned refusal. In Kestra's Open Source edition the Copilot only supports Gemini, and it reads the **plain** `GEMINI_API_KEY` variable (not the base64-encoded `SECRET_GEMINI_API_KEY` that the flows use).

Make sure you exported the plain key before starting Kestra, then restart it:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
docker compose up -d
```

If it still fails, the key is usually missing, mistyped, or rate-limited:

- Confirm the variable is actually set in the shell you ran `docker compose up` from (`echo $GEMINI_API_KEY`).
- Generate a fresh key in [Google AI Studio](https://aistudio.google.com/app/apikey).
- If you've been running the agent/multi-agent flows a lot, you may have hit the free-tier quota (`429 Resource Exhausted`) — wait a minute and retry.

Note that the Copilot needs the **plain** `GEMINI_API_KEY` while the flows need `SECRET_GEMINI_API_KEY` — export both.",04440cab11
"I already set `SECRET_GEMINI_API_KEY` for my flows, but the Copilot still refuses to answer — what should I export?","You should export the plain `GEMINI_API_KEY` too — the Copilot does **not** use `SECRET_GEMINI_API_KEY`.

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
docker compose up -d
```

If you use the flows as well, keep `SECRET_GEMINI_API_KEY` for those, but the Copilot specifically needs `GEMINI_API_KEY`.","This message means the AI Copilot didn't get a valid Gemini API key, so it falls back to a canned refusal. In Kestra's Open Source edition the Copilot only supports Gemini, and it reads the **plain** `GEMINI_API_KEY` variable (not the base64-encoded `SECRET_GEMINI_API_KEY` that the flows use).

Make sure you exported the plain key before starting Kestra, then restart it:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
docker compose up -d
```

If it still fails, the key is usually missing, mistyped, or rate-limited:

- Confirm the variable is actually set in the shell you ran `docker compose up` from (`echo $GEMINI_API_KEY`).
- Generate a fresh key in [Google AI Studio](https://aistudio.google.com/app/apikey).
- If you've been running the agent/multi-agent flows a lot, you may have hit the free-tier quota (`429 Resource Exhausted`) — wait a minute and retry.

Note that the Copilot needs the **plain** `GEMINI_API_KEY` while the flows need `SECRET_GEMINI_API_KEY` — export both.",04440cab11
What is the quickest way to restart Kestra after adding the Gemini API key so the Copilot starts working?,"The quickest way is to restart Kestra with Docker Compose after exporting the key:

```bash
docker compose up -d
```

If needed, first set the plain key in the same shell:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
docker compose up -d
```

","This message means the AI Copilot didn't get a valid Gemini API key, so it falls back to a canned refusal. In Kestra's Open Source edition the Copilot only supports Gemini, and it reads the **plain** `GEMINI_API_KEY` variable (not the base64-encoded `SECRET_GEMINI_API_KEY` that the flows use).

Make sure you exported the plain key before starting Kestra, then restart it:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
docker compose up -d
```

If it still fails, the key is usually missing, mistyped, or rate-limited:

- Confirm the variable is actually set in the shell you ran `docker compose up` from (`echo $GEMINI_API_KEY`).
- Generate a fresh key in [Google AI Studio](https://aistudio.google.com/app/apikey).
- If you've been running the agent/multi-agent flows a lot, you may have hit the free-tier quota (`429 Resource Exhausted`) — wait a minute and retry.

Note that the Copilot needs the **plain** `GEMINI_API_KEY` while the flows need `SECRET_GEMINI_API_KEY` — export both.",04440cab11
"Could the Copilot refusal be caused by an invalid, missing, or rate-limited Gemini key, and how can I check that?","Yes. According to the course notes, a Copilot refusal can happen if the Gemini key is missing, mistyped, invalid, or rate-limited.

How to check:

- In the same shell you used to start Kestra, verify the variable is set:
  ```bash
  echo $GEMINI_API_KEY
  ```
- Make sure you exported the plain `GEMINI_API_KEY` before starting Kestra, then restart it:
  ```bash
  docker compose up -d
  ```
- If the key exists but Copilot still refuses, generate a fresh key in Google AI Studio.
- If you’ve been running the agent/multi-agent flows a lot, you may have hit the free-tier quota and see a `429 Resource Exhausted`; wait a minute and retry.

Also note: the Copilot uses the plain `GEMINI_API_KEY`, not `SECRET_GEMINI_API_KEY`.","This message means the AI Copilot didn't get a valid Gemini API key, so it falls back to a canned refusal. In Kestra's Open Source edition the Copilot only supports Gemini, and it reads the **plain** `GEMINI_API_KEY` variable (not the base64-encoded `SECRET_GEMINI_API_KEY` that the flows use).

Make sure you exported the plain key before starting Kestra, then restart it:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
docker compose up -d
```

If it still fails, the key is usually missing, mistyped, or rate-limited:

- Confirm the variable is actually set in the shell you ran `docker compose up` from (`echo $GEMINI_API_KEY`).
- Generate a fresh key in [Google AI Studio](https://aistudio.google.com/app/apikey).
- If you've been running the agent/multi-agent flows a lot, you may have hit the free-tier quota (`429 Resource Exhausted`) — wait a minute and retry.

Note that the Copilot needs the **plain** `GEMINI_API_KEY` while the flows need `SECRET_GEMINI_API_KEY` — export both.",04440cab11
Why does my Kestra flow say the Gemini API key is missing or invalid even though I set SECRET_GEMINI_API_KEY?,"The most common reason is that the secret was encoded incorrectly, usually because of a trailing space or newline when base64-encoding it. That makes the decoded value wrong, so authentication fails.

Use `echo -n` and quote the value:

```bash
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
```

Also check these:

- Restart Kestra after exporting the variable, so it picks up the new env vars:
  ```bash
  docker compose up -d
  ```
- In your flow, reference it **without** the `SECRET_` prefix:
  ```yaml
  {{ secret('GEMINI_API_KEY') }}
  ```
- Make sure you exported it in the same shell session that starts Kestra.

A cleaner option is to put the variables in a `.env` file and start Kestra with:

```bash
docker compose --env-file ./.env -f ./docker-compose.yml up -d
```

","The most common cause is a corrupted base64 value. If you encode the key with a trailing space or newline, the decoded secret is wrong and authentication fails.

Use `echo -n` (which omits the trailing newline) and quote the value:

```bash
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
```

Other things to check:

- You must **restart** Kestra after exporting, so the container picks up the new variables: `docker compose up -d` (or `docker compose down && docker compose up -d`).
- Reference the secret in flows **without** the `SECRET_` prefix: `{{ secret('GEMINI_API_KEY') }}`.
- Export the variables in the same shell session you start Kestra from.

A cleaner alternative is to put the keys in a `.env` file and pass it explicitly:

```bash
docker compose --env-file ./.env -f ./docker-compose.yml up -d
```",c8ca21af33
Could a bad base64 encode be the reason my secret auth keeps failing in Kestra?,"Yes — a bad base64 encode is a common reason Kestra secret auth fails.

The main pitfall is encoding the API key with a trailing newline or space, which corrupts the decoded secret. Use:

```bash
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
```

Also make sure to:

- restart Kestra after exporting the variable
- define the secret in the same shell session used to start Kestra
- reference it in flows without the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```

If you want, I can help you check your exact command.","The most common cause is a corrupted base64 value. If you encode the key with a trailing space or newline, the decoded secret is wrong and authentication fails.

Use `echo -n` (which omits the trailing newline) and quote the value:

```bash
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
```

Other things to check:

- You must **restart** Kestra after exporting, so the container picks up the new variables: `docker compose up -d` (or `docker compose down && docker compose up -d`).
- Reference the secret in flows **without** the `SECRET_` prefix: `{{ secret('GEMINI_API_KEY') }}`.
- Export the variables in the same shell session you start Kestra from.

A cleaner alternative is to put the keys in a `.env` file and pass it explicitly:

```bash
docker compose --env-file ./.env -f ./docker-compose.yml up -d
```",c8ca21af33
"Do I need to restart Kestra after exporting the API key, or is just setting the env var enough?","Yes — you need to **restart Kestra** after exporting the env var so the container picks up the new variables.

Just setting it in your shell is not enough unless you start Kestra from that same shell session afterward. For example:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
docker compose up -d
```

If Kestra is already running, restart it:

```bash
docker compose down && docker compose up -d
```

Also make sure you’re using the secret in flows without the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```","The most common cause is a corrupted base64 value. If you encode the key with a trailing space or newline, the decoded secret is wrong and authentication fails.

Use `echo -n` (which omits the trailing newline) and quote the value:

```bash
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
```

Other things to check:

- You must **restart** Kestra after exporting, so the container picks up the new variables: `docker compose up -d` (or `docker compose down && docker compose up -d`).
- Reference the secret in flows **without** the `SECRET_` prefix: `{{ secret('GEMINI_API_KEY') }}`.
- Export the variables in the same shell session you start Kestra from.

A cleaner alternative is to put the keys in a `.env` file and pass it explicitly:

```bash
docker compose --env-file ./.env -f ./docker-compose.yml up -d
```",c8ca21af33
"In my flow, should I use SECRET_GEMINI_API_KEY or secret('GEMINI_API_KEY') to read the key?","Use `secret('GEMINI_API_KEY')` inside the flow.

`SECRET_GEMINI_API_KEY` is the environment variable you export for Kestra to read at startup, but in the flow you reference it **without** the `SECRET_` prefix:

```yaml
{{ secret('GEMINI_API_KEY') }}
```","The most common cause is a corrupted base64 value. If you encode the key with a trailing space or newline, the decoded secret is wrong and authentication fails.

Use `echo -n` (which omits the trailing newline) and quote the value:

```bash
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
```

Other things to check:

- You must **restart** Kestra after exporting, so the container picks up the new variables: `docker compose up -d` (or `docker compose down && docker compose up -d`).
- Reference the secret in flows **without** the `SECRET_` prefix: `{{ secret('GEMINI_API_KEY') }}`.
- Export the variables in the same shell session you start Kestra from.

A cleaner alternative is to put the keys in a `.env` file and pass it explicitly:

```bash
docker compose --env-file ./.env -f ./docker-compose.yml up -d
```",c8ca21af33
"What’s the safest way to pass Gemini keys to Kestra, with export commands or a .env file?","The safest option is to use a `.env` file, not ad-hoc export commands in a shell.

For Kestra specifically, the course says:

- Put your keys in environment variables
- Kestra secrets must be prefixed with `SECRET_`
- The secret value should be base64-encoded
- Restart Kestra after setting them

Example:

```bash
export GEMINI_API_KEY=""your-gemini-api-key-here""
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
```

Then start Kestra again:

```bash
docker compose up -d
```

If you want a cleaner and safer setup, use a `.env` file and pass it explicitly to Docker Compose:

```bash
docker compose --env-file ./.env -f ./docker-compose.yml up -d
```

Also, never commit the `.env` file to Git.

So: **`.env` file is the safer and recommended approach**.","The most common cause is a corrupted base64 value. If you encode the key with a trailing space or newline, the decoded secret is wrong and authentication fails.

Use `echo -n` (which omits the trailing newline) and quote the value:

```bash
export SECRET_GEMINI_API_KEY=$(echo -n ""$GEMINI_API_KEY"" | base64)
```

Other things to check:

- You must **restart** Kestra after exporting, so the container picks up the new variables: `docker compose up -d` (or `docker compose down && docker compose up -d`).
- Reference the secret in flows **without** the `SECRET_` prefix: `{{ secret('GEMINI_API_KEY') }}`.
- Export the variables in the same shell session you start Kestra from.

A cleaner alternative is to put the keys in a `.env` file and pass it explicitly:

```bash
docker compose --env-file ./.env -f ./docker-compose.yml up -d
```",c8ca21af33
Why do we even need an orchestrator like Kestra if I can just execute the code in a notebook?,"Notebooks are great for learning and experimenting, but for real workflows you usually need more than “run this cell once.”

An orchestrator like Kestra gives you:

- scheduling
- retries
- monitoring
- secret management
- reliable chaining of multiple tasks

So the main reason is production reliability and automation.

In this course, Kestra is also used to demonstrate AI workflows like:
- generating flows from natural language
- grounding responses with RAG
- building agents that choose which tools to call

So: use a notebook for experimentation, but use an orchestrator when you want repeatable, observable, production-style workflows.","Notebooks are great for learning and experimenting, but real AI workflows need more than a script that runs once: scheduling, retries, monitoring, secret management, and reliably chaining tasks together. That's what an orchestrator like Kestra provides.

In this module Kestra is also the vehicle for the AI techniques the course is teaching: AI Copilot to generate flows from natural language, RAG to ground responses in real data, and AI agents that decide which tools to call. The goal is to see how AI fits into production-style workflows, not just notebook cells.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else. See the [module intro](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/01-intro.md) for the full motivation.",193612db63
What problems does Kestra solve in a real AI workflow that a single notebook script doesn't handle well?,"Kestra solves the parts of a real AI workflow that a one-off notebook script usually doesn’t handle well:

- scheduling and running jobs automatically
- retries when something fails
- monitoring and observability
- secret management
- chaining multiple tasks reliably
- separating workflow logic from the Python code itself

So a notebook is fine for learning and experiments, but Kestra is useful when you need a production-style workflow that runs dependably over time.","Notebooks are great for learning and experimenting, but real AI workflows need more than a script that runs once: scheduling, retries, monitoring, secret management, and reliably chaining tasks together. That's what an orchestrator like Kestra provides.

In this module Kestra is also the vehicle for the AI techniques the course is teaching: AI Copilot to generate flows from natural language, RAG to ground responses in real data, and AI agents that decide which tools to call. The goal is to see how AI fits into production-style workflows, not just notebook cells.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else. See the [module intro](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/01-intro.md) for the full motivation.",193612db63
"In this module, is Kestra mainly for orchestration, or is it also used to demonstrate AI copilot, RAG, and agents?","Kestra is mainly the **orchestration** tool in this module, but it is **also used to demonstrate the AI techniques** the course is teaching: **AI Copilot, RAG, and agents**. The idea is to show how these AI features fit into production-style workflows, not just notebook cells.","Notebooks are great for learning and experimenting, but real AI workflows need more than a script that runs once: scheduling, retries, monitoring, secret management, and reliably chaining tasks together. That's what an orchestrator like Kestra provides.

In this module Kestra is also the vehicle for the AI techniques the course is teaching: AI Copilot to generate flows from natural language, RAG to ground responses in real data, and AI agents that decide which tools to call. The goal is to see how AI fits into production-style workflows, not just notebook cells.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else. See the [module intro](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/01-intro.md) for the full motivation.",193612db63
"Can I switch between OpenAI, Gemini, Anthropic, and other providers in a Kestra flow without rewriting the rest of the workflow?","Yes. Kestra’s AI plugin is provider-agnostic, so you can switch between OpenAI, Gemini, Anthropic, and other supported/OpenAI-compatible providers by changing just the provider block in the flow. You don’t need to rewrite the rest of the workflow.","Notebooks are great for learning and experimenting, but real AI workflows need more than a script that runs once: scheduling, retries, monitoring, secret management, and reliably chaining tasks together. That's what an orchestrator like Kestra provides.

In this module Kestra is also the vehicle for the AI techniques the course is teaching: AI Copilot to generate flows from natural language, RAG to ground responses in real data, and AI agents that decide which tools to call. The goal is to see how AI fits into production-style workflows, not just notebook cells.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else. See the [module intro](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/01-intro.md) for the full motivation.",193612db63
What’s the point of using Kestra here instead of just doing everything in notebook cells during the course?,"The point is that notebook cells are great for learning and experimenting, but Kestra shows how AI workflows are run in a more production-like way.

Kestra gives you things notebooks don’t, like:

- scheduling
- retries
- monitoring
- secret management
- chaining tasks reliably

In Module 3, Kestra is also part of the lesson itself: it’s the platform used to demonstrate AI Copilot, RAG, and AI agents, so you see how those techniques fit into real workflows rather than just isolated notebook code.

So: use notebooks for exploration, but use Kestra to learn orchestration and production-style workflow handling.","Notebooks are great for learning and experimenting, but real AI workflows need more than a script that runs once: scheduling, retries, monitoring, secret management, and reliably chaining tasks together. That's what an orchestrator like Kestra provides.

In this module Kestra is also the vehicle for the AI techniques the course is teaching: AI Copilot to generate flows from natural language, RAG to ground responses in real data, and AI agents that decide which tools to call. The goal is to see how AI fits into production-style workflows, not just notebook cells.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else. See the [module intro](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/01-intro.md) for the full motivation.",193612db63
"Do I actually need to use Kestra for the whole LLM Zoomcamp, or is it only for Module 3?","No — you only need Kestra for Module 3.

For the rest of the LLM Zoomcamp, Kestra is not required. For the capstone project, you can use another orchestrator like Airflow, Prefect, Dagster, or even no orchestrator at all.","No. Kestra is the orchestrator the course teaches in Module 3, but it is not a requirement beyond that module's homework.

For the **capstone project** you are not restricted in technology: you can use Airflow, Prefect, Dagster, or no orchestrator at all. A plain Python script that ingests and indexes your data is enough for full points on the ingestion-pipeline criterion (a Jupyter notebook with the same steps is worth 1 point instead of 2). See the [project guidelines](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for details.

The course uses Kestra in Module 3 because it is the vehicle for the AI techniques being taught — AI Copilot for generating flows, RAG for grounding responses, and AI agents that call tools. Other orchestrators cover the same scheduling/retry/monitoring ground, but they won't map onto those specific lessons, so it's worth running the Kestra flows at least once. If you already know Airflow (or another tool) and want to compare, give Kestra a try for the module and then use whatever fits your project best.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else — see the [supported providers list](https://kestra.io/plugins/plugin-ai/provider).",cce328db64
"For the capstone project, can I skip Kestra and use Airflow, Prefect, Dagster, or even just Python?","Yes — for the capstone project you can skip Kestra and use Airflow, Prefect, Dagster, or even just plain Python.

A plain Python script that ingests and indexes your data is enough for full points on the ingestion-pipeline criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it genuinely fits your project.","No. Kestra is the orchestrator the course teaches in Module 3, but it is not a requirement beyond that module's homework.

For the **capstone project** you are not restricted in technology: you can use Airflow, Prefect, Dagster, or no orchestrator at all. A plain Python script that ingests and indexes your data is enough for full points on the ingestion-pipeline criterion (a Jupyter notebook with the same steps is worth 1 point instead of 2). See the [project guidelines](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for details.

The course uses Kestra in Module 3 because it is the vehicle for the AI techniques being taught — AI Copilot for generating flows, RAG for grounding responses, and AI agents that call tools. Other orchestrators cover the same scheduling/retry/monitoring ground, but they won't map onto those specific lessons, so it's worth running the Kestra flows at least once. If you already know Airflow (or another tool) and want to compare, give Kestra a try for the module and then use whatever fits your project best.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else — see the [supported providers list](https://kestra.io/plugins/plugin-ai/provider).",cce328db64
"If I build the ingestion pipeline with a normal script instead of an orchestrator, do I still get full points?","Yes. For the capstone, a plain Python script that ingests and indexes your data is enough for full points on the ingestion-pipeline criterion. You do **not** need an orchestrator just to get full points.

A Jupyter notebook with the same steps is worth 1 point instead of 2.","No. Kestra is the orchestrator the course teaches in Module 3, but it is not a requirement beyond that module's homework.

For the **capstone project** you are not restricted in technology: you can use Airflow, Prefect, Dagster, or no orchestrator at all. A plain Python script that ingests and indexes your data is enough for full points on the ingestion-pipeline criterion (a Jupyter notebook with the same steps is worth 1 point instead of 2). See the [project guidelines](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for details.

The course uses Kestra in Module 3 because it is the vehicle for the AI techniques being taught — AI Copilot for generating flows, RAG for grounding responses, and AI agents that call tools. Other orchestrators cover the same scheduling/retry/monitoring ground, but they won't map onto those specific lessons, so it's worth running the Kestra flows at least once. If you already know Airflow (or another tool) and want to compare, give Kestra a try for the module and then use whatever fits your project best.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else — see the [supported providers list](https://kestra.io/plugins/plugin-ai/provider).",cce328db64
Why does the course teach Kestra in Module 3 if other orchestrators can do the same job?,"The course teaches Kestra in Module 3 because it’s the **vehicle for the lessons**, not because it’s the only orchestrator that can do orchestration.

Specifically, Kestra is used to demonstrate:

- **AI Copilot** for generating flows from natural language
- **RAG** for grounding responses in real data
- **AI agents** that decide which tools to call

Other orchestrators like Airflow, Prefect, or Dagster can handle similar scheduling/retry/monitoring tasks, but they don’t map as directly onto those AI-specific lessons. So the point is to learn how AI fits into production-style workflows using Kestra.

Also, Kestra is **not required beyond Module 3 homework**. For the capstone project, you can use any orchestrator or none at all.","No. Kestra is the orchestrator the course teaches in Module 3, but it is not a requirement beyond that module's homework.

For the **capstone project** you are not restricted in technology: you can use Airflow, Prefect, Dagster, or no orchestrator at all. A plain Python script that ingests and indexes your data is enough for full points on the ingestion-pipeline criterion (a Jupyter notebook with the same steps is worth 1 point instead of 2). See the [project guidelines](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for details.

The course uses Kestra in Module 3 because it is the vehicle for the AI techniques being taught — AI Copilot for generating flows, RAG for grounding responses, and AI agents that call tools. Other orchestrators cover the same scheduling/retry/monitoring ground, but they won't map onto those specific lessons, so it's worth running the Kestra flows at least once. If you already know Airflow (or another tool) and want to compare, give Kestra a try for the module and then use whatever fits your project best.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else — see the [supported providers list](https://kestra.io/plugins/plugin-ai/provider).",cce328db64
"Can I switch the AI provider in a Kestra flow, like from OpenAI to Gemini or Anthropic, without rewriting the flow?","Yes. Kestra’s AI plugins are provider-agnostic, so you can switch from OpenAI to Gemini, Anthropic, or another supported provider by changing the provider block in the flow, without rewriting the rest of the flow.","No. Kestra is the orchestrator the course teaches in Module 3, but it is not a requirement beyond that module's homework.

For the **capstone project** you are not restricted in technology: you can use Airflow, Prefect, Dagster, or no orchestrator at all. A plain Python script that ingests and indexes your data is enough for full points on the ingestion-pipeline criterion (a Jupyter notebook with the same steps is worth 1 point instead of 2). See the [project guidelines](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for details.

The course uses Kestra in Module 3 because it is the vehicle for the AI techniques being taught — AI Copilot for generating flows, RAG for grounding responses, and AI agents that call tools. Other orchestrators cover the same scheduling/retry/monitoring ground, but they won't map onto those specific lessons, so it's worth running the Kestra flows at least once. If you already know Airflow (or another tool) and want to compare, give Kestra a try for the module and then use whatever fits your project best.

Kestra's AI plugins also work with any major provider (OpenAI, Gemini, Anthropic, and more), so you can swap providers in a flow without changing anything else — see the [supported providers list](https://kestra.io/plugins/plugin-ai/provider).",cce328db64
"Does Kestra work only with Gemini, or can I plug in OpenAI, Anthropic, xAI, Groq, or even a local model like Ollama?","No — Kestra is not limited to Gemini.

Kestra’s AI plugin is provider-agnostic, so you can use:
- OpenAI
- Anthropic
- xAI / Grok
- Groq
- any OpenAI-compatible provider
- local models like Ollama or LM Studio

For OpenAI-compatible providers, you typically just change the provider block or the `base_url`, API key, and model name.","Kestra's AI plugin is provider-agnostic: it supports OpenAI, Gemini, Anthropic, xAI, Grok, and any OpenAI-compatible provider, including local models served through Ollama or LM Studio. You swap the provider block in a flow without changing anything else. See the [full list of supported providers](https://kestra.io/plugins/plugin-ai/provider).

The course uses Gemini because it has a generous free tier, but you are free to use any provider. The [awesome-llms list](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo tracks free and free-tier options; Groq is a popular choice because it is OpenAI-compatible and works with both the chat completions and responses APIs.

There are two ways to use AI in Kestra:

- The **AI plugin** (`io.kestra.plugin.ai`) is the generic one. It is the most flexible for switching providers, though new vendor-specific API features take a bit longer to land here.
- The **provider-specific plugins** (e.g. `plugin-gemini`, `plugin-openai`) expose features unique to that vendor, such as Gemini video generation, before they reach the generic AI plugin.

For OpenAI-compatible providers that don't have their own plugin (DeepSeek, Groq, xAI), point the OpenAI plugin at the provider's base URL instead of the default endpoint.",f442efe5b4
"If I switch from Gemini to another AI provider in a Kestra flow, do I need to rewrite the rest of the flow or just change the provider block?","Just change the provider block.

In Kestra, the AI plugin is provider-agnostic, so you can swap from Gemini to OpenAI, Anthropic, Groq, Ollama, or another compatible provider without rewriting the rest of the flow.","Kestra's AI plugin is provider-agnostic: it supports OpenAI, Gemini, Anthropic, xAI, Grok, and any OpenAI-compatible provider, including local models served through Ollama or LM Studio. You swap the provider block in a flow without changing anything else. See the [full list of supported providers](https://kestra.io/plugins/plugin-ai/provider).

The course uses Gemini because it has a generous free tier, but you are free to use any provider. The [awesome-llms list](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo tracks free and free-tier options; Groq is a popular choice because it is OpenAI-compatible and works with both the chat completions and responses APIs.

There are two ways to use AI in Kestra:

- The **AI plugin** (`io.kestra.plugin.ai`) is the generic one. It is the most flexible for switching providers, though new vendor-specific API features take a bit longer to land here.
- The **provider-specific plugins** (e.g. `plugin-gemini`, `plugin-openai`) expose features unique to that vendor, such as Gemini video generation, before they reach the generic AI plugin.

For OpenAI-compatible providers that don't have their own plugin (DeepSeek, Groq, xAI), point the OpenAI plugin at the provider's base URL instead of the default endpoint.",f442efe5b4
"How do I connect Kestra to an OpenAI-compatible service such as Groq, DeepSeek, or a local LM Studio/Ollama model?","Use Kestra’s **OpenAI-compatible** setup and point it to the provider’s **base URL** instead of OpenAI’s default endpoint.

In short:

- **Groq / DeepSeek / xAI / other OpenAI-compatible services:** use the **OpenAI plugin** and change:
  - `base_url`
  - API key
  - model name

- **LM Studio / Ollama local models:** make sure the local server exposes an **OpenAI-compatible endpoint**, then point Kestra to `localhost` (or your machine’s IP) with the right base URL.

The context says Kestra’s AI plugin is **provider-agnostic** and supports **any OpenAI-compatible provider**, including **Ollama** and **LM Studio**. For providers without their own plugin, you just swap the provider block in the flow; for OpenAI-compatible providers, point the OpenAI plugin at the provider’s base URL instead of the default endpoint.

If you want, I can also show you a concrete Kestra YAML example for Groq or Ollama.","Kestra's AI plugin is provider-agnostic: it supports OpenAI, Gemini, Anthropic, xAI, Grok, and any OpenAI-compatible provider, including local models served through Ollama or LM Studio. You swap the provider block in a flow without changing anything else. See the [full list of supported providers](https://kestra.io/plugins/plugin-ai/provider).

The course uses Gemini because it has a generous free tier, but you are free to use any provider. The [awesome-llms list](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo tracks free and free-tier options; Groq is a popular choice because it is OpenAI-compatible and works with both the chat completions and responses APIs.

There are two ways to use AI in Kestra:

- The **AI plugin** (`io.kestra.plugin.ai`) is the generic one. It is the most flexible for switching providers, though new vendor-specific API features take a bit longer to land here.
- The **provider-specific plugins** (e.g. `plugin-gemini`, `plugin-openai`) expose features unique to that vendor, such as Gemini video generation, before they reach the generic AI plugin.

For OpenAI-compatible providers that don't have their own plugin (DeepSeek, Groq, xAI), point the OpenAI plugin at the provider's base URL instead of the default endpoint.",f442efe5b4
What's the difference between Kestra's generic AI plugin and the vendor-specific plugins like plugin-gemini or plugin-openai?,"The generic Kestra AI plugin (`io.kestra.plugin.ai`) is provider-agnostic, so it’s the most flexible if you want to switch between providers like OpenAI, Gemini, Anthropic, xAI, Grok, or other OpenAI-compatible services without changing the rest of your flow.

The vendor-specific plugins, like `plugin-gemini` or `plugin-openai`, are useful when you want access to provider-only features sooner, such as Gemini video generation, before those features appear in the generic AI plugin.

In short:
- **Generic AI plugin** = best for portability and easy provider switching
- **Vendor-specific plugins** = best for vendor-specific features

","Kestra's AI plugin is provider-agnostic: it supports OpenAI, Gemini, Anthropic, xAI, Grok, and any OpenAI-compatible provider, including local models served through Ollama or LM Studio. You swap the provider block in a flow without changing anything else. See the [full list of supported providers](https://kestra.io/plugins/plugin-ai/provider).

The course uses Gemini because it has a generous free tier, but you are free to use any provider. The [awesome-llms list](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo tracks free and free-tier options; Groq is a popular choice because it is OpenAI-compatible and works with both the chat completions and responses APIs.

There are two ways to use AI in Kestra:

- The **AI plugin** (`io.kestra.plugin.ai`) is the generic one. It is the most flexible for switching providers, though new vendor-specific API features take a bit longer to land here.
- The **provider-specific plugins** (e.g. `plugin-gemini`, `plugin-openai`) expose features unique to that vendor, such as Gemini video generation, before they reach the generic AI plugin.

For OpenAI-compatible providers that don't have their own plugin (DeepSeek, Groq, xAI), point the OpenAI plugin at the provider's base URL instead of the default endpoint.",f442efe5b4
"Why does the course use Gemini by default, and where can I find other free or free-tier model options for the lab?","The course uses **Gemini by default because it has a generous free tier**.

For other free or free-tier options for the lab, check the **`awesome-llms.md`** list in the course repo:
- https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md

That list tracks free and free-tier providers. A popular choice mentioned in the course is **Groq**, since it’s **OpenAI-compatible** and works with both the **chat completions** and **responses** APIs.

You can also use other providers supported by Kestra, including local models via **Ollama** or **LM Studio**.","Kestra's AI plugin is provider-agnostic: it supports OpenAI, Gemini, Anthropic, xAI, Grok, and any OpenAI-compatible provider, including local models served through Ollama or LM Studio. You swap the provider block in a flow without changing anything else. See the [full list of supported providers](https://kestra.io/plugins/plugin-ai/provider).

The course uses Gemini because it has a generous free tier, but you are free to use any provider. The [awesome-llms list](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/awesome-llms.md) in the course repo tracks free and free-tier options; Groq is a popular choice because it is OpenAI-compatible and works with both the chat completions and responses APIs.

There are two ways to use AI in Kestra:

- The **AI plugin** (`io.kestra.plugin.ai`) is the generic one. It is the most flexible for switching providers, though new vendor-specific API features take a bit longer to land here.
- The **provider-specific plugins** (e.g. `plugin-gemini`, `plugin-openai`) expose features unique to that vendor, such as Gemini video generation, before they reach the generic AI plugin.

For OpenAI-compatible providers that don't have their own plugin (DeepSeek, Groq, xAI), point the OpenAI plugin at the provider's base URL instead of the default endpoint.",f442efe5b4
"Can I build a Kestra flow without writing YAML by hand, or do I still need to code it myself?","Yes — you can build a Kestra flow without writing YAML by hand.

You have three no-code / low-code options:

- **No-code form editor**: build the flow from a form instead of editing YAML directly.
- **AI Copilot**: describe the flow in natural language and let Kestra generate it.
- **External agent skills**: tools like Claude Code, Codex, or Kestra CTL can generate and push flows from your editor.

So you do **not** need to code it yourself from scratch if you don’t want to.","Yes. You have three options that require no hand-written YAML or code:

- **The no-code form editor** builds a flow from a form instead of YAML. It is form-based (not drag-and-drop like n8n), so it suits people who don't want to edit YAML directly.
- **The AI Copilot** generates a flow from a natural-language description, and you can iterate with follow-up messages to add tasks or change the order. See the [AI Copilot lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/04-ai-copilot.md).
- **External agent skills** (Claude Code, Codex, Kestra CTL) let you generate flows and push them into Kestra from your editor without touching the Kestra UI.

Unlike Airflow, where business logic and orchestration logic are intertwined in Python, Kestra keeps the two separate: your Python script stays a plain script, and the workflow YAML just describes how and when to run it. This is closer to a cron job than to a traditional Airflow DAG.

For simple point-to-point automations (e.g. ""post a Slack message when a YouTube video goes live""), a tool like Zapier or n8n may be simpler and cheaper. Kestra's advantage is observability, retries, and handling more complex or technical workflows in one place.",bfe5502c30
Is there a form-based no-code way to create workflows in Kestra instead of using drag-and-drop or editing YAML?,"Yes. Kestra has a **no-code form editor** that lets you build a flow from a form instead of editing YAML. It’s **form-based**, not drag-and-drop.","Yes. You have three options that require no hand-written YAML or code:

- **The no-code form editor** builds a flow from a form instead of YAML. It is form-based (not drag-and-drop like n8n), so it suits people who don't want to edit YAML directly.
- **The AI Copilot** generates a flow from a natural-language description, and you can iterate with follow-up messages to add tasks or change the order. See the [AI Copilot lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/04-ai-copilot.md).
- **External agent skills** (Claude Code, Codex, Kestra CTL) let you generate flows and push them into Kestra from your editor without touching the Kestra UI.

Unlike Airflow, where business logic and orchestration logic are intertwined in Python, Kestra keeps the two separate: your Python script stays a plain script, and the workflow YAML just describes how and when to run it. This is closer to a cron job than to a traditional Airflow DAG.

For simple point-to-point automations (e.g. ""post a Slack message when a YouTube video goes live""), a tool like Zapier or n8n may be simpler and cheaper. Kestra's advantage is observability, retries, and handling more complex or technical workflows in one place.",bfe5502c30
"How does the AI Copilot in Kestra help create a flow from plain English, and can I keep editing it after that?","Yes. In Kestra, the AI Copilot can turn a plain-English description into a Kestra flow, and then you can keep iterating on it with follow-up messages to add tasks or change the order.

So the workflow is:
1. Describe what you want in natural language.
2. Copilot generates the flow YAML.
3. You continue editing it afterward, either by asking the Copilot to modify it or by editing the flow yourself.

","Yes. You have three options that require no hand-written YAML or code:

- **The no-code form editor** builds a flow from a form instead of YAML. It is form-based (not drag-and-drop like n8n), so it suits people who don't want to edit YAML directly.
- **The AI Copilot** generates a flow from a natural-language description, and you can iterate with follow-up messages to add tasks or change the order. See the [AI Copilot lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/04-ai-copilot.md).
- **External agent skills** (Claude Code, Codex, Kestra CTL) let you generate flows and push them into Kestra from your editor without touching the Kestra UI.

Unlike Airflow, where business logic and orchestration logic are intertwined in Python, Kestra keeps the two separate: your Python script stays a plain script, and the workflow YAML just describes how and when to run it. This is closer to a cron job than to a traditional Airflow DAG.

For simple point-to-point automations (e.g. ""post a Slack message when a YouTube video goes live""), a tool like Zapier or n8n may be simpler and cheaper. Kestra's advantage is observability, retries, and handling more complex or technical workflows in one place.",bfe5502c30
"Can I use tools like Claude Code, Codex, or Kestra CTL to generate a workflow and send it to Kestra directly from my editor?","Yes. You can use external agent skills like **Claude Code, Codex, or Kestra CTL** to generate a workflow and push it into **Kestra directly from your editor**, without touching the Kestra UI.","Yes. You have three options that require no hand-written YAML or code:

- **The no-code form editor** builds a flow from a form instead of YAML. It is form-based (not drag-and-drop like n8n), so it suits people who don't want to edit YAML directly.
- **The AI Copilot** generates a flow from a natural-language description, and you can iterate with follow-up messages to add tasks or change the order. See the [AI Copilot lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/04-ai-copilot.md).
- **External agent skills** (Claude Code, Codex, Kestra CTL) let you generate flows and push them into Kestra from your editor without touching the Kestra UI.

Unlike Airflow, where business logic and orchestration logic are intertwined in Python, Kestra keeps the two separate: your Python script stays a plain script, and the workflow YAML just describes how and when to run it. This is closer to a cron job than to a traditional Airflow DAG.

For simple point-to-point automations (e.g. ""post a Slack message when a YouTube video goes live""), a tool like Zapier or n8n may be simpler and cheaper. Kestra's advantage is observability, retries, and handling more complex or technical workflows in one place.",bfe5502c30
"If I only need a simple automation like sending a Slack message when a YouTube video is published, should I use Kestra or something like Zapier or n8n?","For a simple one-off automation like “send a Slack message when a YouTube video is published,” **Zapier or n8n is probably simpler and cheaper**.

Kestra is better when you want:
- stronger **observability**
- **retries**
- handling **more complex or technical workflows**
- keeping orchestration in one place

So for that specific use case, I’d lean toward **Zapier/n8n**. Use **Kestra** if you want to learn the course tool or expect the workflow to grow more complex.","Yes. You have three options that require no hand-written YAML or code:

- **The no-code form editor** builds a flow from a form instead of YAML. It is form-based (not drag-and-drop like n8n), so it suits people who don't want to edit YAML directly.
- **The AI Copilot** generates a flow from a natural-language description, and you can iterate with follow-up messages to add tasks or change the order. See the [AI Copilot lesson](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/03-orchestration/lessons/04-ai-copilot.md).
- **External agent skills** (Claude Code, Codex, Kestra CTL) let you generate flows and push them into Kestra from your editor without touching the Kestra UI.

Unlike Airflow, where business logic and orchestration logic are intertwined in Python, Kestra keeps the two separate: your Python script stays a plain script, and the workflow YAML just describes how and when to run it. This is closer to a cron job than to a traditional Airflow DAG.

For simple point-to-point automations (e.g. ""post a Slack message when a YouTube video goes live""), a tool like Zapier or n8n may be simpler and cheaper. Kestra's advantage is observability, retries, and handling more complex or technical workflows in one place.",bfe5502c30
"Why does ChatGPT sometimes invent Kestra plugin fields like `bucket` or `name` when I ask for a flow, and how do I stop that from happening?","ChatGPT invents fields because it’s not grounded in the **actual Kestra plugin schema for your running version**. So it may produce plausible but invalid properties like `bucket` or `name` instead of the real ones.

How to stop it:

- **Cross-check the YAML against the official plugin docs**
  - For GCP plugins: https://kestra.io/plugins/plugin-gcp
- **Use Kestra’s built-in AI Copilot**
  - It’s grounded in the current schema for your Kestra version.
- **Validate the flow in your Kestra environment**
  - That catches invalid fields quickly.

Also, plugin fields can vary by **plugin version**, so a property that works in one release may be wrong in another.

Examples:
- GCS Upload: invented `bucket` + `name` vs correct `to`
- BigQuery LoadFromGcs: invented `projectId` + `dataset` + `table` vs correct `destinationTable`

If you want, paste the flow YAML and I can help check it against the docs.","A general AI (e.g., ChatGPT) isn't grounded in Kestra's plugin schemas for your running version, so it can surface plausible but invalid property names (such as `bucket`/`name`) instead of the real, version-specific property. For example:

**GCS Upload** — invented `bucket`/`name` props vs. the real `to` property:

```yaml
# Incorrect (invented props)
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  bucket: ""my-bucket""
  name: ""path/to/file""

# Correct
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  to: ""gs://my-bucket/path/to/file""
```

**BigQuery LoadFromGcs** — split `projectId`/`dataset`/`table` vs. the real `destinationTable`:

```yaml
# Incorrect (pseudo-split properties)
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  projectId: ""my-project""
  dataset: ""my_dataset""
  table: ""my_table""

# Correct
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  destinationTable: ""my-project.my_dataset.my_table""
```

To avoid this:

- Cross-check generated YAML against the official plugin docs: https://kestra.io/plugins/plugin-gcp
- Use Kestra's built-in AI Copilot, which is grounded in the current plugin schema for your running version.
- Validate and test your YAML in your Kestra environment to ensure it parses and runs as expected.

Note that plugin properties can vary by plugin version; what's correct in one release may be invalid in another. If you share a snippet, we can help verify it against the docs.",93a3e8b98c
How can I check that a Kestra YAML generated by an AI matches the actual plugin schema for my version?,"Check it against the **official plugin docs for your Kestra version** and then **validate/test it in your Kestra environment**.

In practice:
- Compare the generated YAML with the plugin documentation: https://kestra.io/plugins/plugin-gcp
- Use Kestra’s built-in **AI Copilot**, which is grounded in the current plugin schema for your running version
- Run the flow in Kestra to ensure it **parses and executes correctly**

This matters because plugin properties can differ by version, so an AI may generate valid-looking but incorrect fields.","A general AI (e.g., ChatGPT) isn't grounded in Kestra's plugin schemas for your running version, so it can surface plausible but invalid property names (such as `bucket`/`name`) instead of the real, version-specific property. For example:

**GCS Upload** — invented `bucket`/`name` props vs. the real `to` property:

```yaml
# Incorrect (invented props)
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  bucket: ""my-bucket""
  name: ""path/to/file""

# Correct
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  to: ""gs://my-bucket/path/to/file""
```

**BigQuery LoadFromGcs** — split `projectId`/`dataset`/`table` vs. the real `destinationTable`:

```yaml
# Incorrect (pseudo-split properties)
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  projectId: ""my-project""
  dataset: ""my_dataset""
  table: ""my_table""

# Correct
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  destinationTable: ""my-project.my_dataset.my_table""
```

To avoid this:

- Cross-check generated YAML against the official plugin docs: https://kestra.io/plugins/plugin-gcp
- Use Kestra's built-in AI Copilot, which is grounded in the current plugin schema for your running version.
- Validate and test your YAML in your Kestra environment to ensure it parses and runs as expected.

Note that plugin properties can vary by plugin version; what's correct in one release may be invalid in another. If you share a snippet, we can help verify it against the docs.",93a3e8b98c
"For the GCS upload plugin, why is `to` the right field instead of separate `bucket` and `name` values?","Because the plugin expects the destination as a single GCS URI, not as separate parts. In the context example, `to` is the real schema field for `io.kestra.plugin.gcp.gcs.Upload`, and it takes the full path:

```yaml
to: ""gs://my-bucket/path/to/file""
```

So `bucket` and `name` are just invented, split-up properties and don’t exist for that plugin/version.","A general AI (e.g., ChatGPT) isn't grounded in Kestra's plugin schemas for your running version, so it can surface plausible but invalid property names (such as `bucket`/`name`) instead of the real, version-specific property. For example:

**GCS Upload** — invented `bucket`/`name` props vs. the real `to` property:

```yaml
# Incorrect (invented props)
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  bucket: ""my-bucket""
  name: ""path/to/file""

# Correct
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  to: ""gs://my-bucket/path/to/file""
```

**BigQuery LoadFromGcs** — split `projectId`/`dataset`/`table` vs. the real `destinationTable`:

```yaml
# Incorrect (pseudo-split properties)
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  projectId: ""my-project""
  dataset: ""my_dataset""
  table: ""my_table""

# Correct
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  destinationTable: ""my-project.my_dataset.my_table""
```

To avoid this:

- Cross-check generated YAML against the official plugin docs: https://kestra.io/plugins/plugin-gcp
- Use Kestra's built-in AI Copilot, which is grounded in the current plugin schema for your running version.
- Validate and test your YAML in your Kestra environment to ensure it parses and runs as expected.

Note that plugin properties can vary by plugin version; what's correct in one release may be invalid in another. If you share a snippet, we can help verify it against the docs.",93a3e8b98c
"In the BigQuery load task, why do I need `destinationTable` instead of `projectId`, `dataset`, and `table` as separate properties?","Use `destinationTable` because that is the actual property expected by the `io.kestra.plugin.gcp.bigquery.LoadFromGcs` plugin. The plugin does not take separate `projectId`, `dataset`, and `table` fields for this task.

Correct example:

```yaml
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  destinationTable: ""my-project.my_dataset.my_table""
```

The split properties are just a plausible guess, but not the real schema.","A general AI (e.g., ChatGPT) isn't grounded in Kestra's plugin schemas for your running version, so it can surface plausible but invalid property names (such as `bucket`/`name`) instead of the real, version-specific property. For example:

**GCS Upload** — invented `bucket`/`name` props vs. the real `to` property:

```yaml
# Incorrect (invented props)
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  bucket: ""my-bucket""
  name: ""path/to/file""

# Correct
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  to: ""gs://my-bucket/path/to/file""
```

**BigQuery LoadFromGcs** — split `projectId`/`dataset`/`table` vs. the real `destinationTable`:

```yaml
# Incorrect (pseudo-split properties)
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  projectId: ""my-project""
  dataset: ""my_dataset""
  table: ""my_table""

# Correct
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  destinationTable: ""my-project.my_dataset.my_table""
```

To avoid this:

- Cross-check generated YAML against the official plugin docs: https://kestra.io/plugins/plugin-gcp
- Use Kestra's built-in AI Copilot, which is grounded in the current plugin schema for your running version.
- Validate and test your YAML in your Kestra environment to ensure it parses and runs as expected.

Note that plugin properties can vary by plugin version; what's correct in one release may be invalid in another. If you share a snippet, we can help verify it against the docs.",93a3e8b98c
What’s the safest way to use AI-generated Kestra flows without ending up with invalid YAML that only looks correct?,"The safest way is to **not trust the AI output blindly**. Instead:

- **Cross-check the generated YAML against Kestra’s official plugin docs** for your version.
- Prefer **Kestra’s built-in AI Copilot**, since it’s grounded in the current plugin schema.
- **Validate the flow in your Kestra environment** before using it.

Why this matters: generic AI may generate YAML that *looks* correct but uses **invented or version-mismatched properties**. For example, it might write `bucket` and `name` for GCS upload, while the real property is `to`.

If you want, paste the YAML here and I can help verify it against the docs.","A general AI (e.g., ChatGPT) isn't grounded in Kestra's plugin schemas for your running version, so it can surface plausible but invalid property names (such as `bucket`/`name`) instead of the real, version-specific property. For example:

**GCS Upload** — invented `bucket`/`name` props vs. the real `to` property:

```yaml
# Incorrect (invented props)
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  bucket: ""my-bucket""
  name: ""path/to/file""

# Correct
- id: upload_to_gcs
  type: ""io.kestra.plugin.gcp.gcs.Upload""
  to: ""gs://my-bucket/path/to/file""
```

**BigQuery LoadFromGcs** — split `projectId`/`dataset`/`table` vs. the real `destinationTable`:

```yaml
# Incorrect (pseudo-split properties)
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  projectId: ""my-project""
  dataset: ""my_dataset""
  table: ""my_table""

# Correct
- id: load_to_bq
  type: ""io.kestra.plugin.gcp.bigquery.LoadFromGcs""
  destinationTable: ""my-project.my_dataset.my_table""
```

To avoid this:

- Cross-check generated YAML against the official plugin docs: https://kestra.io/plugins/plugin-gcp
- Use Kestra's built-in AI Copilot, which is grounded in the current plugin schema for your running version.
- Validate and test your YAML in your Kestra environment to ensure it parses and runs as expected.

Note that plugin properties can vary by plugin version; what's correct in one release may be invalid in another. If you share a snippet, we can help verify it against the docs.",93a3e8b98c
Why do we bother tracking token usage in Kestra flows when the LLM call already works fine?,"Because the LLM provider bills by tokens, not just by “successful calls.” In Kestra flows, especially scheduled or looping ones, token usage helps you:

- control and forecast cost,
- optimize prompts and outputs,
- set guardrails so a runaway flow doesn’t create a big bill.

So even if the call works fine, tracking tokens tells you what it’s costing and helps you keep it efficient.","Token usage is tracked because it maps directly to what LLM providers charge you for, and Kestra workflows run LLM calls on a schedule or in loops where costs add up fast. The main reasons:

- **Cost control**: tokens are the unit providers bill by, so tracking usage lets you budget, forecast, and spot cost spikes before they get expensive.
- **Prompt optimization**: seeing how many tokens each prompt and response consumes helps you tighten prompts and trim output without losing quality.
- **Guardrails**: in long-running or looping flows, usage monitoring lets you set thresholds and alerts so a runaway task doesn't quietly rack up a large bill.

In practice, each AI task in a Kestra flow can capture usage from the provider's response (for example `usage.total_tokens`), and you can surface those numbers in logs, metrics, or dashboards to keep an eye on spend per run.",a4adc70f41
How does watching token counts help with controlling costs in a scheduled or looping workflow?,"Watching token counts helps you control costs because LLM providers bill by tokens. In a scheduled or looping workflow, each run can add up quickly, so tracking tokens lets you:

- estimate and forecast spend,
- spot cost spikes early,
- optimize prompts and outputs,
- set thresholds or alerts to prevent runaway bills.

So token monitoring is basically a cost-control guardrail for workflows that repeat over time.","Token usage is tracked because it maps directly to what LLM providers charge you for, and Kestra workflows run LLM calls on a schedule or in loops where costs add up fast. The main reasons:

- **Cost control**: tokens are the unit providers bill by, so tracking usage lets you budget, forecast, and spot cost spikes before they get expensive.
- **Prompt optimization**: seeing how many tokens each prompt and response consumes helps you tighten prompts and trim output without losing quality.
- **Guardrails**: in long-running or looping flows, usage monitoring lets you set thresholds and alerts so a runaway task doesn't quietly rack up a large bill.

In practice, each AI task in a Kestra flow can capture usage from the provider's response (for example `usage.total_tokens`), and you can surface those numbers in logs, metrics, or dashboards to keep an eye on spend per run.",a4adc70f41
"Can token usage numbers help me improve my prompts, and if so, how?","Yes. Token usage numbers can help you improve prompts by showing how many tokens your prompt and the model’s response consume.

How to use them:
- **Tighten prompts**: remove unnecessary wording and repetition.
- **Trim outputs**: ask for shorter or more focused responses if the output is too long.
- **Compare versions**: test different prompt phrasings and see which uses fewer tokens without losing quality.
- **Watch costs**: since providers bill by tokens, lower token usage usually means lower cost.

In short, token counts are a useful signal for prompt optimization and cost control.","Token usage is tracked because it maps directly to what LLM providers charge you for, and Kestra workflows run LLM calls on a schedule or in loops where costs add up fast. The main reasons:

- **Cost control**: tokens are the unit providers bill by, so tracking usage lets you budget, forecast, and spot cost spikes before they get expensive.
- **Prompt optimization**: seeing how many tokens each prompt and response consumes helps you tighten prompts and trim output without losing quality.
- **Guardrails**: in long-running or looping flows, usage monitoring lets you set thresholds and alerts so a runaway task doesn't quietly rack up a large bill.

In practice, each AI task in a Kestra flow can capture usage from the provider's response (for example `usage.total_tokens`), and you can surface those numbers in logs, metrics, or dashboards to keep an eye on spend per run.",a4adc70f41
What are the guardrails for stopping a Kestra flow from accidentally running up a huge bill?,"Use **token-usage monitoring as a guardrail**.

In Kestra, you can track token counts from the LLM provider response, for example `usage.total_tokens`, and then:

- set **thresholds/limits**
- create **alerts**
- watch for **runaway loops or long-running flows**

That way, if a flow starts consuming too many tokens, you can stop it before it quietly racks up a large bill.","Token usage is tracked because it maps directly to what LLM providers charge you for, and Kestra workflows run LLM calls on a schedule or in loops where costs add up fast. The main reasons:

- **Cost control**: tokens are the unit providers bill by, so tracking usage lets you budget, forecast, and spot cost spikes before they get expensive.
- **Prompt optimization**: seeing how many tokens each prompt and response consumes helps you tighten prompts and trim output without losing quality.
- **Guardrails**: in long-running or looping flows, usage monitoring lets you set thresholds and alerts so a runaway task doesn't quietly rack up a large bill.

In practice, each AI task in a Kestra flow can capture usage from the provider's response (for example `usage.total_tokens`), and you can surface those numbers in logs, metrics, or dashboards to keep an eye on spend per run.",a4adc70f41
"Where do I get the token usage info from in an AI task response, and how can I show it in logs or dashboards?","Get the token usage from the model’s **response object**. For example, in many OpenAI-compatible responses you can read fields like:

- `response.usage.total_tokens`
- `response.usage.input_tokens`
- `response.usage.prompt_tokens`

It depends on the provider/API style.

To show it in **logs or dashboards**, surface those values from the response and emit them as:

- log lines,
- metrics,
- or dashboard fields

so you can track spend per run.

If the provider exposes a different usage schema, use that provider’s usage fields. If it doesn’t expose usage, use its native tokenizer as an approximation.","Token usage is tracked because it maps directly to what LLM providers charge you for, and Kestra workflows run LLM calls on a schedule or in loops where costs add up fast. The main reasons:

- **Cost control**: tokens are the unit providers bill by, so tracking usage lets you budget, forecast, and spot cost spikes before they get expensive.
- **Prompt optimization**: seeing how many tokens each prompt and response consumes helps you tighten prompts and trim output without losing quality.
- **Guardrails**: in long-running or looping flows, usage monitoring lets you set thresholds and alerts so a runaway task doesn't quietly rack up a large bill.

In practice, each AI task in a Kestra flow can capture usage from the provider's response (for example `usage.total_tokens`), and you can surface those numbers in logs, metrics, or dashboards to keep an eye on spend per run.",a4adc70f41
"Why does Docker Desktop say ""mounts denied"" when I run Kestra, and how can I allow that folder to be mounted?","Docker Desktop says **“mounts denied”** because the folder Kestra is trying to mount is **not shared from your host machine**. This is common with paths like **`/tmp/kestra-wd`**.

To allow it:

1. Open **Docker Desktop**
2. Click the **gear icon** to open **Settings**
3. Go to **Resources > File Sharing**
4. Click **+**
5. Add **`/tmp`** or the specific folder like **`/tmp/kestra-wd`**
6. Click **Apply & restart**

After Docker restarts, run your Kestra command again, e.g. `docker compose up -d`.","This error means Docker Desktop isn't allowed to access the host path the container is trying to mount (commonly `/tmp/kestra-wd` when running Kestra). Add the path to Docker Desktop's file sharing list:

1. Open Docker Desktop.
2. Click the gear icon (Settings) in the top right.
3. Go to **Resources > File Sharing**.
4. Click the **+** (Add) button.
5. Enter `/tmp` (or the specific path like `/tmp/kestra-wd`) and press Enter.
6. Click **Apply & restart**.

Once Docker restarts, rerun `docker compose up -d` to start Kestra.",dd8b4c9fda
What should I do if Docker complains that the host path under /tmp is not shared when starting a container?,"Add `/tmp` to Docker Desktop’s file sharing list:

1. Open **Docker Desktop**.
2. Go to **Settings**.
3. Open **Resources > File Sharing**.
4. Click **+** and add `/tmp` (or the exact path, like `/tmp/kestra-wd`).
5. Click **Apply & restart**.

Then rerun your container command, for example:

```bash
docker compose up -d
```

If you want, I can also help you find the exact path Docker is complaining about.","This error means Docker Desktop isn't allowed to access the host path the container is trying to mount (commonly `/tmp/kestra-wd` when running Kestra). Add the path to Docker Desktop's file sharing list:

1. Open Docker Desktop.
2. Click the gear icon (Settings) in the top right.
3. Go to **Resources > File Sharing**.
4. Click the **+** (Add) button.
5. Enter `/tmp` (or the specific path like `/tmp/kestra-wd`) and press Enter.
6. Click **Apply & restart**.

Once Docker restarts, rerun `docker compose up -d` to start Kestra.",dd8b4c9fda
How do I add /tmp or /tmp/kestra-wd to Docker Desktop so my compose setup works again?,"Open **Docker Desktop** and add the path to file sharing:

1. Click the **gear icon** in the top right to open **Settings**.
2. Go to **Resources > File Sharing**.
3. Click the **+** button.
4. Add **`/tmp`** or the specific path **`/tmp/kestra-wd`**.
5. Press **Enter**.
6. Click **Apply & restart**.

After Docker restarts, run `docker compose up -d` again.","This error means Docker Desktop isn't allowed to access the host path the container is trying to mount (commonly `/tmp/kestra-wd` when running Kestra). Add the path to Docker Desktop's file sharing list:

1. Open Docker Desktop.
2. Click the gear icon (Settings) in the top right.
3. Go to **Resources > File Sharing**.
4. Click the **+** (Add) button.
5. Enter `/tmp` (or the specific path like `/tmp/kestra-wd`) and press Enter.
6. Click **Apply & restart**.

Once Docker restarts, rerun `docker compose up -d` to start Kestra.",dd8b4c9fda
"After getting the ""path is not shared from the host"" error, where in Docker Desktop do I enable file sharing for that directory?","In Docker Desktop, go to:

**Settings (gear icon) → Resources → File Sharing**

Then click **+** and add the directory, such as **`/tmp`** or the specific path like **`/tmp/kestra-wd`**. After that, click **Apply & restart**.","This error means Docker Desktop isn't allowed to access the host path the container is trying to mount (commonly `/tmp/kestra-wd` when running Kestra). Add the path to Docker Desktop's file sharing list:

1. Open Docker Desktop.
2. Click the gear icon (Settings) in the top right.
3. Go to **Resources > File Sharing**.
4. Click the **+** (Add) button.
5. Enter `/tmp` (or the specific path like `/tmp/kestra-wd`) and press Enter.
6. Click **Apply & restart**.

Once Docker restarts, rerun `docker compose up -d` to start Kestra.",dd8b4c9fda
"Once I fix the Docker file-sharing setting, what do I need to restart or rerun to bring Kestra back up?","After fixing the Docker file-sharing setting, **restart Docker Desktop** if prompted, then rerun:

```bash
docker compose up -d
```

That brings Kestra back up.","This error means Docker Desktop isn't allowed to access the host path the container is trying to mount (commonly `/tmp/kestra-wd` when running Kestra). Add the path to Docker Desktop's file sharing list:

1. Open Docker Desktop.
2. Click the gear icon (Settings) in the top right.
3. Go to **Resources > File Sharing**.
4. Click the **+** (Add) button.
5. Enter `/tmp` (or the specific path like `/tmp/kestra-wd`) and press Enter.
6. Click **Apply & restart**.

Once Docker restarts, rerun `docker compose up -d` to start Kestra.",dd8b4c9fda
"For the Module 3 Kestra homework, what exactly am I supposed to upload if I did everything in the Kestra UI and not in a code editor?","Upload the **Kestra flow YAML files** you created or edited for the homework.

Since you did everything in the Kestra UI, you should:

- **download/export the flows from Kestra**
- put the `.yml` / `.yaml` files in a public repo
- commit any extra scripts those flows call, if you used any
- optionally add a short README, but it’s not required

A typical structure would be:

- `module-3/flows/your-flow.yaml`

Then paste the **public repo link** into the submission form.

The **Q1, Q2, etc. answers go directly into the form fields**, not the repo.","The submission form asks for a link to a public repo (GitHub or any code-hosting site), but unlike earlier modules the work happens inside the Kestra UI rather than as code you write from scratch. That leaves people unsure what to put in the repo.

Download the Kestra flows you created or edited for the homework and commit them to the repo. The flow YAML files are the code you submit. If you also wrote or tweaked any scripts the flows call, include those too. A README summarizing your answers is welcome but not required as long as the flow files are there.

Put the flows in a folder (e.g. `module-3/flows/`), make the repo public, and paste the link in the submission form. The form answers (Q1, Q2, etc.) are separate and go directly into the form fields.",f29f622684
"Do I need to submit the actual Kestra flow YAML files, or is a screenshot / form answers enough for homework 3?","You need to submit the actual Kestra flow YAML files in a public repo. A screenshot or form answers alone is not enough for the repo submission.

The form answers (Q1, Q2, etc.) go directly into the submission form fields, but for the repo you should include the downloaded flow YAML files you created or edited in Kestra. If you tweaked any scripts the flows call, include those too. A README is optional.","The submission form asks for a link to a public repo (GitHub or any code-hosting site), but unlike earlier modules the work happens inside the Kestra UI rather than as code you write from scratch. That leaves people unsure what to put in the repo.

Download the Kestra flows you created or edited for the homework and commit them to the repo. The flow YAML files are the code you submit. If you also wrote or tweaked any scripts the flows call, include those too. A README summarizing your answers is welcome but not required as long as the flow files are there.

Put the flows in a folder (e.g. `module-3/flows/`), make the repo public, and paste the link in the submission form. The form answers (Q1, Q2, etc.) are separate and go directly into the form fields.",f29f622684
Where should I put the Kestra homework files in my repo before sending the submission link?,"Put the Kestra flow YAML files in a folder in your repo, for example:

- `module-3/flows/`

Also include any scripts those flows call if you modified them. Then make the repo public and submit the repo link.","The submission form asks for a link to a public repo (GitHub or any code-hosting site), but unlike earlier modules the work happens inside the Kestra UI rather than as code you write from scratch. That leaves people unsure what to put in the repo.

Download the Kestra flows you created or edited for the homework and commit them to the repo. The flow YAML files are the code you submit. If you also wrote or tweaked any scripts the flows call, include those too. A README summarizing your answers is welcome but not required as long as the flow files are there.

Put the flows in a folder (e.g. `module-3/flows/`), make the repo public, and paste the link in the submission form. The form answers (Q1, Q2, etc.) are separate and go directly into the form fields.",f29f622684
"If my Kestra flows call any scripts, should those scripts be included in the repo too?","Yes — if your Kestra flows call any scripts, those scripts should be included in the repo too. The flow YAML files are required, and any scripts the flows use should be committed alongside them.","The submission form asks for a link to a public repo (GitHub or any code-hosting site), but unlike earlier modules the work happens inside the Kestra UI rather than as code you write from scratch. That leaves people unsure what to put in the repo.

Download the Kestra flows you created or edited for the homework and commit them to the repo. The flow YAML files are the code you submit. If you also wrote or tweaked any scripts the flows call, include those too. A README summarizing your answers is welcome but not required as long as the flow files are there.

Put the flows in a folder (e.g. `module-3/flows/`), make the repo public, and paste the link in the submission form. The form answers (Q1, Q2, etc.) are separate and go directly into the form fields.",f29f622684
"Does the homework repo need a README, or can I just make the flow files public and submit the link?","Yes — a README is **not required**.

You can just make the repo public and submit the link, as long as the **flow YAML files** are included in the repo. A README is welcome, but optional.","The submission form asks for a link to a public repo (GitHub or any code-hosting site), but unlike earlier modules the work happens inside the Kestra UI rather than as code you write from scratch. That leaves people unsure what to put in the repo.

Download the Kestra flows you created or edited for the homework and commit them to the repo. The flow YAML files are the code you submit. If you also wrote or tweaked any scripts the flows call, include those too. A README summarizing your answers is welcome but not required as long as the flow files are there.

Put the flows in a folder (e.g. `module-3/flows/`), make the repo public, and paste the link in the submission form. The form answers (Q1, Q2, etc.) are separate and go directly into the form fields.",f29f622684
Why do I get `JSONDecodeError: Expecting value` when I call `json.loads()` on the LLM response?,"The LLM is probably returning **something that is not raw JSON** — for example:

- a markdown code fence like ```json ... ```
- extra explanation text before/after the JSON

So `json.loads(response)` fails immediately with `JSONDecodeError: Expecting value` because the string doesn’t start with valid JSON.

To fix it, force the model to output JSON only, for example with OpenAI:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

And make the prompt explicit, e.g.:

```text
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

I don't know if you’re using a provider without JSON mode, but most providers have an equivalent.","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
How can I make the model return only valid JSON when generating ground-truth questions?,"Use the API’s JSON mode and make the prompt explicit.

Example with OpenAI:

```python
response = openai_client.chat.completions.create(
    model=""gpt-4o-mini"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also tell the model exactly what to return, for example:

```text
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

If you’re using another provider, use its equivalent JSON response setting, like Gemini’s `response_mime_type=""application/json""`.","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
Is there a way to force OpenAI chat completions to output a JSON object instead of prose or a code block?,"Yes. Use `response_format={""type"": ""json_object""}` with `chat.completions.create(...)` so the model returns JSON instead of prose or a fenced code block.

Example:

```python
response = openai_client.chat.completions.create(
    model=""gpt-4o-mini"",
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)

parsed = json.loads(response.choices[0].message.content)
```

It also helps to be explicit in the prompt, for example:

> Output a JSON object with a single key `""questions""` whose value is a list of 5 strings.  
> Do not include any extra text, explanation, or formatting.

Some providers have similar options too, like Gemini’s `response_mime_type=""application/json""`.","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
What should I put in the prompt so the LLM gives me exactly a `questions` list with 5 strings?,"Output a JSON object with a single key `""questions""` whose value is a list of 5 strings. Do not include any extra text, explanation, or formatting.","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
"Does this JSON-output fix work only with OpenAI, or do other providers have something similar?","No — it’s not OpenAI-only.

The context says most providers have an equivalent, for example:
- Gemini: `response_mime_type=""application/json""`
- Groq: `response_format`
- and other OpenAI-compatible providers often support similar JSON output controls.

Also, the prompt should still explicitly request the JSON shape to reduce malformed output.","The LLM sometimes wraps the JSON in a markdown code fence or adds prose around it, so `json.loads(response)` fails with:

```
JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```

Force JSON output with OpenAI's `response_format`:

```python
response = openai_client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[{""role"": ""user"", ""content"": prompt}],
    response_format={""type"": ""json_object""},
)
parsed = json.loads(response.choices[0].message.content)
```

Also be explicit in the prompt about the expected shape:

```
Output a JSON object with a single key ""questions"" whose value is a list of 5 strings.
Do not include any extra text, explanation, or formatting.
```

Most providers have an equivalent (Gemini's `response_mime_type=""application/json""`, Groq's `response_format`, etc.).",a3c7e1b4f9
Why does my Jupyter kernel keep crashing when I try to embed the ground-truth set in the evaluation notebook?,"Small-RAM machines can run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Common fixes:
- Use a smaller embedder like `sentence-transformers/all-MiniLM-L6-v2`
- Move embedding to a separate Python script and load the saved vectors back into the notebook
- Use a machine type with more RAM, or run locally
- Embed the ground-truth set in batches and free memory between batches with `del` and `gc.collect()`

If you want, I can help you choose the best workaround for your setup.","Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
Is there a lighter embedding model I can use instead of the default one so my notebook doesn’t run out of memory?,"Yes — a lighter option is `sentence-transformers/all-MiniLM-L6-v2` (384-dim). It’s a common drop-in replacement and should use less memory.

Just note that changing the embedding model will change your evaluation numbers, so you should re-run the eval after switching.","Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
"If my laptop or Codespaces VM has low RAM, what’s the best way to handle the embedding step without crashing the notebook?","Use a smaller embedder or move the embedding work out of the notebook.

Best options for low-RAM machines:
- Switch to `sentence-transformers/all-MiniLM-L6-v2` (384-dim).  
  Re-run the evaluation afterward because metrics like hit-rate/MRR will change.
- Run the embedding step in a separate Python script from the terminal, save the vectors, then load them back into the notebook.
- If possible, use a Codespaces machine type with more RAM or run locally.
- Process the ground-truth set in batches and free memory between batches with `del` and `gc.collect()`.

If you want the safest approach to avoid notebook crashes, the separate-script approach is usually best.","Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
Can I do the ground-truth embeddings in a separate Python script and then bring the saved vectors back into Jupyter?,"Yes — you can do the ground-truth embedding step in a separate Python script, run it from the terminal, and then load the saved vectors back into Jupyter.","Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
"Does processing the eval set in batches help with memory issues, and do I need to rerun the evaluation metrics after changing the embedder?","Yes — processing the ground-truth/eval set in batches can help with memory issues, especially on small-RAM machines. The context also suggests freeing memory between batches with `del` and `gc.collect()`.

And yes, if you change the embedder, you need to rerun the evaluation metrics, because switching models will change your hit-rate / MRR numbers.","Small-RAM machines (Codespaces default, low-end laptops) run out of memory when an embedding model is loaded alongside the rest of the notebook state.

Workarounds:

- Switch to a smaller embedder. `sentence-transformers/all-MiniLM-L6-v2` (384-dim) is a common drop-in. Note: switching models will change your hit-rate / MRR numbers, so re-run the eval after the switch.
- Move the embedding step into a separate Python script that you run from the terminal, then load the saved vectors back into the notebook.
- Use a Codespaces machine type with more RAM (Settings → ""Machine type"" on a Codespace), or run locally.
- Process the ground-truth set in batches and free memory between batches (`del`, `gc.collect()`).",b8f2d6a30c
Why am I still getting rate-limited in Gemini even after adding a 4-second sleep between requests?,"You’re probably still hitting the **per-day Gemini quota**. The `time.sleep(4)` only helps with the **per-minute** limit, but a long loop can still exhaust the **daily** request allowance in a single run.

Possible fixes:
- Use a different model with higher limits, like `gpt-4o-mini` on OpenAI
- Use Groq’s free tier
- Lower your concurrency if you’re using a thread pool
- Save progress and resume instead of rerunning from scratch","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
What should I do if my long tqdm loop keeps failing on the free Gemini tier because of quota limits?,"If your long `tqdm` loop keeps failing on the free Gemini tier, it’s likely because you’re hitting the **per-day quota**, not just the per-minute limit.

What to do:
- **Save progress periodically** so you can resume from where you stopped, e.g. dump partial results to a JSONL file.
- **Lower concurrency** if you’re using a thread pool — use something like **2–3 workers**.
- **Switch providers**:
  - use **Groq’s free tier** (`llama-3.3-70b-versatile`), which has generous request-per-minute limits, or
  - spend about **$5 on OpenAI** and use **`gpt-4o-mini`**, which is cheap and has higher rate limits.

Also, `time.sleep(4)` only helps with the **per-minute** limit; it won’t prevent hitting the **per-day** quota in one long run.","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
Is there a cheaper or easier API option for generating the whole ground-truth dataset instead of Gemini free tier?,"Yes. The context suggests a few easier/cheaper alternatives to Gemini free tier for generating the whole ground-truth dataset:

- **OpenAI `gpt-4o-mini`**: around **$5** should be enough, and it has **higher rate limits**.
- **Groq free tier** with **`llama-3.3-70b-versatile`**: generous request-per-minute limits and OpenAI-compatible.
- Also, you can **lower concurrency** and **resume from saved progress** to make generation easier on any provider.

So the short answer is: **yes — `gpt-4o-mini` or Groq are the recommended alternatives.**","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
How can I reduce rate-limit problems when using a thread pool to call the model in parallel?,"Use a **smaller pool size** so you don’t overwhelm the API. The context recommends keeping the thread/process pool to about **2–3 workers** instead of using a large pool, because a larger pool can quickly hit the per-minute limit.

Other helpful fixes from the context:
- **Retry with backoff** (`OpenAI(max_retries=5)`)
- **Cache results** so reruns don’t call the API again
- **Batch requests** when possible
- Use a cheaper/free provider with higher limits, like **`gpt-4o-mini`** or Groq’s free tier

If you want, I can also suggest a safe thread-pool pattern for this.","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
What’s the best way to avoid losing progress if my dataset generation gets interrupted by API limits?,"Save progress periodically and resume from where you stopped.

A good approach is to dump partial results to a file as you go, for example JSONL, so if you hit API limits or the run gets interrupted, you can continue from the last saved item instead of starting over. Lowering concurrency also helps reduce the chance of hitting limits.","Free-tier Gemini limits both per-minute and per-day requests. Adding `time.sleep(4)` only fixes the per-minute side — a long `tqdm` loop can still blow through the per-day quota in one run.

Options when this happens:

- Spend ~$5 on OpenAI and use `gpt-4o-mini`. It's cheap enough to embed/generate the entire ground-truth set and has higher rate limits.
- Use Groq's free tier (`llama-3.3-70b-versatile`) — generous request-per-minute limits.
- Lower concurrency for thread-pool calls. Use a smaller pool size (2–3 workers) instead of pushing the API hard.
- Resume from where you stopped. Save progress periodically (e.g. dump the partial results to a JSONL file) so a hit limit doesn't lose all work.",c1e9f4b820
How do I fix WinError 126 when Python says it can’t load fbgemm.dll on Windows?,"Install the **Visual C++ Redistributable**. If that doesn’t fix it, install **Visual Studio** (not Visual Studio Code).

The course notes also point to this PyTorch discussion for more details:
https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
I’m getting an OSError about torch\lib\fbgemm.dll missing a module in Windows — what should I install?,"Install the **Visual C++ Redistributable**. If that doesn’t fix it, also install **Visual Studio** (not Visual Studio Code).","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
Does this fbgemm.dll error mean I need Visual C++ Redistributable or Visual Studio on my PC?,"Yes. Based on the context, this `fbgemm.dll` error can be fixed by installing either:

- **Visual C++ Redistributable**, or
- **Visual Studio** — **not** Visual Studio Code.

So the error indicates you likely need one of those runtime/development dependencies on your PC.","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
"Why does PyTorch fail with ""The specified module could not be found"" on Windows, and what’s the workaround?","PyTorch fails with `OSError: [WinError 126] The specified module could not be found` on Windows because `fbgemm.dll` or one of its dependencies is missing.

Workaround:
- Install the **Visual C++ Redistributable**
- Or install **Visual Studio** (not Visual Studio Code)

If you want, I can also point you to the referenced PyTorch forum thread.","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
I have the torch fbgemm.dll dependency error on Windows 10/11 — is installing Visual Studio Code enough or do I need the full Visual Studio?,"No — **Visual Studio Code is not enough**.

For the `torch\lib\fbgemm.dll` dependency error on Windows, the course notes say to install:

- **Visual C++ Redistributable**
- **Visual Studio** — **not** Visual Studio Code

So if you’re asking whether VS Code alone is sufficient: **you need the full Visual Studio (plus the C++ Redistributable)**.","- **Solution 1**: Install Visual C++ Redistributable.

- **Solution 2**: Install Visual Studio, not Visual Studio Code. 

  <{IMAGE:image_1}>

  For more details, please follow this link: [discuss.pytorch.org](https://discuss.pytorch.org/t/failed-to-import-pytorch-fbgemm-dll-or-one-of-its-dependencies-is-missing/201969)",15e931476c
"Why am I getting an OperationalError in `prep.py` saying it can't translate host name `postgres`, and what should I change to make it work?","The error happens because `prep.py` is trying to connect to a PostgreSQL host named `postgres`, but on your machine that hostname doesn’t resolve.

Change your `.env` file and set:

```ini
POSTGRES_HOST=localhost
```

Then run `prep.py` again.","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
"My `psycopg2` setup fails with `could not translate host name ""postgres"" to address`; is there a config I need to update?","Yes — update your `.env` file and set:

```ini
POSTGRES_HOST=localhost
```

This fixes the `could not translate host name ""postgres"" to address` error when running `python prep.py`.","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
"When I run `python prep.py`, it crashes with a PostgreSQL host error for `postgres` — how do I fix the connection settings?","Update your `.env` connection settings and set:

```ini
POSTGRES_HOST=localhost
```

This fixes the `postgres` host resolution error when running `python prep.py`.","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
"In Module 5, what should I put in my `.env` file if `prep.py` says `No such host is known` for `postgres`?","Put this in your `.env` file:

```ini
POSTGRES_HOST=localhost
```","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
How do I resolve the database host error from `python prep.py` and connect to PostgreSQL locally?,"Update your `.env` file and set:

```ini
POSTGRES_HOST=localhost
```

That will make `python prep.py` connect to your local PostgreSQL instead of trying to use the `postgres` host.","To resolve this error, update the `.env` file:

- Change the `POSTGRES_HOST` variable to `localhost`.

```ini
POSTGRES_HOST=localhost
```",4b65d5542d
How do I make Pandas show the full text in a column instead of cutting it off with ...?,"Use:

```python
pd.set_option('display.max_colwidth', None)
```

This tells Pandas not to truncate column text with `...` and to show the full content.","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
"In the offline rag evaluation notebook, how can I see the full Explanation from the judge LLM in Pandas?","Use:

```python
pd.set_option('display.max_colwidth', None)
```

This tells Pandas not to truncate the text, so you can see the full `Explanation` column from the judge LLM.","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
What Pandas setting changes the max column width so long text is displayed completely?,"`pd.set_option('display.max_colwidth', None)`","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
"Why does Pandas only show about 50 characters in a text column, and how do I disable that?","Pandas truncates text in columns by default to about 50 characters for display.

To show the full text, disable the column width limit:

```python
pd.set_option('display.max_colwidth', None)
```

- `display.max_colwidth`: controls the maximum width of a column in the printed representation
- Default is `50`
- Setting it to `None` allows unlimited width","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
What code do I need to run so the entire Explanation column content is visible in the LLM-as-judge section?,"Use:

```python
pd.set_option('display.max_colwidth', None)
```

This removes the default truncation so the full content in the Explanation column is visible.","By default, Pandas truncates text content in a column to 50 characters. To view the entire explanation provided by the judge LLM for a non-relevant answer, use the following instruction:

```python
pd.set_option('display.max_colwidth', None)
```

- **Option:** `display.max_colwidth`
- **Type:** `int` or `None`
- **Description:** Sets the maximum width in characters of a column in the representation of a pandas data structure. When a column overflows, a ""..."" placeholder is used in the output. Setting it to 'None' allows unlimited width.
- **Default:** 50

Refer to the [official documentation](https://pandas.pydata.org/docs/user_guide/options.html) for more details.

<{IMAGE:image_1}>",641aedbbf5
How do I normalize vectors stored in a Pandas DataFrame column?,"You can normalize each vector in the column by dividing it by its L2 norm:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```

This creates a new column with normalized vectors.","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
What's the easiest way to normalize each vector in a Pandas Series?,"The easiest way is to divide each vector by its L2 norm:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

series_normalized = series.apply(normalize_vec)
```

If you're working with a DataFrame column, the same idea applies:

```python
df[""new_col""] = df[""org_col""].apply(normalize_vec)
```","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
How can I create a new column with normalized vectors from an existing one in pandas?,"You can create a new column by normalizing each vector in the existing column with `np.linalg.norm`:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```

This applies the normalization row by row and stores the normalized vectors in `new_col`.","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
Is there a simple lambda for normalizing vector values in a DataFrame column?,"Yes. A simple lambda is:

```python
normalize_vec = lambda v: v / np.linalg.norm(v)
```

Then apply it to the DataFrame column:

```python
df[""new_col""] = df[""org_col""].apply(normalize_vec)
```","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
How do I divide each vector by its norm for a pandas column?,"Use the vector’s norm and divide elementwise:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)
df[""new_col""] = df[""org_col""].apply(normalize_vec)
```

","To normalize vectors in a Pandas DataFrame column, you can use the following approach:

```python
import numpy as np

normalize_vec = lambda v: v / np.linalg.norm(v)

df[""new_col""] = df[""org_col""].apply(normalize_vec)
```",5039707c1a
How do I get the 75th percentile for a column in a pandas DataFrame?,"Use `quantile` with `q=0.75`:

```python
quantile = df[""col""].quantile(q=0.75)
```

","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
What’s the simplest way to calculate the 0.75 quantile of a pandas Series?,"Use:

```python
series.quantile(q=0.75)
```

For example:

```python
quantile = df[""col""].quantile(q=0.75)
```","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
How can I find a percentile value from a DataFrame column using pandas?,"You can use pandas’ `quantile` method on the column:

```python
quantile = df[""col""].quantile(q=0.75)
```

This gives you the 75th percentile.","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
Is there a pandas method to compute quartiles or quantiles for one column?,"Yes. You can use Pandas’ `quantile()` method on a single column:

```python
quantile = df[""col""].quantile(q=0.75)
```

For example, `q=0.75` gives the 75th percentile.","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
How do I extract the 75% percentile from df['col'] in pandas?,"Use:

```python
df[""col""].quantile(q=0.75)
```

This returns the 75th percentile.","To compute the 75% percentile or 0.75 quantile:

```python
quantile = df[""col""].quantile(q=0.75)
```",b4c8ac5a0c
"How do I delete every Docker container from the terminal, even the ones that are still running?","To delete every Docker container from the terminal, including running ones, use:

```bash
docker rm -f $(docker ps -aq)
```","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
What’s the command to force-remove all Docker images at once?,"Use this command:

```bash
docker rmi -f $(docker images -q)
```","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
How can I clear out all Docker volumes using the command line?,"You can clear out all Docker volumes with:

```bash
docker volume rm $(docker volume ls -q)
```

If you want to remove all Docker containers, images, and volumes together, use:

```bash
docker rm -f $(docker ps -aq)
docker rmi -f $(docker images -q)
docker volume rm $(docker volume ls -q)
```","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
"Is there a quick way to wipe all containers, images, and volumes in Docker from terminal commands?","Yes. From the terminal, you can wipe them with these commands:

```bash
docker rm -f $(docker ps -aq)
docker rmi -f $(docker images -q)
docker volume rm $(docker volume ls -q)
```

This removes all containers, images, and volumes.","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
How do I clean up my Docker setup and remove everything with one terminal session?,"You can clean up Docker from one terminal session by running:

```bash
docker rm -f $(docker ps -aq)
docker rmi -f $(docker images -q)
docker volume rm $(docker volume ls -q)
```

This removes:
- all containers, including running ones
- all images
- all volumes

I don’t know about removing builds from the context.","1. Delete all containers (including running ones):

```bash
docker rm -f $(docker ps -aq)
```

2. Remove all images:

```bash
docker rmi -f $(docker images -q)
```

3. Delete all volumes:

```bash
docker volume rm $(docker volume ls -q)
```",66ccbb7da0
How can I make sure a user can only leave feedback once per submission in Streamlit?,I don't know.,"Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
Why does `st.session.submitted` keep turning back to True even after I press +1 or -1?,I don't know.,"Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
How do I disable the feedback buttons after the ask button is used in a Streamlit app?,I don't know.,"Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
What is the fix for session state getting reassigned in Streamlit when tracking submission feedback?,"The fix is to follow the solution in Streamlit Discuss:  
https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2

","Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
Is there a known solution for `st.session.submitted` being reset after one feedback button is clicked?,"Yes — there is a known solution. The context says this issue is **solved**, and it points to the Streamlit Discuss solution here:

[Streamlit Discuss solution](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)","Solved:

[Refer to the solution on Streamlit Discuss](https://discuss.streamlit.io/t/streamlit-session-attributes-reassigned-somewhere/76059/2?u=mohammed2)",2e729f1271
"Why do I get ""streamlit: executable file not found in $PATH"" when I start my app with docker-compose, even though it works normally outside Docker?","Because the container is trying to run `streamlit` as its startup command, but your Docker image/container doesn’t have `streamlit` available in its PATH.

According to the context, the fix is to:

1. Make sure you have a `Dockerfile`
2. Add `streamlit` to the `docker-compose` configuration
3. Rebuild the image and start again:

```bash
docker-compose up --build
```

So it works outside Docker because your local environment has `streamlit`, but the Docker container needs it installed/configured too.","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
What should I check first if docker-compose fails to create the container for my Streamlit app with an OCI runtime error?,"First, check whether you’ve created a **Dockerfile** and added **`streamlit`** to your **docker-compose** configuration.

Then rebuild and start the app with:

```bash
docker-compose up --build
```

","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
"Do I need a Dockerfile for a Streamlit app that I want to run with docker-compose, or can I just use the compose file?","Yes — based on the context, you should create a **Dockerfile** for the Streamlit app.

The compose file alone is not enough here. The fix for the `streamlit: executable file not found in $PATH` error explicitly says to:

1. **Create a `Dockerfile`**
2. **Add `streamlit` to the `docker-compose` configuration**
3. Rebuild with:

```bash
docker-compose up --build
```

So the answer is: **use a Dockerfile plus docker-compose, not just compose alone.**","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
How do I fix the issue where docker-compose says it cannot find the streamlit command inside the container?,"To fix this:

1. Make sure you have a `Dockerfile`.
2. Add `streamlit` to your `docker-compose` setup.
3. Rebuild the container:

```bash
docker-compose up --build
```

This should make the `streamlit` command available inside the container.","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
"After changing my Docker setup for a Streamlit app, what is the right command to rebuild everything and start it again?","Run:

```bash
docker-compose up --build
```","To resolve this issue:

1. Ensure you have created a `Dockerfile`.
2. Add `streamlit` to the `docker-compose` configuration.
3. Run the following command to rebuild and start the application:

   ```bash
   docker-compose up --build
   ```",e8df9f0d12
"Can the capstone be done in a team, or does each student need to submit their own project?","The capstone is an individual project, not a team project.

You can discuss ideas or collaborate at a high level, but each submitted project must stand on its own, with a separate qualifying component and separate repository. If a shared system can’t be evaluated independently for each student, it does not meet the requirement.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
"If I work with classmates on one idea, is that still okay for the capstone?","Yes — you can collaborate or discuss a larger idea with classmates. But each submitted capstone must be an individual project and must stand on its own.

If you share an idea, make sure:
- each person has a separate qualifying component,
- each person has a separate repository,
- the work can be evaluated independently for each participant.

If the shared work can’t be evaluated independently, then it doesn’t meet the capstone requirement.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
Can multiple people build the same system for the project as long as we split the work somehow?,"No. The capstone is an individual project. You can discuss or collaborate on a bigger idea, but each submitted project must stand on its own with a separate qualifying component and separate repository. If the work can’t be evaluated independently for each person, it doesn’t meet the requirement.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
What counts as an independent capstone submission if the idea was shared with others?,"An independent capstone submission means your project must be evaluable on its own, even if the idea was discussed or shared with others.

In practice:
- You may collaborate or discuss a larger idea with other students.
- But each submitted project must stand on its own.
- If a shared system is used, it must be split into clearly independent projects.
- Each person needs:
  - a separate qualifying component
  - a separate repository

If the work cannot be judged separately for each participant, it does not count as a valid capstone submission.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
"Does the project have to be fully separate, or can parts of it overlap with someone else's work?","It doesn’t have to be fully separate, but each submitted project must stand on its own.

You can share a larger idea or collaborate, but if a system overlaps, it must be clearly split into independent projects, with:

- a separate qualifying component for each person
- a separate repository for each project
- something that can be evaluated independently

If it can’t be evaluated independently for each participant, then it does not meet the requirement.","No, the capstone is an individual project.

You can collaborate or discuss a larger idea with other students, but each submitted project must stand on its own. A shared system can work only if it is clearly decomposed into independent projects, where each person has a separate qualifying component and a separate repository.

If the work cannot be evaluated independently for each participant, it does not satisfy the project requirement.",0fab61eca2
"Do we need to turn in two capstone projects, or is one enough?","You only need to submit **one** capstone project.

If your first submission fails, you can improve it and resubmit during the **attempt #2** window. If you want to submit two projects for extra experience, they must use **different datasets and problem statements**.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
What do attempt 1 and attempt 2 mean for the capstone submission?,"Attempt 1 and attempt 2 are two submission windows for the same capstone project.

- You only need to submit **one project**.
- If your **first submission (attempt 1)** does not pass, you can **improve it and resubmit** in **attempt 2**.
- If you miss attempt 1, you can still submit in attempt 2.

So, attempt 2 is basically a second chance for the same capstone submission.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
"If my first project submission gets rejected, can I fix it and send it again later?","Yes. If your first capstone submission fails, you can improve it and resubmit during the attempt #2 submission window.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
"Can I submit a second project just for practice, and if so, does it need a different dataset and problem?","Yes. If you want to submit two projects for practice/exposure, they must use **different datasets and different problem statements**.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
"If I miss the first submission window, can I still submit in the second one and count it toward certification?","Yes — if you miss the first submission window, you can still submit during the second attempt window and it can count toward certification.

Just make sure you also participate in the peer-review of three peers in your cohort, because the submission won’t count toward certification without that.","You only need to submit one project. If the submission at the first attempt fails, you can improve it and re-submit during the attempt#2 submission window.

- If you want to submit two projects for the experience and exposure, you must use different datasets and problem statements.
- If you can’t make it to the attempt#1 submission window, you still have time to catch up to meet the attempt#2 submission window.

Remember that the submission does not count towards the certification if you do not participate in the peer-review of three peers in your cohort.",d5fc98925d
"Is the competition considered the capstone project for this course, or is it separate?","The competition is separate from the capstone project. It does **not** count as the capstone.

You can join the competition in a group if you want, but the **capstone itself must be an individual project**.","No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
Can I use the competition entry as my capstone submission?,"No. The competition does not count as the capstone. The capstone is an individual attempt, though you can participate in the competition as a group if you want.","No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
"If I join the math-kaggle-llm competition, does that replace the capstone requirement?",No. Joining the math-kaggle-llm competition does **not** replace the capstone requirement. The capstone is an **individual project** and still needs to be submitted separately.,"No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
Are team entries allowed for the competition even though the capstone is individual?,"Yes. Team entries are allowed for the competition, even though the capstone itself is individual.","No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
Do I have to do the capstone on my own if I also want to take part in the competition?,"No. The competition can be done in a group, but the capstone itself is an individual attempt.","No, it does not. You can participate in the [math-kaggle-llm-competition](https://datatalks-club.slack.com/archives/C0791HB4A58) as a group if you want to form teams; but the capstone is an individual attempt.",e892d44e42
How will the capstone projects be graded in the end?,The final grade will be the **median score** of the grades given by the peer reviewers.,"Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
Who reviews a submitted capstone project?,Each submitted capstone project is reviewed by three randomly assigned students who have also submitted the project.,"Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
Do I need to score other students’ capstone projects too?,"Yes. Each submitted project is reviewed by three other students, and you’re also expected to grade three fellow students’ projects yourself.","Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
What happens if I don’t take part in the peer grading process?,"If you don’t take part in the peer grading process, you may fail to achieve the Certificate at the end of the course.","Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
Is the final capstone grade based on all the reviews or some other score?,"The final capstone grade is the **median score of the grades from the peer reviewers**. It is based on the reviews from three randomly assigned students, not on some separate score.","Each submitted project will be evaluated by three randomly assigned students who have also submitted the project.

You will also be responsible for grading the projects from three fellow students yourself. Please be aware that not complying with this rule implies you may fail to achieve the Certificate at the end of the course.

The final grade you receive will be the median score of the grades from the peer reviewers. The peer review criteria for evaluation must follow the guidelines defined here (TBA for link).",9a2e2d2008
When do we get the projects for review and grading?,"After the submission deadline has passed, an Excel sheet will be shared with 3 projects assigned to each participant.","After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
How are the projects assigned to each participant after the deadline?,"After the submission deadline has passed, an Excel sheet will be shared, and each participant will be assigned 3 projects to review.","After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
Will the review assignments be sent out right after submissions close?,I don't know.,"After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
How many projects will each person have to grade?,Each person will have to grade **3 projects**.,"After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
What format will the project assignment list be shared in?,An Excel sheet will be shared.,"After the submission deadline has passed, an Excel sheet will be shared with 3 projects being assigned to each participant.",eae0fb50aa
"I already submitted my capstone project, so why am I still unable to review other projects?","Once the project submission deadline has passed, projects are assigned to you for evaluation. You can’t choose which projects to review, and you can’t review anything before the list has been released.","Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
When do the project reviews get assigned after the submission deadline is over?,"After the submission deadline has passed, an Excel sheet is shared and 3 projects are assigned to each participant.","Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
Can I pick which capstone projects I want to evaluate myself?,"No. Once the submission deadline has passed, projects are assigned to you for evaluation, and you can’t choose which ones to review.","Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
Why don’t I see any projects available for review yet?,"You won’t see projects yet because they’re only assigned **after the submission deadline has passed**. You can’t choose which projects to review, and the review list hasn’t been released yet.","Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
Is it normal that I can’t start reviewing before the project list is released?,Yes. You can’t review before the project list has been released; projects are assigned only after the submission deadline passes.,"Once the project submission deadline has passed, projects will be assigned to you for evaluation. You can't choose which projects to evaluate, and you can’t review before the list has been released.",a8a7fef016
Where can I find project ideas and datasets for the capstone project?,"You can find project ideas and datasets on the GitHub page linked here:

https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md

It includes several ideas, datasets, and tips/guidelines for the capstone project.","Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
Is there a page with some good dataset ideas I can use for this project?,"Yes — please check **[this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md)** for several project ideas and datasets, plus tips and guidelines.","Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
I’m looking for inspiration for the capstone project — do you have a link with ideas and datasets?,"Yes — the course points to a GitHub page with project ideas and datasets:

https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md

It also includes tips and guidelines for the capstone project.","Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
"Where should I look for tips, guidelines, and possible datasets for the project?","Please check this GitHub page: https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md

It includes tips, guidelines, and several ideas and datasets you can use for the project.","Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
Can you point me to a GitHub page that lists project ideas and datasets for the capstone?,"Yes — check this GitHub page for project ideas, datasets, tips, and guidelines:

https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md","Please check [this GitHub page](https://github.com/DataTalksClub/llm-zoomcamp/blob/main/project.md) for several ideas and datasets that could be used for the project, along with tips and guidelines.",e76a70cde3
"Do I actually need Airflow, Mage, or Kestra for the capstone project, or is a simple Python script enough?","No — you do **not** need Airflow, Mage, or Kestra for the capstone.

A **simple Python script** that ingests and indexes your data is enough to get **full points** for the ingestion-pipeline criterion. If you use a **Jupyter notebook** with the same steps, that’s worth **1 point instead of 2**.

Use an orchestrator only if it genuinely fits your project, like **recurring daily ingestion**. Don’t add one just for the sake of it.","No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
"Can I just use a Jupyter notebook for the ingestion pipeline part of the capstone, and how does that affect the points?","Yes — you can use a Jupyter notebook for the capstone ingestion pipeline.

Points-wise:
- A plain Python script that ingests and indexes your data gives **full points** for the ingestion-pipeline criterion.
- A **Jupyter notebook with the same steps** is worth **1 point instead of 2**.

So if you want the full score for that criterion, use a Python script rather than just a notebook.","No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
What counts as enough for the ingestion pipeline criterion in the capstone project?,"A plain Python script that ingests and indexes your data is enough for full points on the ingestion-pipeline criterion. If you use a Jupyter notebook with the same steps, that counts for 1 point instead of 2.","No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
"Should I add an orchestration tool to my capstone only to get more points, or is that unnecessary?","No — don’t add an orchestration tool just to get more points.

For the capstone, a plain Python script that ingests and indexes your data is enough for full points on the ingestion pipeline criterion. A Jupyter notebook would be worth fewer points, but an orchestrator like Airflow, Mage, or Kestra is only needed if it actually makes sense for your project, such as recurring daily ingestion.","No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
When would it make sense to use an orchestrator in the capstone project instead of just plain Python?,"It would make sense to use an orchestrator only if your project actually needs it — for example, if you have a feed or dataset that updates regularly and you want recurring ingestion, scheduling, retries, or monitoring.

If your capstone is just a one-time ingestion/indexing pipeline, plain Python is enough and gets full points for that criterion. Don’t add an orchestrator just to check a box.","No. A plain Python script that ingests and indexes your data is enough for full points on the ""ingestion pipeline"" criterion. A Jupyter notebook with the same steps is worth 1 point instead of 2.

Use an orchestrator only if it actually fits your project — for example, recurring ingestion of a feed that updates daily. Don't add one just to score the criterion.",d4f7c08ea1
How can I evaluate a recommender-style RAG project when I don't have obvious ground-truth Q&A pairs?,"You can evaluate it with two complementary methods:

1. **Synthetic ground truth**
   - For each item in your dataset, ask an LLM to generate about 5 user queries that should retrieve that item as the top result.
   - Run those queries through your retriever/RAG system.
   - Measure **hit rate**, **MRR**, or **NDCG**.

2. **LLM-as-a-judge**
   - Sample real or synthetic queries.
   - Run the full RAG pipeline.
   - Have an LLM score the result with a fixed rubric, such as **relevance/usefulness on a 1–5 scale**.

For recommender-style ranking tasks, **NDCG is often a better fit than hit rate**, because multiple items may be acceptable and it rewards putting good items higher in the ranking.

If you want, I can also help you design a concrete evaluation prompt and rubric.","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
What’s a good way to create synthetic test queries for retrieval evaluation in a recommender system?,"A good way is to use **synthetic ground truth**:

1. Take each item in your dataset.
2. Prompt an LLM with the item’s description.
3. Ask it to generate about **5 user queries** that should retrieve that item as the **top result**.
4. Run those queries through your retrieval system.
5. Evaluate with metrics like **hit rate, MRR, or NDCG**.

For recommender-style retrieval, **NDCG** is often especially useful because it handles cases where **multiple items could be acceptable**, and it rewards having good results ranked near the top.","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
Can I use an LLM as a judge to score the quality of my RAG answers end to end?,"Yes — you can use an LLM as a judge to score the end-to-end quality of your RAG answers.

A common approach is to:
- sample queries,
- run the full RAG pipeline,
- then have an LLM rate each answer on a fixed rubric, such as relevance/usefulness on a 1–5 scale.

Just make sure you define the criteria clearly in the prompt.","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
"Which metrics make sense for ranking-style RAG problems: hit rate, MRR, or NDCG?","For ranking-style RAG problems, **all three can make sense**, but they measure slightly different things:

- **Hit rate**: good if you only care whether a relevant item appears in the top-k at all.
- **MRR**: good if the position of the first relevant result matters a lot.
- **NDCG**: often the **best fit for ranking-style problems**, especially when **multiple items can be acceptable**.

From the context: **NDCG is often a better fit than hit rate for ranking-style problems where multiple items are acceptable answers** because it rewards getting good items **high in the list**, not just whether the first hit exists.

So the short answer is: **use hit rate, MRR, and NDCG if you can, but NDCG is often the most appropriate for ranking-style RAG.**","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
Why would NDCG be better than hit rate if multiple retrieved items could all be acceptable?,"Because **hit rate only checks whether at least one acceptable item appears in the retrieved set**, while **NDCG also measures how high the good items are ranked**.

So if multiple retrieved items are all acceptable, NDCG is better because it:
- gives **more credit to better-ranked relevant items**
- distinguishes between “one good item buried low” and “one good item at the top”
- works better for **ranking-style problems** where there may be several valid answers

Hit rate would treat both cases the same as long as one acceptable item is present.","Two complementary approaches that both score for the evaluation criterion:

1. Synthetic ground truth (same idea as the course, adapted). For each item in your dataset, prompt the LLM with the item's description and ask it to generate ~5 user queries that should return that item as the top result. Then run those queries through your retrieval and measure hit rate / MRR / NDCG.

2. LLM-as-a-judge for end-to-end quality. Sample queries, run the full RAG, and have an LLM rate the result for relevance/usefulness on a fixed rubric (e.g. 1–5 scale, with criteria you specify in the prompt).

NDCG is often a better fit than hit-rate for ranking-style problems where multiple items are acceptable answers — it rewards getting good items high in the list, not just first.",9b0e2d6a47
How should I chunk a huge PDF corpus for my RAG project without overthinking it first?,"Start simple and optimize later.

For a huge PDF corpus in a RAG project, the course suggests:

- Extract text with whatever tool works best on a sample first
- Clean the text by removing obvious noise like broken OCR, boilerplate, repeated headers/footers, and duplicates
- Preserve useful structure such as titles, section names, dates, page numbers, and Q&A format
- Store the result in a structured format if helpful, often JSON
- Chunk in a way that keeps related context together
- Keep metadata for filtering/ranking later

So the practical advice is: don’t overthink chunking upfront. Use a straightforward chunking approach that preserves semantic units and context, test it on a sample, and refine based on retrieval quality.","Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
Is it better to start with simple fixed-size chunks or try LLM-based section splitting right away?,"Yes — **start simple first**.

The recommended approach is:

- **Start with fixed-size chunking** around **1000 tokens with some overlap**
- Run a **small ground-truth evaluation**
- Then try **LLM-based section splitting** and compare results
- Re-evaluate after each change

So the idea is **not** to try the perfect LLM-based chunker right away, but to **iterate and measure improvement** on hit rate / MRR.","Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
"Should I add summaries to each chunk, and does that actually help retrieval?",I don't know.,"Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
"For legal or financial docs, when does hybrid search make more sense than only dense embeddings?","For long, structured documents like legal or financial docs, hybrid search makes more sense because exact wording matters and dense embeddings can miss it. Using **BM25 + dense** helps preserve and retrieve precise terms while still benefiting from semantic matching.","Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
What tools can I use to turn PDFs into cleaner markdown before I chunk them?,"You can use:

- **pymupdf4llm** / **PyMuPDF** — fast text extraction, Markdown output
- **Docling** — good structured Markdown/JSON, often higher quality
- **unstructured** — handles many document types and layouts
- **GROBID** — especially good for academic papers
- **pytesseract** (Tesseract OCR) — for scanned/image-based PDFs
- **Hosted OCR** like **Mistral OCR** — if you want an API instead of running OCR locally

A good approach is to try a couple on a sample of your PDFs and see which gives the cleanest Markdown before chunking.","Don't try to find the perfect chunker upfront — iterate.

- Start simple: fixed-size chunking (~1000 tokens with some overlap) and run a small ground-truth eval.
- Try smart chunking: ask an LLM to split each document into logical sections, then index each section.
- Add a short LLM-generated summary per chunk and index it alongside, or use it to boost retrieval.
- For long, structured documents (legal, financial), prefer hybrid search (BM25 + dense) so exact wording isn't lost during semantic matching.

Useful tools for parsing PDFs to clean markdown before chunking:

- [`pymupdf4llm`](https://pypi.org/project/pymupdf4llm/) — fast, decent quality.
- [Docling](https://github.com/docling-project/docling) — slower but higher quality on tables/figures.
- [GROBID](https://github.com/kermitt2/grobid) — for academic papers, extracts structure (sections, refs, etc.).

Run the eval again after each change. The goal is measurable improvement on hit rate / MRR for your ground-truth set, not a ""perfect"" chunker in the abstract.",6f2a8b3d10
"In the capstone project, what does reproducibility actually mean for reviewers?","For reviewers, reproducibility means they can **clone your repo and run the instructions in your README to recreate the system from scratch using their own credentials**.

So you should:
- **not** include API keys or hosted-service credentials,
- provide a script/notebook to rebuild the data/index locally,
- include a `.env.example` with variable names only,
- and pin dependencies / document the Python version.

","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
Do I need to put my API keys or hosted-service credentials in the repo so someone can run my project?,"No. Never put your API keys or hosted-service credentials in the repo.

For reproducibility, reviewers should be able to clone your repo and run it using their own credentials. You should:

- include a `.env.example` with variable names only, no secrets
- keep `.env` in `.gitignore`
- provide a script or notebook to rebuild the index / setup from scratch
- pin dependency versions and document the Python version

So the answer is: **don’t store keys in the repo; use placeholders and let reviewers supply their own.**","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
How should I let a reviewer recreate the search index or rebuild the project from scratch?,"Provide a script or notebook that ingests the dataset and rebuilds the search index locally from scratch, and explain it in your README so a reviewer can run it with their own credentials.

Also:
- include a `.env.example` with variable names only, not real keys
- keep `.env` in `.gitignore`
- pin dependency versions and document the Python version
- if using Docker, document the Docker version too

The reviewer should be able to clone the repo and recreate everything without your API keys.","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
"What should I include in a .env.example file, and should my real .env be committed?","Include only the variable names in `.env.example`, with no secret values. For example:

```bash
OPENAI_API_KEY=
GROQ_API_KEY=
GEMINI_API_KEY=
```

Then tell reviewers to copy it to a real `.env` and fill in their own keys.

No, your real `.env` should not be committed. Add `.env` to `.gitignore`, and keep secrets out of the repo.","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
How can I make sure reviewers can run the project cheaply without wasting their own credits?,"To keep reviewers from wasting their own credits, make the project reproducible **and cheap to run**:

- **Do not include your API keys or hosted credentials** in the repo.
- Add a **`.env.example`** with only the variable names, and ask reviewers to create their own **`.env`**.
- Make sure the project can be run with **their own credentials** from scratch.
- Provide a **script or notebook** that rebuilds the dataset/search index locally.
- Use a **cheap model** like `gpt-4o-mini` or Groq so reviewers don’t burn through credits.
- Pin dependency versions and document the Python/Docker versions.

In short: reviewers should be able to clone the repo, follow the README, and run it cheaply using their own credentials.","Never share API keys or hosted-service credentials in your repo. Reproducibility means a peer reviewer can clone the repo and follow your README to recreate the system from scratch — using their own credentials.

Concretely:

- Provide a script (or notebook) that ingests the dataset and (re)builds the search index locally.
- Ship a `.env.example` with the variable names but no values; have the reviewer create their own `.env` with their own keys. Keep `.env` in `.gitignore`.
- Use a cheap model (`gpt-4o-mini`, Groq, etc.) so reviewers don't burn through credits when running your project.
- Pin dependency versions (`requirements.txt` / `pyproject.toml` lock file) and document the Python version (and Docker version, if used).",e5d8a2c761
"Is it okay to do the homework or capstone in something like JavaScript, Go, Rust, Java, Scala, C#, or R instead of Python?","Yes, but **Python is strongly recommended**.

Using JavaScript, Go, Rust, Java, Scala, C#, or R is technically allowed for homework and the capstone, but you should only do it if you have a strong reason. The course materials and reviewer expectations are Python-based, so non-Python stacks make review and reproducibility harder.

If you use another language, make sure your README is very thorough and explains how to install and run everything on **Windows, macOS, and Linux**.","In most cases, use Python. The course materials, examples, and reviewer expectations are Python-based, so Python is the easiest path. This applies to every homework and to the final/capstone project.

Using another programming language or stack - for example JavaScript, TypeScript, Go, Rust, Java, Scala, C#, or R - is technically possible, but do it only if you have a strong reason. We do not want to restrict your choice of technology, but a non-Python stack makes reproducibility and review harder.

If you use a language other than Python, your documentation must be very thorough. Assume the reviewer has no knowledge of that language or ecosystem. Your README should explain how to install dependencies and run the homework or project on Windows, macOS, and Linux.

For example, a Go project should include steps at the level of:

```bash
go mod tidy
go run .
```

The submission must still be easy to reproduce and evaluate.",ea815eb858
"Do I have to use Python for all assignments and the final project, or can I pick another stack if I really want to?","In most cases, yes — you should use Python for all homework and for the final/capstone project.

Using another stack is technically allowed, but only if you have a strong reason. If you do, you need very thorough documentation so the reviewer can reproduce it easily on Windows, macOS, and Linux.

So the short answer is: Python is the expected and easiest path, but another stack is possible if necessary.","In most cases, use Python. The course materials, examples, and reviewer expectations are Python-based, so Python is the easiest path. This applies to every homework and to the final/capstone project.

Using another programming language or stack - for example JavaScript, TypeScript, Go, Rust, Java, Scala, C#, or R - is technically possible, but do it only if you have a strong reason. We do not want to restrict your choice of technology, but a non-Python stack makes reproducibility and review harder.

If you use a language other than Python, your documentation must be very thorough. Assume the reviewer has no knowledge of that language or ecosystem. Your README should explain how to install dependencies and run the homework or project on Windows, macOS, and Linux.

For example, a Go project should include steps at the level of:

```bash
go mod tidy
go run .
```

The submission must still be easy to reproduce and evaluate.",ea815eb858
"If I submit the project in a non-Python language, what should I put in the README so the reviewer can run it?","In your README, include **very thorough run instructions** so the reviewer can reproduce the project on **Windows, macOS, and Linux**.

At minimum, explain:

- how to install the language/runtime and dependencies
- how to set up any environment variables or config files
- the exact commands to run the project

For example, for a Go project, the README should include steps like:

```bash
go mod tidy
go run .
```

Also make sure the submission is easy to reproduce and evaluate.","In most cases, use Python. The course materials, examples, and reviewer expectations are Python-based, so Python is the easiest path. This applies to every homework and to the final/capstone project.

Using another programming language or stack - for example JavaScript, TypeScript, Go, Rust, Java, Scala, C#, or R - is technically possible, but do it only if you have a strong reason. We do not want to restrict your choice of technology, but a non-Python stack makes reproducibility and review harder.

If you use a language other than Python, your documentation must be very thorough. Assume the reviewer has no knowledge of that language or ecosystem. Your README should explain how to install dependencies and run the homework or project on Windows, macOS, and Linux.

For example, a Go project should include steps at the level of:

```bash
go mod tidy
go run .
```

The submission must still be easy to reproduce and evaluate.",ea815eb858
How detailed do the install and run instructions need to be for a project that isn’t written in Python?,"Your README should be **very thorough**.

For a non-Python project, assume the reviewer has **no knowledge of that language or ecosystem** and explain:

- how to **install dependencies**
- how to **run the project**
- on **Windows, macOS, and Linux**

For example, a Go project should include steps like:

```bash
go mod tidy
go run .
```

The key requirement is that the submission must still be **easy to reproduce and evaluate**.","In most cases, use Python. The course materials, examples, and reviewer expectations are Python-based, so Python is the easiest path. This applies to every homework and to the final/capstone project.

Using another programming language or stack - for example JavaScript, TypeScript, Go, Rust, Java, Scala, C#, or R - is technically possible, but do it only if you have a strong reason. We do not want to restrict your choice of technology, but a non-Python stack makes reproducibility and review harder.

If you use a language other than Python, your documentation must be very thorough. Assume the reviewer has no knowledge of that language or ecosystem. Your README should explain how to install dependencies and run the homework or project on Windows, macOS, and Linux.

For example, a Go project should include steps at the level of:

```bash
go mod tidy
go run .
```

The submission must still be easy to reproduce and evaluate.",ea815eb858
"Would a project in Go be acceptable, and if so, what kind of commands should I include to make it easy to reproduce?","Yes, a Go project can be acceptable, but only if you have a strong reason to use it instead of Python.

If you use Go, your README should be very thorough and explain how to install dependencies and run the project on Windows, macOS, and Linux. The context specifically says a Go project should include commands at this level, for example:

```bash
go mod tidy
go run .
```

The key requirement is that the submission must still be easy to reproduce and evaluate.","In most cases, use Python. The course materials, examples, and reviewer expectations are Python-based, so Python is the easiest path. This applies to every homework and to the final/capstone project.

Using another programming language or stack - for example JavaScript, TypeScript, Go, Rust, Java, Scala, C#, or R - is technically possible, but do it only if you have a strong reason. We do not want to restrict your choice of technology, but a non-Python stack makes reproducibility and review harder.

If you use a language other than Python, your documentation must be very thorough. Assume the reviewer has no knowledge of that language or ecosystem. Your README should explain how to install dependencies and run the homework or project on Windows, macOS, and Linux.

For example, a Go project should include steps at the level of:

```bash
go mod tidy
go run .
```

The submission must still be easy to reproduce and evaluate.",ea815eb858
Where do I see old LLM Zoomcamp capstone project submissions?,"You can browse old LLM Zoomcamp capstone submissions here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)","You can browse previous LLM Zoomcamp project submissions here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

These pages show submitted repositories and can help you understand the expected scope and quality of capstone projects.",930286278d
Is there a page with past projects from the 2024 LLM Zoomcamp?,"Yes — there is a page with past projects from the 2024 LLM Zoomcamp:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)","You can browse previous LLM Zoomcamp project submissions here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

These pages show submitted repositories and can help you understand the expected scope and quality of capstone projects.",930286278d
Can I check 2025 LLM Zoomcamp project examples somewhere?,"Yes — you can browse previous LLM Zoomcamp project submissions here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

These pages show submitted repositories and can help you understand the expected scope and quality of capstone projects.","You can browse previous LLM Zoomcamp project submissions here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

These pages show submitted repositories and can help you understand the expected scope and quality of capstone projects.",930286278d
Where can I look at submitted repos from previous Zoomcamp projects?,"You can browse submitted repos from previous Zoomcamp projects here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)","You can browse previous LLM Zoomcamp project submissions here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

These pages show submitted repositories and can help you understand the expected scope and quality of capstone projects.",930286278d
Do you have links to earlier capstone project examples for this course?,"Yes — you can look at previous capstone projects here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)","You can browse previous LLM Zoomcamp project submissions here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

These pages show submitted repositories and can help you understand the expected scope and quality of capstone projects.",930286278d
Do I need to reserve my capstone project idea before I start working on it?,No. You do not need to announce or reserve your project idea before starting.,"No. You do not need to announce or reserve your project idea before starting.

If you want feedback, you can ask in Slack. You can also look at previous projects to understand scope:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

Similar topics are okay as long as your implementation is your own and your submission meets the project requirements.",897a350476
Should I announce my project topic somewhere before building it?,"No. You do not need to announce or reserve your project idea before starting.

If you want feedback, you can ask in Slack.","No. You do not need to announce or reserve your project idea before starting.

If you want feedback, you can ask in Slack. You can also look at previous projects to understand scope:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

Similar topics are okay as long as your implementation is your own and your submission meets the project requirements.",897a350476
"Can I ask for feedback on my project idea in Slack, or is that only for finished projects?","Yes — you can ask for feedback on your project idea in Slack before it’s finished. The context says, “If you want feedback, you can ask in Slack.”","No. You do not need to announce or reserve your project idea before starting.

If you want feedback, you can ask in Slack. You can also look at previous projects to understand scope:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

Similar topics are okay as long as your implementation is your own and your submission meets the project requirements.",897a350476
Is it okay if my capstone topic is similar to someone else’s as long as I build it differently?,Yes — similar topics are okay as long as your implementation is your own and your submission satisfies the project requirements.,"No. You do not need to announce or reserve your project idea before starting.

If you want feedback, you can ask in Slack. You can also look at previous projects to understand scope:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

Similar topics are okay as long as your implementation is your own and your submission meets the project requirements.",897a350476
Where can I look at past LLM Zoomcamp projects to judge the right scope for my own idea?,"You can browse previous LLM Zoomcamp projects here:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

These should help you judge the expected scope and quality for your own idea.","No. You do not need to announce or reserve your project idea before starting.

If you want feedback, you can ask in Slack. You can also look at previous projects to understand scope:

- [2024 projects](https://courses.datatalks.club/llm-zoomcamp-2024/projects)
- [2025 projects](https://courses.datatalks.club/llm-zoomcamp-2025/projects)

Similar topics are okay as long as your implementation is your own and your submission meets the project requirements.",897a350476
Can I build my capstone as a Telegram or WhatsApp bot instead of a website?,"Yes. You can build your capstone as a Telegram or WhatsApp bot instead of a website.

The course allows any interface for the project, including:
- web app
- CLI
- Telegram or WhatsApp bot
- Slack app
- notebook
- anything else

Just make sure it meets the evaluation criteria.","Yes. You can use any interface for your project — a web app, a CLI, a Telegram or WhatsApp bot, a Slack app, a notebook, or anything else. There are no strict guidelines on how you make the project available to the end user. Pick whatever fits your idea, as long as the project meets the evaluation criteria.

Note that the course doesn't teach how to build these interfaces, so you'll need to figure that part out on your own. With an AI assistant it's usually quite simple.",7318a90b14
"Is a web app required for the project, or can I use a CLI or Slack app too?","No, a web app is not required. You can use any interface for the project, including a CLI or a Slack app, as long as it fits your idea and meets the evaluation criteria.","Yes. You can use any interface for your project — a web app, a CLI, a Telegram or WhatsApp bot, a Slack app, a notebook, or anything else. There are no strict guidelines on how you make the project available to the end user. Pick whatever fits your idea, as long as the project meets the evaluation criteria.

Note that the course doesn't teach how to build these interfaces, so you'll need to figure that part out on your own. With an AI assistant it's usually quite simple.",7318a90b14
Do I have to follow any strict rules about how users access the capstone project?,"There are **no strict rules** about the interface users use to access the capstone project. You can use **any interface** — a web app, CLI, Telegram/WhatsApp bot, Slack app, notebook, or something else.

The only requirement is that it should **fit your idea and meet the evaluation criteria**.","Yes. You can use any interface for your project — a web app, a CLI, a Telegram or WhatsApp bot, a Slack app, a notebook, or anything else. There are no strict guidelines on how you make the project available to the end user. Pick whatever fits your idea, as long as the project meets the evaluation criteria.

Note that the course doesn't teach how to build these interfaces, so you'll need to figure that part out on your own. With an AI assistant it's usually quite simple.",7318a90b14
"If I choose a notebook or some other interface, is that acceptable for the final project?","Yes. A notebook or any other interface is acceptable for the final project. The course allows any interface for the capstone, including a notebook, web app, CLI, Telegram/WhatsApp bot, Slack app, etc., as long as it meets the evaluation criteria.","Yes. You can use any interface for your project — a web app, a CLI, a Telegram or WhatsApp bot, a Slack app, a notebook, or anything else. There are no strict guidelines on how you make the project available to the end user. Pick whatever fits your idea, as long as the project meets the evaluation criteria.

Note that the course doesn't teach how to build these interfaces, so you'll need to figure that part out on your own. With an AI assistant it's usually quite simple.",7318a90b14
"Will the course show me how to make the interface, or do I need to handle that part myself?","The course does **not** teach how to build the interface itself. You can use any interface you want for the project, but you’ll need to figure out that part on your own.","Yes. You can use any interface for your project — a web app, a CLI, a Telegram or WhatsApp bot, a Slack app, a notebook, or anything else. There are no strict guidelines on how you make the project available to the end user. Pick whatever fits your idea, as long as the project meets the evaluation criteria.

Note that the course doesn't teach how to build these interfaces, so you'll need to figure that part out on your own. With an AI assistant it's usually quite simple.",7318a90b14
"What are some good tools to pull text out of PDFs for a RAG app, especially if the files are messy or scanned?","Some good options the course participants commonly use are:

- **docling** — converts PDFs and other docs into structured Markdown/JSON, and works well for RAG.
- **PyMuPDF / pymupdf4llm** — fast PDF text extraction, with Markdown output.
- **unstructured** — useful for many document types and complex layouts.
- **pytesseract** (Tesseract OCR) — good for scanned or image-based PDFs that don’t have a text layer.
- **Hosted OCR services** like **Mistral OCR** — if you want to call an API instead of running OCR locally.

If the files are messy or scanned, OCR-based options like **pytesseract** or a hosted OCR service are especially relevant. The best approach is to try a couple on a sample of your documents and see which gives the cleanest output.

","The course doesn't prescribe a specific tool — use whatever works best for your data. Some options people in the community reach for:

- **[docling](https://github.com/docling-project/docling)** — converts PDFs (and other docs) to structured Markdown/JSON, good for RAG.
- **[pymupdf4llm](https://pymupdf.readthedocs.io/en/latest/pymupdf4llm/)** / **PyMuPDF** — fast text extraction, Markdown output.
- **[unstructured](https://github.com/Unstructured-IO/unstructured)** — handles many document types and layouts.
- **[pytesseract](https://github.com/madmaze/pytesseract)** (Tesseract OCR) — for scanned/image-based PDFs that have no text layer.
- **Hosted OCR** such as Mistral OCR — if you'd rather call an API than run OCR locally.

Try a couple on a sample of your documents and see which gives the cleanest text. For how to chunk/prepare the extracted text afterwards, see ""[How should I prepare documents for RAG?](../module-1-rag/023_649c280e6d_how-should-i-prepare-documents-for-rag.md)"".",d8b28c47a0
"If my PDFs are actually images, do I need OCR, and which OCR tool is worth trying first?","Yes — if your PDFs are image-based scans with no text layer, you’ll need OCR.

The first OCR tool worth trying from the course context is **`pytesseract` (Tesseract OCR)**, since it’s specifically mentioned for scanned/image-based PDFs. If you’d rather use an API instead of running OCR locally, **Mistral OCR** is a hosted option.

A good approach is to try OCR on a sample of your PDFs and see which gives the cleanest text.","The course doesn't prescribe a specific tool — use whatever works best for your data. Some options people in the community reach for:

- **[docling](https://github.com/docling-project/docling)** — converts PDFs (and other docs) to structured Markdown/JSON, good for RAG.
- **[pymupdf4llm](https://pymupdf.readthedocs.io/en/latest/pymupdf4llm/)** / **PyMuPDF** — fast text extraction, Markdown output.
- **[unstructured](https://github.com/Unstructured-IO/unstructured)** — handles many document types and layouts.
- **[pytesseract](https://github.com/madmaze/pytesseract)** (Tesseract OCR) — for scanned/image-based PDFs that have no text layer.
- **Hosted OCR** such as Mistral OCR — if you'd rather call an API than run OCR locally.

Try a couple on a sample of your documents and see which gives the cleanest text. For how to chunk/prepare the extracted text afterwards, see ""[How should I prepare documents for RAG?](../module-1-rag/023_649c280e6d_how-should-i-prepare-documents-for-rag.md)"".",d8b28c47a0
"Should I just use the tool the course recommends for document extraction, or can I pick my own based on my data?",Yes — you can pick your own document extraction tool based on your data. The course doesn’t prescribe a specific one; it recommends trying a few options on a sample of your documents and choosing the one that gives the cleanest text for your use case.,"The course doesn't prescribe a specific tool — use whatever works best for your data. Some options people in the community reach for:

- **[docling](https://github.com/docling-project/docling)** — converts PDFs (and other docs) to structured Markdown/JSON, good for RAG.
- **[pymupdf4llm](https://pymupdf.readthedocs.io/en/latest/pymupdf4llm/)** / **PyMuPDF** — fast text extraction, Markdown output.
- **[unstructured](https://github.com/Unstructured-IO/unstructured)** — handles many document types and layouts.
- **[pytesseract](https://github.com/madmaze/pytesseract)** (Tesseract OCR) — for scanned/image-based PDFs that have no text layer.
- **Hosted OCR** such as Mistral OCR — if you'd rather call an API than run OCR locally.

Try a couple on a sample of your documents and see which gives the cleanest text. For how to chunk/prepare the extracted text afterwards, see ""[How should I prepare documents for RAG?](../module-1-rag/023_649c280e6d_how-should-i-prepare-documents-for-rag.md)"".",d8b28c47a0
What’s a decent way to get PDFs into Markdown or JSON for a retrieval project without too much cleanup?,"A decent option is **`pymupdf4llm`** for fast PDF-to-Markdown extraction, or **Docling** if you want higher-quality structured **Markdown/JSON** and can tolerate it being a bit slower.

If the PDFs are scanned/image-only, use **OCR** first, like **Tesseract (`pytesseract`)** or a hosted OCR service such as **Mistral OCR**.

Best advice from the course: **try a couple on a sample of your documents and see which gives the cleanest output**, then chunk/index that result.","The course doesn't prescribe a specific tool — use whatever works best for your data. Some options people in the community reach for:

- **[docling](https://github.com/docling-project/docling)** — converts PDFs (and other docs) to structured Markdown/JSON, good for RAG.
- **[pymupdf4llm](https://pymupdf.readthedocs.io/en/latest/pymupdf4llm/)** / **PyMuPDF** — fast text extraction, Markdown output.
- **[unstructured](https://github.com/Unstructured-IO/unstructured)** — handles many document types and layouts.
- **[pytesseract](https://github.com/madmaze/pytesseract)** (Tesseract OCR) — for scanned/image-based PDFs that have no text layer.
- **Hosted OCR** such as Mistral OCR — if you'd rather call an API than run OCR locally.

Try a couple on a sample of your documents and see which gives the cleanest text. For how to chunk/prepare the extracted text afterwards, see ""[How should I prepare documents for RAG?](../module-1-rag/023_649c280e6d_how-should-i-prepare-documents-for-rag.md)"".",d8b28c47a0
"For scanned documents, is there a simple local option instead of using a hosted OCR API?","Yes — for scanned/image-based PDFs, a simple local option is **pytesseract** (Tesseract OCR). It runs OCR locally instead of calling a hosted API.

","The course doesn't prescribe a specific tool — use whatever works best for your data. Some options people in the community reach for:

- **[docling](https://github.com/docling-project/docling)** — converts PDFs (and other docs) to structured Markdown/JSON, good for RAG.
- **[pymupdf4llm](https://pymupdf.readthedocs.io/en/latest/pymupdf4llm/)** / **PyMuPDF** — fast text extraction, Markdown output.
- **[unstructured](https://github.com/Unstructured-IO/unstructured)** — handles many document types and layouts.
- **[pytesseract](https://github.com/madmaze/pytesseract)** (Tesseract OCR) — for scanned/image-based PDFs that have no text layer.
- **Hosted OCR** such as Mistral OCR — if you'd rather call an API than run OCR locally.

Try a couple on a sample of your documents and see which gives the cleanest text. For how to chunk/prepare the extracted text afterwards, see ""[How should I prepare documents for RAG?](../module-1-rag/023_649c280e6d_how-should-i-prepare-documents-for-rag.md)"".",d8b28c47a0
"Can I reuse the workshop material in my own capstone project, or is it only for the class?","Yes — you can reuse the workshop material in your own capstone project. The context says that since dlt is open-source, you can use the content of the workshop for a capstone project and even directly in your projects.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
"Since dlt is open source, does that mean I’m allowed to use this workshop content in other projects too?","Yes — according to the workshop context, since dlt is open-source, you can use the workshop content in your own projects, including a capstone project or other Zoomcamps. Feel free to ask questions or use it directly in your projects.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
"Can I take what I learned here and apply it to a different Zoomcamp project, like MLOps?","Yes — you can apply what you learned to another Zoomcamp project, including MLOps. The workshop materials are open-source, and the context explicitly says you can use them for other Zoomcamps like the MLOps Zoomcamp project.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
Is it okay if I share the workshop content with others or use it as part of my own work?,"Yes. Since dlt is open-source, you can use the workshop content in your own projects and even for a capstone project. You can also ask questions about it or use it directly in your work.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
"What’s the intended use of this workshop material — just learning, or can I actually build with it?","You can actually build with it.

The workshop materials are not just for learning — since dlt is open-source, you can use the content for your own projects, including a capstone project, and even use it in other Zoomcamps.","Since dlt is open-source, you can use the content of this workshop for a capstone project. As the main goal of dlt is to load and store data easily, you can even use it for other Zoomcamps (like the MLOps Zoomcamp project). Feel free to ask questions or use it directly in your projects.",d850a0e7ea
How do I start a fresh dlt project for loading data from cloud storage?,"Start a fresh dlt project for loading from cloud storage with:

```bash
dlt init filesystem duckdb
```

More directions are available at: https://dlthub.com/docs/tutorial/filesystem","Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
What command should I run to initialize a filesystem-to-duckdb dlt setup?,"Run:

```bash
dlt init filesystem duckdb
```","Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
I'm trying to set up dlt for cloud loading — what's the first terminal command?,`dlt init filesystem duckdb`,"Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
How do I create a new dlt project using filesystem and duckdb?,"Use:

```bash
dlt init filesystem duckdb
```

This creates a new dlt project for loading from cloud using filesystem and duckdb.","Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
Where should I begin if I want to bootstrap a dlt ingestion project from cloud files?,"Begin with:

```bash
dlt init filesystem duckdb
```

This is the recommended starting command for setting up a new dlt project when loading from cloud files. More directions are available at the dlt filesystem tutorial on dlthub.com.","Start with the following command on the command line:

```bash
 dlt init filesystem duckdb
```

More directions can be found at [dlthub.com](https://dlthub.com/docs/tutorial/filesystem)",6bceeebf04
"Why do I get `Table notion_pages___homework does not exist` when I try to open it with `db.open_table(""notion_pages___homework"")`?","This usually means the table name in your pipeline/settings is still inconsistent.

According to the workshop context, the error happens because not all instances of **`employee_handbook`** were changed to **`homework`** in your pipeline settings. So `db.open_table(""notion_pages___homework"")` fails because that table wasn’t actually created under that name.

A useful check is to list the existing tables with:

```python
db.table_names()
```

Then open the table using the exact name that appears there.","The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
"I already changed the table name to `notion_pages___homework`, but I still see a `FileNotFoundError` — what else could be wrong?","The most likely issue is that you changed the table name in one place, but not everywhere in the pipeline.

According to the context, if `db.open_table(""notion_pages___homework"")` still raises `FileNotFoundError`, it usually means some instances of **`employee_handbook`** were not changed to **`homework`** in your pipeline settings.

So check for leftover references to the old name.","The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
What setting did I forget to update if my dlt pipeline is still looking for `employee_handbook` instead of `homework`?,You likely forgot to change all instances of **`employee_handbook`** to **`homework`** in your **pipeline settings**.,"The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
How can I fix the issue where the pipeline says I need to call `db.create_table(...)` for `notion_pages___homework`?,"The issue usually means not all references were updated from `employee_handbook` to `homework` in your pipeline settings.

Make sure the table name is consistently:

```python
dbtable = db.open_table(""notion_pages___homework"")
```

If it still fails, check your pipeline config and replace every remaining `employee_handbook` reference with `homework`.","The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
"If my homework table isn't found, is it because some `employee_handbook` references are still left in the pipeline config?","Yes — if your homework table isn’t found, it can be because not all `employee_handbook` references were changed to `homework` in your pipeline settings.","The error indicates that you have not changed all instances of ""employee_handbook"" to ""homework"" in your pipeline settings.",0407213397
Why do I get `FileNotFoundError: Table notion_pages___homework does not exist` when I run `main()`?,"Because your pipeline settings still contain some `employee_handbook` references. You need to replace **all** instances of `""employee_handbook""` with `""homework""` so the table name is created/opened correctly.

","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
"How can I fix the error about `db.create_table(notion_pages___homework, data)` not finding the table?","The error usually means your pipeline still has some references to the old table name.

Make sure you changed **all instances of `""employee_handbook""` to `""homework""`** in your pipeline settings.

If needed, also verify the table name you’re opening is correct:

```python
dbtable = db.open_table(""notion_pages___homework"")
```","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
"In the dlt workshop homework, what table name should I open on line 3?","Open this table on line 3:

```python
notion_pages___homework
```","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
"If `notion_pages___homework` is missing, what should I change in the code to make `main()` work?","Change the table name in `main()` so it opens the correct table:

```python
dbtable = db.open_table(""notion_pages___homework"")
```

If `notion_pages___homework` is missing, make sure you’re using that exact table name in the code.","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
"Why is my script failing with a table-not-found message for `notion_pages___homework`, and what is the correct `db.open_table(...)` call?","Your script is failing because the table name in your pipeline settings is still inconsistent — you likely changed some references from `employee_handbook` to `homework`, but not all of them.

The correct call is:

```python
dbtable = db.open_table(""notion_pages___homework"")
```

If you want, I can also help you check for the leftover `employee_handbook` references.","Make sure you open the correct table in line 3:

```python
dbtable = db.open_table(""notion_pages___homework"")
```",e95bfafc0e
How can I list all the tables in the database?,You can use the `db.table_names()` method to list all the tables in the database.,You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
Is there a way to see which tables exist in the db?,Yes — you can use `db.table_names()` to list all the tables in the database.,You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
What method do I use to get the table names from the database?,Use the `db.table_names()` method.,You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
How do I check the tables available in my database?,Use the `db.table_names()` method to list all the tables in your database.,You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
Where can I find the names of all tables in the db?,Use `db.table_names()` to list all the table names in the database.,You can use the `db.table_names()` method to list all the tables in the database.,e394e6f738
"Does DLT already support ClickHouse or StarRocks connectors, or are they not available yet?","Currently, DLT does **not** support ClickHouse or StarRocks connectors yet. It’s open to community contributions to add them.","Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
Can I use DLT to load data into ClickHouse or StarRocks right now?,I don't know.,"Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
Is there any built-in connector in DLT for ClickHouse and StarRocks?,"No. Currently, DLT does **not** have built-in connectors for **ClickHouse** or **StarRocks**. It’s open to community contributions to add them.","Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
Why isn't there a connector for ClickHouse or StarRocks in DLT yet?,"Currently, DLT does not have connectors for ClickHouse or StarRocks yet. The context says the project is open to community contributions to add these connectors.","Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
"If DLT doesn't support ClickHouse or StarRocks, can the community add those connectors?",Yes — the context says dlt is open to community contributions to add ClickHouse or StarRocks connectors.,"Currently, DLT does not have connectors for ClickHouse or StarRocks but is open to contributions from the community to add these connectors.",cd19f122f3
Why am I getting a 401 Unauthorized error when the notebook calls the Notion search API?,"A 401 Unauthorized error usually means the notebook does not have valid access to the Notion API. Check:

1. The API key/token is correct and has the right permissions.
2. The notebook is calling the correct Notion API endpoint.
3. The token has not expired.
4. Your access/configuration settings are set up properly.

If it still fails, review the Notion API documentation and authentication steps.","If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
What should I check first if my notebook says it doesn't have secret access to api.notion.com/v1/search?,"First check your **API key / Notion integration token and its permissions**.  

This error usually means an authorization issue with the Notion API, so verify that:

- the key is correct
- it has the needed permissions
- it hasn’t expired
- your notebook/config is using the right credentials and endpoint

If it still fails, double-check the access configurations.","If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
Could this Notion API unauthorized error be because my token is wrong or expired?,"Yes — based on the context, a 401 Unauthorized / Notion API auth error can be caused by the token being wrong or expired.

You should check:
- that you’re using the correct API key/token
- that it has the right permissions
- whether the token has expired and needs regeneration
- that the API endpoint and access configuration are correct

If it still fails, review the Notion API documentation and authentication steps.","If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
How do I fix permission or authentication issues with the Notion API in the workshop notebook?,"If you see `Notebook does not have secret access` or a `401 Client Error: Unauthorized` from `api.notion.com`, it usually means there’s a Notion authorization problem.

To fix it:

1. Check that you’re using the correct Notion API key.
2. Make sure the key has the right permissions.
3. Verify you’re calling the correct Notion API endpoint.
4. If your token expired, regenerate it.
5. Double-check your app’s access/configuration settings.

If it still doesn’t work, review the Notion API documentation and confirm that all authentication steps are set up correctly.","If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
"If the notebook can’t access Notion, what configuration or endpoint settings should I verify?",I don’t know.,"If you encounter this error, it typically indicates an authorization issue with the Notion API. Here’s how you can resolve it:

1. **Check API Key**: Ensure that you are using the correct API key with appropriate permissions.
2. **Verify API Endpoint**: Confirm that you are hitting the correct Notion API endpoint.
3. **Token Expiry**: Check if your token has expired and regenerate it if necessary.
4. **Configurations**: Double-check all access configurations in your application.

If the error persists, review the API documentation and make sure all necessary authentication steps are correctly implemented.",0dfba07ace
How can I install the newer requests version from the source when pip keeps giving me v2.28?,"Install it directly from the source with:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```

This should force pip to install `requests` v2.32.3 instead of v2.28.","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
What pip command should I use to force requests v2.32.3 instead of the older release?,"Use:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
Is there a direct install URL for requests 2.32.3 if the normal install pulls the wrong version?,"Yes — use this direct install URL:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
Why am I seeing a 401 Client Error while trying to set up lancedb with requests?,"A 401 Client Error usually means an authentication or access issue: the key may be incorrect, expired, or you may need to grant access/permissions to it.

For `lancedb` with `requests`, also make sure you have the required `requests` version by installing it directly from the source:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```

If the 401 persists, check that your key is valid and has the necessary access.","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
"What should I check if my key is getting a 401 error, and how do I get the right requests package version?","If your key is getting a **401 error**, check whether:

1. The key is **correct**
2. The key has the **right permissions / access granted**
3. The token has **expired** and needs to be regenerated
4. Your app is using the **correct API endpoint/configuration**

To get the correct `requests` package version directly from source, use:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```","If you encounter a 401 Client Error, it may indicate the need to grant access to the key or that the key is incorrect.

To install the correct version directly from the source, use the following command:

```bash
pip install ""requests @ https://github.com/psf/requests/archive/refs/tags/v2.32.3.zip""
```",4b30b918bc
